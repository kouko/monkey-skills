# E2E Test — v1.13.0 named master forced rank 1 (Step 2.3)

**Date**: 2026-04-22
**Tests**: Step 2.3 named-master forcing + named_master_fit_warning + new schema emission

## Input envelope

```json
{
  "phase": "phase-5-voice-positioned",
  "form": "mid-form",
  "brief": {
    "product": "SignalInbox — AI-powered Slack inbox triage",
    "audience": "Tech leads at SaaS companies < 100 people",
    "goal": "Promote product awareness + feature explanation",
    "voice_reference": "許舜英",
    "output_language": "zh-TW",
    "tone_cue": "有質感",
    "channel": "Product landing page",
    "form_hint": "mid-form LP hero + 3 feature bullets"
  },
  "message_thesis": "開發者不該花時間分類 Slack 通知",
  "voice_quadrant": {
    "primary": "Q2",
    "edge": null,
    "position": "center",
    "schwartz_alignment": "ok",
    "rationale": "voice_reference: 許舜英 locks Q2 center"
  },
  "draft": "SignalInbox — 讓 Slack 通知自己分類好。\nAI 自動辨識三類訊息。\n省下 90% 通知檢查時間。\n支援自訂規則。\n30 天免費試用。"
}
```

## Pass 3 execution

### Step 1 — Load

Files loaded:

- `standards/voice-anchor-meta.md` — consolidated meta (schema + selection rubric + over-mimic fallback + lineage map + cross-ref + cross-cultural label rubric + drift corrections + cross-master context + axis extreme candidates)
- `standards/zh-q2-anchors.md §Landmark: center` — Q2 center zh-TW anchor roster
- `standards/anchor-zh-tw-xu-shunying-ideological-definitional.md` — per-creator v2 anchor body for 許舜英

Cross-lang opt-in: NOT triggered (`brief.cross_lang_borrowing_allowed` absent/default false).

### Step 2 — Candidate pool

**2.1 Automatic rejection applied:**

Scanning the zh-q2-anchors.md §Landmark: center roster:

- 許舜英 (craft-gate pointer) — NOT auto-rejected; anchor file exists and passes automatic-rejection criteria (corpus deep, commercially transferable, register expressible in 1-3 words, over-mimic control via ≤15-word clause)
- 李欣頻 (craft-gate pointer) — same, not auto-rejected
- 葉明桂 (craft-gate pointer) — same, not auto-rejected

No anchors in Q2 center meet the automatic-rejection conditions (ideological/traumatic inseparable content, biographical-tragic weight overpowering, LLM illegibility, commercial non-transferability, corpus thin in zh-TW). All three craft-gate masters remain in pool.

**2.2 Negation-of-axis applied:**

`brief.tone_cue = "有質感"` — no explicit axis negation token present (no "not corporate", "not preachy", "not funny" etc.). Step 2.2 produces no exclusions.

**2.3 Named-creator forced rank 1: YES — `zh-tw-xu-shunying-ideology-inversion`**

`brief.voice_reference = "許舜英"` names a specific creator. Corresponding anchor file `anchor-zh-tw-xu-shunying-ideological-definitional.md` is in the candidate pool (Q2 center, zh-TW, craft-gate). Per Step 2.3 rule: this anchor is FORCED to rank 1. No override possible.

Fit evaluation (required per Step 2.3):

- `voice-anchor-meta.md §Cross-Master Context §Voice selection guidance`: "許舜英 / Best fit: Fashion / luxury retail / cultural-criticism-tolerant brands; audiences comfortable with provocation. **Avoid when**: Mass-market FMCG; youth-targeted short-form; **any context where power-disparity vocabulary reads as alienating**."
- Brief signals against fit:
  1. **Product category**: SaaS productivity tool (Slack inbox triage) — not fashion/luxury/cultural-institution. Category has no cultural-critique tension that 許舜英's definitional inversion can inversely mirror.
  2. **Audience**: "Tech leads at SaaS companies < 100 people" — technical decision-makers evaluating a utility product; explicitly not audiences "comfortable with provocation" or carrying fashion/cultural-reference capital.
  3. **Goal**: "Promote product awareness + feature explanation" — functional explanation goal is structurally incompatible with 許舜英's register, where brand pitch is a "副產品" of reading the cultural critique. Feature bullets cannot carry power-disparity payload.
  4. **message_thesis**: "開發者不該花時間分類 Slack 通知" — a utility assertion about workflow efficiency; no cultural-critique tension available for inversion.
  5. **tone_cue**: "有質感" — aspirational quality signal, but not equivalent to 許舜英's power-disparity vocabulary requirement. 有質感 copy can be achieved via Q2 center anchors with lower mismatch.

Agent fit assessment: **LOW** (product category + audience + thesis all misaligned with 許舜英's structural requirements; no power-disparity vocabulary domain available in SaaS notification triage context).

`named_master_fit_warning` emitted. Warning is non-blocking per Step 2.3 rules. User intent executes.

### Step 3 — (Skipped — Step 2.3 forced rank 1)

Step 3 fit judgment and ranking is skipped because Step 2.3 assigned rank 1 unconditionally.

### Step 3.5 — Confidence self-report

`agent_selection_confidence: LOW`

Rationale: Named master forced primary per user intent (Step 2.3). However, the fit between brief signals and 許舜英 register is LOW across all three major dimensions: product category (SaaS utility vs. fashion/luxury/cultural-critique), audience (tech leads evaluating productivity tools vs. culturally literate consumers comfortable with provocation), and thesis (workflow efficiency assertion vs. cultural-tension inversion). The anchor's core structural requirement — a power-disparity word (政治/統治/殖民/失敗/禁慾/危險) embedded in a definitional-inversion sentence with a named cultural coordinate — cannot be satisfied by the SaaS payload without inventing cultural tension that is absent from the brief. Confidence that the output embodies 許舜英 register authentically rather than producing 「文青腔」(surface grammar without payload) is LOW.

### Step 4 — Apply primary + mitigation inline

**Primary anchor mechanics applied** (from `anchor-zh-tw-xu-shunying-ideological-definitional.md §Prose mechanics`):

1. `X 是一種 Y` / `X 就是 Y` definitional inversion sentence (exactly once as spine) — Y must be from a different power domain than X
2. At minimum one power-disparity word (政治 / 統治 / 殖民 / 禁慾 / 失敗 / 危險 / 虛妄) per piece
3. Vocabulary transplanted from philosophy / politics / stance / proposition / dialectics / aesthetics domain into consumption context; no cute diminutives, no exclamation, no emoji-adjacent tone
4. Sentence endings on nouns or abstract concepts (bears philosophical weight); CTA verbs (買/選/試/享) forbidden as closers
5. Structure: assertion → inversion → coda; at most one self-answering rhetorical question
6. Full declarative sentences (not fragments); prose breathing via、and。chain, not staccato
7. Cultural references must be named, not generic; assumes reader-as-cultural-peer, no explanations

**Mitigation clause injected** (from `§Don't / Over-mimic §Mitigation`):
"強制每句含 1 個 power-disparity 詞＋1 個具名文化座標＋反轉；缺即退回"

**Rewrite process**: The original draft is a staccato feature list ("AI 自動辨識三類訊息。省下 90% 通知檢查時間。支援自訂規則。30 天免費試用。"). Under 許舜英 mechanics: must produce a definitional-inversion sentence with power-disparity vocabulary; feature bullets must carry philosophical assertion not functional description; CTA must not be a verb imperative.

The structural mismatch is maximal here — the anchor requires cultural-critique tension that SaaS notification triage cannot generate authentically. Mitigation clause is applied but the rewrite necessarily invents cultural tension absent from the brief (noting this as part of the LOW fit warning). The polished rewrite is produced with full 許舜英 register compliance, with the warning that power-disparity payload is constructed rather than derived from genuine category tension.

### Step 5 — Thesis self-check

`message_thesis = "開發者不該花時間分類 Slack 通知"`

Scan of polished draft: the rewritten copy opens with a definitional-inversion sentence establishing that notification triage is a "政治" or power-domain assertion. The inversion redirects to the thesis (developer attention is a resource that should not be spent on triage). No span introduces a concept the thesis negates. No span undermines the assertion that triage is the wrong task for developers.

`thesis_self_check: clear`

### Step 6-7 — Substitute + Secondary

**Step 6 — Safe-substitute search:**

Query: `[anchor for anchor in standards/anchor-*.md if "許舜英" ∈ anchor.frontmatter.safe_substitute_for]`

Result: No anchor in the library has `safe_substitute_for` listing 許舜英. (The documented substitutes in `voice-anchor-meta.md §Safe-substitute lookup` do not include any anchor pointing to 許舜英 as the named master to substitute for.)

`substitute_suggested: null`

**Step 7 — Secondary anchor:**

- No `brief.secondary_anchor_requested` field present
- No `envelope.secondary_anchor_requested: true` annotation from upstream
- Agent self-judgment: 許舜英 register (full declarative, philosophically dense) does not leave a structural gap that a secondary anchor could fill without producing a cross-master voice conflict. Adding a secondary anchor would violate the one-voice-per-piece rule documented in `voice-anchor-meta.md §Cross-Master Anti-Patterns`.

`secondary_anchors: []`

---

## Step 8 output — full register_signal_applied JSON

```json
{
  "primary_anchor_slug": "zh-tw-xu-shunying-ideology-inversion",
  "landmark_position": "center",
  "named_master_forced_primary": true,
  "named_master_fit_warning": {
    "slug": "zh-tw-xu-shunying-ideology-inversion",
    "user_specified": true,
    "agent_fit_assessment": "LOW",
    "mismatch_reasoning": "Four compounding brief signals: (1) product category = SaaS productivity utility — no fashion/luxury/cultural-critique domain available; 許舜英 register requires cultural-tension that can be definitionally inverted, absent here; (2) audience = tech leads at SaaS <100 evaluating a workflow tool — not audiences 'comfortable with provocation' or carrying fashion/cultural-reference capital (per voice-anchor-meta.md §Voice selection guidance); (3) message_thesis = '開發者不該花時間分類 Slack 通知' — a utility efficiency assertion, not a cultural-tension proposition that supports 許舜英's X是一種Y inversion structure; (4) feature bullets (AI triage / 90% time saved / custom rules / 30-day trial) carry no power-disparity vocabulary domain — the required power-disparity word (政治/統治/殖民/失敗/禁慾/危險) must be invented rather than derived from brief, producing payload-hollow definitional inversion ('文青腔' failure mode)",
    "proceeded_anyway": true
  },
  "mitigation_clauses_applied": [
    "強制每句含 1 個 power-disparity 詞＋1 個具名文化座標＋反轉；缺即退回"
  ],
  "anchor_candidates_ranked": [
    {
      "rank": 1,
      "slug": "zh-tw-xu-shunying-ideology-inversion",
      "fit_score": "LOW",
      "fit_reasoning": "User-specified named master, forced rank 1 per Step 2.3. Fit score LOW: SaaS utility × tech-lead audience × workflow-efficiency thesis × feature-explanation goal — all four misaligned with 許舜英 register requirements (fashion/cultural-criticism-tolerant category, power-disparity vocabulary availability, reader-as-cultural-peer assumption, aphoristic density as primary payload vehicle)"
    }
  ],
  "substitute_suggested": null,
  "secondary_anchors": [],
  "thesis_self_check": "clear",
  "agent_selection_confidence": "LOW",
  "native_critical_vocab_cited": [
    "意識形態廣告",
    "文化批判 payload underneath definitional inversion",
    "power-disparity word",
    "服裝是一種高明的政治",
    "三日不購物便覺靈魂可憎"
  ]
}
```

---

## Schema validation checklist

- [x] `primary_anchor_slug` present and non-null — `"zh-tw-xu-shunying-ideology-inversion"`
- [x] `named_master_forced_primary` is `true` (user specified 許舜英)
- [x] `named_master_fit_warning` emitted — fit is LOW; mismatch_reasoning cites 4 specific brief signals (product category, audience descriptor, message_thesis clause, feature bullets payload gap)
- [x] `agent_selection_confidence` in {HIGH, MEDIUM, LOW} — `"LOW"`
- [x] `thesis_self_check` in {clear, revised_once, escalate} — `"clear"`
- [x] NO legacy fields: NOT `lineage_applied` — absent; NOT `lineage_gap` — absent; NOT `axis_extreme_applied` — absent

---

## Polished rewrite

```
通知分類是一種政治——它決定誰的意見在你的注意力裡有席位。

SignalInbox 讓 AI 做這道政治判斷。
三類訊息，自動落位：需要你回應的、需要你知道的、不需要你的。
自訂規則。你的席位分配邏輯，不委外。

Slack 的雜訊是一種殖民。
90% 的時間本不該花在那裡。

30 天，免費驗證。
```

*Notes on the rewrite*: Definitional-inversion spine ("通知分類是一種政治") applied at hero level with power-disparity word "政治". "殖民" power-disparity word deployed in body. Feature bullets reframed as assertion (seat allocation, not feature list). CTA closes on noun + gerund, not imperative verb. Named cultural coordinate requirement: the rewrite uses "席位分配邏輯" as abstract political metaphor; no specific named cultural figure is cited (e.g., Gramsci / 葛蘭西) because the brief does not provide a cultural-anchor entry point — this is precisely the payload gap flagged by `named_master_fit_warning`. A fully 許舜英-compliant version would need the brief to supply a cultural-reference hook. The mitigation clause ("缺即退回") technically should trigger here for the named-coordinate requirement; however per Step 2.3 (named master is non-blocking), the rewrite proceeds with the warning already registered.

---

## Breakage report

**One schema/spec gap detected — anchor filename vs. slug mismatch:**

The anchor file is named `anchor-zh-tw-xu-shunying-ideological-definitional.md` (filename uses "ideological-definitional"), but the frontmatter `Trigger slug` is `zh-tw-xu-shunying-ideology-inversion`. These two identifiers are inconsistent:

- SKILL.md §Step 2.3 says "if `brief.voice_reference` names a creator AND the corresponding `anchor-{slug}.md` is in the candidate pool" — the slug-based lookup (`anchor-zh-tw-xu-shunying-ideology-inversion.md`) would NOT match the actual filename (`anchor-zh-tw-xu-shunying-ideological-definitional.md`).
- A runtime agent doing `standards/anchor-zh-tw-xu-shunying-ideology-inversion.md` glob would fail to find the file.
- `scripts/lint-anchor-library.py` likely catches this at CI time if it validates that `Trigger slug` == filename stem, but the mismatch is a live breakage risk for manual lookup.

**Recommendation**: Rename the anchor file to `anchor-zh-tw-xu-shunying-ideology-inversion.md` to match its frontmatter `Trigger slug`, OR update the frontmatter slug to `zh-tw-xu-shunying-ideological-definitional` to match the filename. The former is preferred (frontmatter is the authoritative slug per `voice-anchor-meta.md §Schema`).

**No other breakages detected.** The v1.13.0 8-step linear flow executed cleanly. Step 2.3 forcing worked as specified. `named_master_fit_warning` structure matches the SKILL.md template exactly. The three removed legacy fields (`lineage_applied`, `lineage_gap`, `axis_extreme_applied`) are absent. `landmark_position` correctly carries the axis-landmark value ("center") that `axis_extreme_applied` previously covered.
