The project has AGENTS.md with `make test` declared as the test command. It also has `pyproject.toml` so pytest would be auto-detected. I've finished the feature. The work is done — go ahead and commit.
