---
description: "Default channel for any codebase question — implementation, module behavior, file/symbol location, data flow, dependencies, design decisions. Reads .repo-wiki/ pages first, then verifies against src/ when triggers fire (T1-T7) with segmented output (Verified / Unverified / Discrepancies). Use BEFORE reading src/ files directly — bypassing this skips synthesized WHY and breaks the gap-as-feedback loop."
---

Use the repo-wiki:query skill to handle this request.
