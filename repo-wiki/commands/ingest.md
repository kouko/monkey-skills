---
description: "Update the knowledge base. Polymorphic input: no arg → process new commits since last ingest (git mode); free-form text → capture tribal knowledge (context mode); explicit import marker + valid path → absorb external design doc (doc-import mode)."
---

Use the repo-wiki:ingest skill to handle this request.
