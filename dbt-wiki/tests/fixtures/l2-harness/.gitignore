# dbt run artifacts and the built DuckDB database — always regenerated by
# the dbt_build fixture (dbt-wiki/tests/conftest.py), never committed.
target/
logs/
dbt_packages/
*.duckdb
*.duckdb.wal
.user.yml
