# Real-run e2e validation reports (Task 11) — regenerated by a live, quota-
# spending `claude -p` invocation via test_e2e_validation.py. The report bodies
# capture a point-in-time model transcript + grader scores; they are audit
# artifacts, not shippable source, so their contents are never committed. This
# .gitignore itself IS committed so the directory exists on a fresh checkout.
*.json
