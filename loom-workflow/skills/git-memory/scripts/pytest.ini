[pytest]
# Redirect pytest cache outside the skill directory so .pytest_cache/ doesn't
# violate the flat-subfolder skill structure rule (see conftest.py for context).
cache_dir = /tmp/pytest_cache_git_memory_scripts
