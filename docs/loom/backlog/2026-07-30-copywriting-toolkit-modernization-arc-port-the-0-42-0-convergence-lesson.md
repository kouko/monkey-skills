---
name: 2026-07-30-copywriting-toolkit-modernization-arc-port-the-0-42-0-convergence-lesson
description: copywriting-toolkit modernization arc — port the 0.42.0 convergence lessons
status: SHIPPED
origin: 2026-07-30 three-stream audit (structure/mechanics, architecture/convergence, knowledge layer) after the description-diet arc; user committed to both knives.
---

- Original inner order held: P3 verdict semantics FIRST, then P2 validator pinning the
  post-P3 vocabulary.
- Origin: 2026-07-30 three-stream audit (structure/mechanics, architecture/convergence,
  knowledge layer) after the description-diet arc; user committed to both knives.
- What:
  1. P3 — replace the flag-mode "2+ 🟡 → NEEDS_REVISION" rule over qualitative prose
     dimensions (form-check §8b, voice-consistency; evaluator agent :64-68) with a
     closed blocking class + round-2 prior-findings verification duty (no re-raising
     in new words), per the loom-code 0.42.0 shape. Caps already exist (2 revise/phase,
     bounce ≥3, total ≥4) — only the verdict semantics need porting. Checklist mode is
     already close (FAIL_FATAL gates, FIXABLE ≈ recorded).
  2. P2 — envelope validator script (schema + counter monotonicity + manual-PASS ban),
     loom_gate_markers-style; today the whole state machine is prose-only self-report
     (CLAUDE.md:146,188-190,282-283 names the failure it cannot enforce).
  3. Ride-alongs surfaced by the audit + knife 1: FIXABLE auto-revise runs in the same
     context that re-declares the gate passed (worker≠judge seam); intake's
     judgment-shaped BLOCKED halt lacks a verifiable action; alt-entry minimal shape's
     express_mode_used has no writer (CLAUDE.md:290 allows omission — validator must
     handle); persuasion-psychology-anchor.md 5× copies (documented accepted debt —
     build the sync script); neta overlay output example omits passthrough fields.
  - Dogfood: reuse the 2026-07-30 evidence-class trap probe recipe
    (docs/skill-dogfood/… + docs/loom/memory/docs-review-dogfood-must-probe-the-evidence-class-trap.md),
    adapted to a cold copywriter-evaluator.
