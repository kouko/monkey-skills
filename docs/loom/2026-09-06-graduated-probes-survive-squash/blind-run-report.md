# 畢業探針在壓過的 main 上仍然綠 — 我試了什麼、發生了什麼

於 2026-09-07，在專案 `ebc3ddc2` 的乾淨副本中試跑。

## 你要求的事，逐條驗證

### 1. 我在合併後的 main 上跑整包測試，沒有任何測試因為「找不到自己 change 的 commit」而紅；今天那支紅的測試變綠。

- **我怎麼試**：我不能真的合併到 main，因此把同一批探針分別放進「保留完整分支歷史」與「整個分支壓成一顆 commit」的乾淨副本。
- **發生了什麼**：完整歷史形狀有 11 個通過、1 個因缺少遠端 main 基準而跳過；壓成一顆 commit 的形狀有 12 個全部通過。原本會因找不到自身 commit 而紅的案例，在兩種形狀都通過。
- **證據**：`fix-round-6-acceptance-1` — 完整歷史為 `FAILED (0) / SKIPPED (1) / 11 passed`；壓平形狀為 `FAILED (0) / SKIPPED (0) / 12 passed`。
- **判定**：部分 — 已重現合併後的一顆 commit 形狀並通過，但沒有真的合併到 main。

### 2. 我拿一支故意依賴自己 plan commit 的探針丟進畢業前的排練，排練是紅的，並指名是哪一支、為什麼；把那個依賴拿掉之後排練轉綠。

- **我怎麼試**：我建立一次性的本機 Git 專案，讓探針搜尋自己那顆 plan commit 的標題，再分別排練完整與壓平歷史；接著把它改成只檢查成品中的一般檔案，再跑一次。
- **發生了什麼**：第一次完整歷史通過，壓平形狀失敗；輸出指名該探針，並顯示原因是自己的 plan commit 標題已不存在。移除歷史依賴後，兩種形狀都各有 1 個通過、0 個失敗。
- **證據**：`fix-round-6-acceptance-2` — 第一次為 `CI 1 passed / SQUASHED SHAPE FAILED (1)`；第二次兩邊皆為 `FAILED (0) / 1 passed`。
- **判定**：可以 — 紅燈會指出探針與原因，移除依賴後確實轉綠。

### 3. 我對一個已經出貨、intent 已關閉的 change 問 checker「plan 定稿後有沒有被亂改」，它不會給我一個看起來像「有人亂改了」的 BLOCK，而是說這個問題對已合併的 change 不適用。

- **我怎麼試**：我查詢一個已關閉、且合併時失去 plan commit 的既有 change；再查詢本次仍在進行、plan commit 還在的 change；最後用一次性專案模擬「尚未關閉且缺少 plan commit」。
- **發生了什麼**：已出貨的案例明確回覆「不適用」且成功結束；本次 change 安靜通過；尚未關閉又缺少定稿 commit 的案例仍明確封鎖並回覆找不到 plan commit。
- **證據**：`fix-round-6-acceptance-3` — 三次結束碼依序為 `0`、`0`、`1`；最後一次輸出 `BLOCK ... no plan commit found`。
- **判定**：可以 — 已出貨與仍在進行的情況沒有被混為一談。

### 4. build 站的畢業段落寫明這條規則（畢業探針不得依賴自己 change 的 commit 存在），而且有測試釘住那句話，改掉就紅。

- **我怎麼試**：我先讀畢業說明，確認它明說會排練壓成一顆 commit 的形狀，也明說基準分支必須是目前分支的祖先；測試原句一次，再只在乾淨副本暫時刪掉壓平形狀那句並重跑，之後逐字復原。
- **發生了什麼**：原句存在時測試通過；刪句後同一測試失敗，訊息說讀者無法知道還有壓平形狀。復原後副本沒有留下修改。
- **證據**：`fix-round-6-acceptance-4` — 完整文字時 `1 passed`；刪句後 `1 failed`；復原後差異檢查通過。
- **判定**：可以 — 規則可讀，而且測試確實會在句子消失時變紅。

### 5. 整包測試通過，改到的 SKILL.md 仍在字數帽內。

- **我怎麼試**：我在乾淨副本執行指定的兩段整包測試，再以指定算法計算操作說明的字數；另外核對審查紀錄中在沙箱外完成的同一套測試。
- **發生了什麼**：我的第一段測試有 2092 個通過、1 個跳過、1 個預期失敗、1 個環境失敗，因此第二段沒有開始。失敗仍是沙箱不准另一個 Python 環境寫入既有套件快取。審查紀錄中的沙箱外執行已得到第一段 2089 個通過、1 個跳過、1 個預期失敗，第二段 183 個通過、1 個跳過。現在的字數是 3320，低於 3750 上限。
- **證據**：`fix-round-6-acceptance-5`、`round-5-package-tests-3d3422f7` — 本次沙箱輸出為 `1 failed, 2092 passed, 1 skipped, 1 xfailed`；已記錄的沙箱外輸出為 `2089 passed, 1 skipped, 1 xfailed; 183 passed, 1 skipped`；字數為 `3320`。
- **判定**：部分 — 字數符合，且較早版本有正式的整包全綠紀錄；我所在的沙箱無法替目前版本取得整包全綠證據。

### 對抗案例（審查者自己寫的攻擊）

- **我怎麼試**：我重跑三個先前會失敗的攻擊：同一份 intent 同時寫「進行中」與「已關閉」；遠端 main 落後於分支真正起點；遠端 main 已超前且和分支分岔。接著把三份攻擊檔各自整份跑完。
- **發生了什麼**：重複狀態現在會被當作格式錯誤並繼續封鎖。兩種不可信的 main 都會讓排練拒絕壓平，而不是猜一個基準製造綠燈。三個指定攻擊各自通過；三份攻擊檔合計 26 個案例全部通過。
- **證據**：`test_planedits_duplicatestatus_blocks` 為 `1 passed, 6 deselected`；`test_rehearsal_trunkbehindmergebase_failsloudly` 與 `test_rehearsal_trunkaheadofbranch_failsloudly` 各為 `1 passed, 11 deselected`；整檔結果依序為 `7 passed`、`12 passed`、`7 passed`。
- **判定**：可以 — 三個先前已知的阻擋邊界現在都被拒絕，完整攻擊集也全綠。

## 對你既有的資料做了什麼

沒有碰你的既有資料。這次只改了它自己新增的紀錄與測試，以及既有的檢查器、排練工具和一段操作說明；沒有讀寫或轉換你擁有的內容。

## 我替你決定了

- **怎樣才算已出貨** — 我選擇只有「關閉狀態已被 commit 且從目前版本找得到，同時目前的 intent 存在並可正確讀取」才算已出貨；少任何一項都當作仍在進行並繼續封鎖。日後改成較寬鬆的判定，成本是重新承擔誤放行風險並補齊邊界測試。
- **重複狀態怎麼處理** — 我選擇把同一份 intent 裡出現兩行狀態視為格式錯誤，絕不把其中的「已關閉」當作放行依據。日後若要容許重複狀態，必須定義優先順序，並重新驗證不會讓仍在進行的工作冒充已出貨。
- **基準分支不可信時怎麼處理** — 我選擇在 main 已越過分支起點，或本機 main 比遠端看到的版本新時停止排練，要求先 rebase 或 fetch，不猜哪一個歷史才是真的。日後若要自動猜測，必須實作真正的合併結果並重新驗證不會遺失上游內容。
- **怎樣製造壓平形狀** — 我選擇在既有乾淨副本內，把分支相對 main 的內容壓成一顆 commit，而不是再複製一次專案；改法會影響排練時間與遠端分支模擬，需重驗兩種形狀。
- **怎樣維持兩份同源探針** — 我選擇同步修改原始證據與常設副本，只允許它們的來源位置文字不同；日後拆開維護會失去兩份內容一致的保證。
- **規則放在哪裡** — 我選擇把壓平排練規則與基準分支條件放進既有的畢業說明，不另外新增一道規則；若日後拆開，需新增另一套登記與釘測試。
- **怎樣保存畢業探針** — 我選擇讓常設副本除了來源位置那一行外逐位元相同，方便後來的人直接比對；改成各自演進後，需另建防止兩份內容漂移的方法。
- **先前漏掉的阻擋問題** — 「重複狀態誤放行」、「兩種錯誤 main 造成不忠實壓平」與「操作說明沒寫前提」都已由修正處理，對應攻擊與文字釘測試現在通過。
- **審查證據不完整** — 先前缺少 branch-end 的攻擊與盲跑紀錄；現在三份攻擊檔、26 個結果與刷新後的盲跑結果都已呈現。
- **盲跑報告隱藏已知阻擋** — 上一版只寫套件環境問題，漏掉三個已知失敗邊界；本版逐項揭露它們原本攻擊什麼、現在發生什麼。
- **套件測試環境問題** — 先前由我提出的問題已由沙箱外的正式執行關閉；本次仍如實保留目前 HEAD 在沙箱內無法完整重跑的限制。
- **沒有駁回重要問題** — round 5 所有嚴重或重要發現都以修正、補齊證據或刷新報告處理，沒有任何一項被駁回。

## 我不確定你要不要的事

沒有產品選擇上的未決問題。唯一仍缺的是在可寫入既有套件快取的環境，對目前 HEAD 再取得一次整包全綠紀錄。

## 語言與模板檢查

| 內容種類 | 結果 | 證據 |
|---|---|---|
| 計畫 | 符合；工作內容為英文，使用者原話保留原語言 | `plan.md` |
| 規格 | 不適用；本次沒有規格 | `intent.md:4` |
| 審查發現 | 符合；使用 Conventional Comments 標籤 | `branch-end-round-5-verdicts` |
| 證據 | 符合；內部證據為英文 | `test_abuse_branch_end_plan_edits.py`, `test_abuse_branch_end_squashed_shape.py`, `test_abuse_branch_end_gate_text.py` |
| 測試說明 | 符合；為英文 | `branch-end-probe-docstrings` |
| 測試名稱 | 部分符合；本輪三個名稱是單元／狀態／預期結果三段式，較早新增的部分名稱仍使用駝峰片段 | `test_planedits_duplicatestatus_blocks`, `test_rehearsal_trunkbehindmergebase_failsloudly`, `test_rehearsal_trunkaheadofbranch_failsloudly`, `test_squashedShape_probeNeedingBranchOnlyCommit_isReportedFailedWithReason` |
| Commit 訊息 | 符合；為英文 | `3b0ef50c`, `93a43823`, `ebc3ddc2` |
