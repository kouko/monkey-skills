# 畢業探針在壓過的 main 上仍然綠 — 我試了什麼、發生了什麼

於 2026-09-07，在專案 `7bd989fe` 的乾淨副本中試跑。

## 你要求的事，逐條驗證

### 1. 我在合併後的 main 上跑整包測試，沒有任何測試因為「找不到自己 change 的 commit」而紅；今天那支紅的測試變綠。

- **我怎麼試**：我不能真的合併到 main，因此用正式排練把同一批探針分別放進「保留完整分支歷史」與「整個分支壓成一顆 commit」的乾淨副本。
- **發生了什麼**：完整歷史形狀是 11 個通過、1 個因缺少遠端 main 基準而跳過；壓成一顆 commit 的形狀是 12 個全部通過。原本會因找不到自身 commit 而紅的案例，在兩種形狀都通過。
- **證據**：原始觀察紀錄中的「Acceptance 1」；完整歷史輸出為 `FAILED (0) / SKIPPED (1) / 11 passed`，壓平輸出為 `FAILED (0) / SKIPPED (0) / 12 passed`。
- **判定**：部分 — 已重現合併後的一顆 commit 形狀並通過，但沒有真的合併到 main。

### 2. 我拿一支故意依賴自己 plan commit 的探針丟進畢業前的排練，排練是紅的，並指名是哪一支、為什麼；把那個依賴拿掉之後排練轉綠。

- **我怎麼試**：我建立一個一次性的本機 Git 專案，讓探針搜尋自己那顆 plan commit 的標題，再排練完整歷史與壓平歷史；接著把它改成只檢查成品中的一般檔案，再跑一次。
- **發生了什麼**：第一次完整歷史通過，壓平形狀失敗；輸出指名該探針，並顯示原因是自己的 plan commit 標題已不存在。移除歷史依賴後，兩種形狀都各有 1 個通過、0 個失敗。
- **證據**：原始觀察紀錄中的「Acceptance 2」；第一次是 `CI 1 passed`、`SQUASHED SHAPE FAILED (1)`，第二次兩邊皆為 `FAILED (0) / 1 passed`。
- **判定**：可以 — 紅燈會指出探針與原因，移除依賴後確實轉綠。

### 3. 我對一個已經出貨、intent 已關閉的 change 問 checker「plan 定稿後有沒有被亂改」，它不會給我一個看起來像「有人亂改了」的 BLOCK，而是說這個問題對已合併的 change 不適用。

- **我怎麼試**：我查詢一個已關閉、且合併時失去 plan commit 的既有 change；再查詢本次仍在進行、plan commit 還在的 change；最後用一次性專案模擬「尚未關閉且缺少 plan commit」。
- **發生了什麼**：已出貨的案例明確回覆「不適用」且成功結束；本次 change 安靜通過；尚未關閉又缺少定稿 commit 的案例仍明確封鎖並回覆找不到 plan commit。
- **證據**：原始觀察紀錄中的「Acceptance 3」；三次結束碼依序為 `0`、`0`、`1`，最後一次輸出 `BLOCK ... no plan commit found`。
- **判定**：可以 — 已出貨與仍在進行的情況沒有被混為一談。

### 4. build 站的畢業段落寫明這條規則（畢業探針不得依賴自己 change 的 commit 存在），而且有測試釘住那句話，改掉就紅。

- **我怎麼試**：我先讀畢業說明，確認它明說會排練壓成一顆 commit 的形狀；測試原句一次，再只在乾淨副本暫時刪掉整句重跑，之後逐字復原。
- **發生了什麼**：原句存在時 1 個測試通過；刪句後同一測試失敗，訊息說讀者無法知道還有壓平形狀。復原後副本沒有留下修改。
- **證據**：原始觀察紀錄中的「Acceptance 4」；完整句子時 `1 passed`，刪句後 `1 failed`，復原後差異檢查通過。
- **判定**：可以 — 規則可讀，而且測試確實會在句子消失時變紅。

### 5. 整包測試通過，改到的 SKILL.md 仍在字數帽內。

- **我怎麼試**：我執行指定的兩段整包測試命令，再以指定算法計算改動說明的字數。
- **發生了什麼**：第一段得到 2088 個通過、1 個跳過、1 個預期失敗、1 個失敗，因此第二段沒有開始。唯一失敗發生在測試另開 Python 3.11 環境時：沙箱先拒絕寫入使用者快取；改用可寫的空快取後，又因無網路而無法取得 pytest。字數是 3278，低於 3750 上限。
- **證據**：原始觀察紀錄中的「Acceptance 5」；整包輸出為 `1 failed, 2088 passed, 1 skipped, 1 xfailed`，字數輸出為 `3278`。
- **判定**：部分 — 字數符合；這個受限環境無法證明整包測試通過。

## 對你既有的資料做了什麼

沒有碰你的既有資料。這次只改了它自己新增的紀錄與測試，以及既有的檢查器、排練工具和一段操作說明；沒有讀寫或轉換你擁有的內容。

## 我替你決定了

- **怎樣才算已出貨** — 我選擇只有「關閉狀態已被 commit 且從目前版本找得到，同時目前的 intent 檔存在並可正確讀取」才算已出貨；少任何一項都當作仍在進行並繼續封鎖。日後改成較寬鬆的判定，成本是重新承擔誤放行風險並補齊邊界測試。
- **怎樣製造壓平形狀** — 我選擇在既有乾淨副本內，把分支相對 main 的內容壓成一顆 commit，而不是再複製一次專案；改法會影響排練時間與遠端分支模擬，需重驗兩種形狀。
- **怎樣維持兩份同源探針** — 我選擇同步修改原始證據與常設副本，只允許它們的來源位置文字不同；日後拆開維護會失去兩份內容一致的保證。
- **規則放在哪裡** — 我選擇把壓平排練規則放進既有的畢業說明，不另外新增一道規則；若日後拆開，需新增另一套登記與釘測試。
- **怎樣保存畢業探針** — 我選擇讓常設副本除了來源位置那一行外逐位元相同，方便後來的人直接比對；改成各自演進後，需另建防止兩份內容漂移的方法。
- **先前的重要問題怎麼處理** — 四項先前發現都標記為已解決，沒有任何嚴重或重要問題被駁回或略過。

## 我不確定你要不要的事

沒有產品選擇上的未決問題。唯一尚未取得的證據，是在允許使用既有 `uv` 快取或能連線取得 pytest 的環境中重跑整包測試。

## 語言與模板檢查

| 內容種類 | 結果 | 證據 |
|---|---|---|
| 計畫 | 符合；工作內容為英文，使用者原話保留原語言 | `plan.md` |
| 規格 | 不適用；本次沒有規格 | `intent.md:4` |
| 審查發現 | 符合；六項都以 Conventional Comments 的 `issue` 標籤開頭 | `verdict-1-finding-1`–`verdict-5-finding-1` |
| 證據 | 符合；內部證據為英文 | `test_abuse_plan_edits_shipped.py`, `test_abuse_squashed_rehearsal.py`, `blind-run-observations.txt` |
| 測試說明 | 符合；為英文 | `test_plan_edits_shipped_change.py`, `test_rehearse_probes.py`, `test_build_station_text.py` |
| 測試名稱 | 符合；本次新增名稱遵循單元／狀態／預期結果三段式 | `test_plan_edits_closed_intent_missing_commit_reports_not_applicable`, `test_squashedShape_probeNeedingBranchOnlyCommit_failsInProcessWhileCiShapedStaysGreen`, `test_graduation_paragraph_names_the_squashed_rehearsal_shape` |
| Commit 訊息 | 符合；為英文 | `7d34f10f`, `002fdaf1`, `34614272`, `a627d620` |
