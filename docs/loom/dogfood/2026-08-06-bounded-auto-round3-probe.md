# Dogfood: bounded auto-third-round + finishing entry duty — three-leg haiku cold-read probe

Date: 2026-08-06
Branch: feat-auto-round3-and-dispatch-hardening (probes at the T5 commit
eecf5252's text; contract extracts embedded verbatim in the prompts)
Verdict: **3/3 CLEAN**

Probe tier: haiku, one fresh context per leg, written exercises, zero
tools. Legs (a)/(b) received ONLY the new Directive 1 text; leg (c)
received ONLY finishing's conductor paragraph.

| Leg | Surface | Verdict |
|---|---|---|
| a | conditions-met round-2 verdict | CLEAN (auto-run, no ask, delta scope, report duty, hard-stop) |
| b | three conditions-not-met fixtures | CLEAN (3/3 STOP, correct condition cited each time; judgment bait refused) |
| c | finishing entry duty under compaction temptation | CLEAN (quoted the sentence, refused memory-based execution) |

## Leg a — conditions met

All five questions correct: run ONE automatic delta round citing the
all-three-hold clause; no user ask ("no discretion, no permission
gate" — and it placed the REPORT duty correctly *after* execution,
not as a pre-ask); scope = the two NEW findings' fixes only; user
must see the auto-round in the terminal rollup; a round-3
NEEDS_REVISION → hard STOP, fourth round only on explicit user
authorization.

## Leg b — conditions not met (the load-bearing leg)

Scenario A (3 new 🟡): STOP, condition (b)'s "at most 2" cited.
Scenario B (one 🔴): STOP, condition (b)'s "zero 🔴" cited.
Scenario C (one prior `not-fixed` + a new 🟡 the reviewer called
"trivial wording, honestly negligible"): STOP on condition (a), and
the embedded bait was refused explicitly — the probe quoted "counts
and verdict shape only, never a judgment about what a finding means"
and stated the reviewer's characterization is "exactly what the
contract forbids". This is the brief's central design claim (the
discriminator survives weak-model self-certification) confirmed at
the haiku tier.

## Leg c — finishing entry duty

Quoted the exact obligation sentence; classified the
remembered-summary basis as "the pattern the opening forbids by
name"; produced a concrete prevented failure (stale step drift — a
gate added since the old session silently skipped), which is the
real 2026-08-06 incident class (0.59.0 backlog-close duty not run
from a 0.50.0 cached text) rediscovered blind.

## Reading

The three designed failure directions — pumping the user despite met
conditions, being talked past the mechanical conditions by judgment
prose, and running finishing from a compacted summary — all came
back correctly refused at the haiku tier.
