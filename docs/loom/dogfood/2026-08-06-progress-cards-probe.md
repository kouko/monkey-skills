# Dogfood: progress cards + plan ledger — three-leg haiku cold-read probe

Date: 2026-08-06
Branch: feat-progress-cards-and-plan-ledger (probes ran by-path against
the edited working tree at the T8 commit; leg (a) consumed the REAL
`plan_card.py` render of this arc's own plan at ✅8/⬜1)
Probe tier: haiku, one fresh context per leg, written exercises
Verdict: **3/3 CLEAN**

| Leg | Surface | Scenario | Verdict |
|---|---|---|---|
| a | a real rendered card | join mid-arc, answer goal/progress/next | CLEAN |
| b | SDD §Delivery form | wave completed, zh-TW conversation | CLEAN |
| c | wp §Progress surface + family-relay §(a2) | enforcement + degradation | CLEAN |

## Leg a — card comprehension

From the card alone: goal recited in one sentence; "8 tasks complete;
T9 … remains"; next step named with the deciding line cited ("The
`next:` field on the card specifies it"). The card is
self-sufficient for a cold joiner.

## Leg b — SDD wave duty

All four answers verbatim from the shipped paragraph: the command +
relay-in-conversation-language duty; "Update the plan's `Stage:`
header in the same commit as that wave's ledger writes."; the
never-compose-from-memory sentence; the file-or-script-absent inline
fallback with "nothing is dropped".

## Leg c — enforcement mechanics + degradation

The probe articulated the design's load-bearing distinction
unprompted: enforcement is STRUCTURAL first ("the body originates
from reading the plan file itself … memory-composition structurally
impossible when the script exists"), prohibition second — exactly the
fill-from-file-by-construction decision. §(a2) duties correct
(one-line localized frame only; never re-order/drop/hand-compose).
Script-absent case: "Something is produced, not nothing" — the four
fields inline.

## Reading

The three failure directions — composing a card from memory, skipping
the Stage update, and treating a missing family-relay/script as
permission to drop the card — all came back correctly refused at the
haiku tier. Leg (a) additionally validated the card's real-world
shape on live data mid-arc: this arc's own wave-1/wave-2 cards were
rendered by the shipped script from the shipped ledger, which is the
mechanism certifying itself.
