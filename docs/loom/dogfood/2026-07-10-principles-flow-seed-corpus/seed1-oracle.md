# Oracle 1 — B2B incident-response dashboard (grader-only)

named_anchors: HEART; MoSCoW; Nielsen's 10 Usability Heuristics; IBM Carbon; Swiss / International Typographic Style; Layered/N-Tier; Hexagonal/Ports & Adapters; TypeScript; React; Go; PostgreSQL; Redis

deferred_items: none in this seed (2-deferred trap lives in seed 2)

out_of_jurisdiction_bait: 年約企業授權/annual contract + 10 個 logo 業務目標 (must be noted out-of-jurisdiction, never a Product Principle); MVP 8 週上線時程 (timeline, must not become a principle)

stances: task=事故當下戰情室視圖only; user=on-call值班工程師; quality order=即時性>密度>客製化; success=MTTA-30%; why-new=無預組版面; language=僅英文; lifecycle=archive不刪除; design=資訊密度優先+深色模式; GRANULARITY TRAP — one seed bullet packs 5 eng stances (打磨次要, 可逆性=快速rollback, 隱私=無實際數據只留metadata, 升級胃口=事前參與, 測試嚴謹度=property-based test) — each MUST still surface as its own carrying principle, none may be silently merged away or dropped

negative: postmortem 撰寫; SLA 報表; 排班
# note: the three tokens above are scope-out items (Q1/Scope-out) — presence
# anywhere means they leaked back in as a committed Product Principle rather
# than staying a scope-out note. Separately (not machine-checked — no single
# literal string captures an absence-of-marker check): any unchecked
# platitude with no `— check:` is a NEGATIVE finding for LLM/human graders.
