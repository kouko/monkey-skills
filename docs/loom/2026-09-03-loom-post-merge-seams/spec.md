# loom 1.0 merge 後的接縫 — spec
intent: 2026-09-03-loom-post-merge-seams@90aedebf

## Requirements
REQ-1 — Intent closed on the branch before the merge, under the unchanged push gate
  The ship station's order becomes: push → `gh pr create` (the PR number is now known) → commit `docs(loom): close intent <change-id>` changing that intent's `status:` line from `confirmed <date>` to `closed <YYYY-MM-DD> — PR #<N>` → a review-only commit (only `review.json`; `reviewed_sha` = the close commit; package-tests and adversarial probes re-pinned at it) → push again. `loom_checker.py push` passes on its existing rules, no rule gains a shape and nothing is bypassed; `closed` reaches the trunk only by the merge. `loom_checker.py intake write-plan <change-id>` on a closed intent blocks with `intake.confirmed: … this change is closed (PR #<N>); a new change starts from a new intent`. The already-merged intent `2026-09-02-simple-loom-flow` is closed the same way inside this change's own diff → Acceptance #1
REQ-2 — Contract grammar knows `closed`, and closed is terminal
  `loom-code/contract/manifest.yaml` (`status` grammar) and `loom-code/contract/templates/intent.md` (status comment) both list `closed <YYYY-MM-DD> — PR #<N>`; `--list-rules` describes it under `intake.confirmed`; `test_the_repos_own_change_matches_its_own_review_json` derives `{intake.confirmed}` as the expected block when the repo's own intent is closed; the declared package command `python3 -m pytest loom-code/scripts/ scripts/ .claude/hooks/ -q` passes locally and in the CI job `pytest + knowledge-drift + codex-manifest-drift` (.github/workflows/loom-code-ci.yml) → Acceptance #2
REQ-3 — Scaffold copy is host plumbing for the trailer rule too, by content
  `push.dispatch-covers-tasks` ignores a path in the host-plumbing set (`HOST_PLUMBING_FILES`, `HOST_PLUMBING_DIR_PREFIX`) only when that path's blob at the commit equals the canonical file the running checker ships (see Design decision); a commit that only refreshes `.codex/hooks/` with genuine scaffold output carries no trailer duty, while any other content under those paths counts as gate work and still needs a trailer → Acceptance #3
REQ-4 — Checkpoint cost measured on this change
  `docs/loom/2026-09-03-loom-post-merge-seams/evidence/checkpoint-cost.md` holds one table — per checkpoint: commits, dispatches, review rounds — plus the branch's `git rev-list --count <trunk>..HEAD`, side by side with the #771 replay (34 commits / 31 today), and one line of recommendation; no coefficient is changed → Acceptance #4
REQ-5 — Plugins versioned and observable
  loom-code 1.0.1, loom-design 1.0.1, loom-workflow 4.0.1 in each plugin's `.claude-plugin/plugin.json` with a CHANGELOG entry naming this change; after `claude plugin update <plugin>@monkey-skills` the directory `~/.claude/plugins/cache/monkey-skills/loom-code/1.0.1/` exists and `python3 ~/.claude/plugins/cache/monkey-skills/loom-code/1.0.1/scripts/loom_checker.py --list-rules` prints the `closed` grammar → Acceptance #5
REQ-6 — Five carried test nits closed
  R24-O2 (`changed_paths` docstring "below"→"above", loom_checker.py:455), R28-O2 (`check=True` on the skip-guard probe, test_check_mechanisms.py:664), R30-O1 (literal `5` asserted, :670), R30-O2 (the moot locale test replaced by a guard that `check_mechanisms.py` spawns no `wc`, :672), R30-O3 (`_run` captures bytes and decodes UTF-8 with errors="replace", test_session_start_words.py:49) are fixed with the package command green → Constraints ("五個測試 nit 順手一起清"); no Acceptance line names them

## Design decision
- **Close before the merge (user-decided 2026-09-03; replaces round 1's new push shape).** The intent is closed on the change branch once the PR number exists; the review-only commit that follows re-pins `reviewed_sha` and the probes at the close commit, exactly as any post-review docs edit is handled today. Consequences: no push rule changes (the intent Constraint stands as written); the closed line lands on the trunk with the squash and nowhere else, so a `closed` on the trunk is itself the proof of the merge — no PR-number verification is needed and none is done; a PR that is never merged leaves `closed` on a dead branch only. The ship station text (`loom-code/skills/ship/SKILL.md` §6) is rewritten to this order, and its "the status line cannot be written on the branch" paragraph goes. Rejected: the round-1 design (a second accepted HEAD shape for `push.review-only-head`) — one more rule semantics, a grammar-only proof, and every red-team escape listed in evidence/spec-redteam-r1.md.
- **`intake.confirmed` on a closed intent (agent-decided).** Blocks with the message above; `closed` is not a pass state anywhere. The status grammar is one regex in `loom_checker.py` shared by `intake.confirmed`, `intent.schema` and the new shape, so the three cannot drift.
- **Content-bound plumbing exemption (agent-decided, replaces the path-prefix reading of round 1).** `check_dispatch_covers_tasks` filters `commit_paths()` through `_is_host_plumbing()` (loom_checker.py:400) **and** a blob comparison: `.codex/hooks/loom_checker.py` and `.codex/hooks/git_exec.py` must equal the running checker's own source files (`Path(__file__).parent`), the copy's stamp line (`# loom-checker <version>`) stripped; `.codex/hooks/loom-checker` must equal the shim the scaffold renders for the running version; `.codex/hooks/contract/<rel>` must equal the `contract/<rel>` shipped next to the running checker; `.codex/hooks/.loom-hook-fired` is ignored (it is git-ignored anyway). A path that fails the comparison is classified by the manifest as today (`gate`) and owes a trailer. On Codex the running checker is the copy, so the comparison of the checker file is trivially true — that is the artifact-content gap concept-model §7 already accepts, recorded here rather than hidden.
- **Test oracle for the self-check (agent-decided).** `test_the_repos_own_change_matches_its_own_review_json` (loom-code/scripts/test_loom_checker_intake.py:385-447) derives its expected block set dynamically; it gains the branch: intent status `closed …` → expected `{intake.confirmed}`. The CI failure it currently produces is kept as `evidence/ci-781-intake-confirmed.md`.
- **Contract version (agent-decided).** Stays 1.0: an added accepted value is backward compatible for every consumer requiring `>=1.0`. Plugin bumps as REQ-5.
- **Checkpoint-cost table (agent-decided).** Written by the blind-runner at branch end from `git log` and `review.json`, so the numbers are recomputed by someone who did not produce them. Recommendation only — the decision is deferred to the second and third real change (user-decided 2026-09-03).
- **Merge (user-decided 2026-09-03).** The user presses merge; the agent stops after `gh pr create`.
- **Test nits (agent-decided).** Fixed in the task that already touches each file (`test_check_mechanisms.py`: R28-O2, R30-O1, R30-O2; `test_session_start_words.py`: R30-O3; `loom_checker.py`: R24-O2); R30-O2's guard asserts, via `inspect.getsource(check_mechanisms)`, that no `subprocess` call in the module names `wc`.

## Alternatives considered
- A `push` flag or waiver for docs-only commits — rejected: a declared bypass; the design has no waivers.
- A second accepted HEAD shape for `push.review-only-head` with the merge proven from the parent tree (round-1 design) — rejected: one more rule semantics and a grammar-only PR pointer; superseded by closing on the branch.
- Writing `closed` inside the review-only commit itself — rejected: that commit may touch only review.json; the close is its own commit immediately before it.
- Exempting plumbing paths by prefix for the trailer rule (round-1 design) — rejected by the red team: it lets gate content hide under `.codex/hooks/contract/`.
- Bumping only loom-code — rejected: Acceptance #5 (user-confirmed) and both sibling plugins describe the status values in prose.

## Current state evidence
- Forward：`loom-code/skills/ship/SKILL.md:326-336` prescribes the close commit (`docs(loom): close intent <change-id>`, `status: closed <YYYY-MM-DD> — PR #<N>`) **after** the merge, on the trunk; `loom-code/scripts/loom_checker.py:1557` (`check_review_only_head`) rejects any HEAD that touches a file other than `review.json` — which is why that post-merge commit could not be pushed (evidence/ci-781-intake-confirmed.md).
- Reverse：`loom-code/scripts/loom_checker.py:791` (`CONFIRMED` regex) and `:825-835` (`intake.confirmed`) accept only `confirmed <date>`; `loom-code/scripts/test_loom_checker_intake.py:385-447` (`test_the_repos_own_change_matches_its_own_review_json`) derives the expected block set from the repo's own intent and spec state (up to `intake.spec-pass` / `intake.confirmed-behavior`) and has no branch for `closed`.
- Error：`evidence/ci-781-intake-confirmed.md` — CI run 33705803149 red on `intake.confirmed` ("status is closed 2026-09-03 — PR #780"); the same PR's push blocked by `push.review-only-head` in this session.
- Data：`loom-code/contract/manifest.yaml:85` grammar `open | confirmed <date> | withdrawn — <reason>`; `loom-code/contract/templates/intent.md:7` comment mirrors it; `loom_checker.py:389-400` `HOST_PLUMBING_FILES` / `HOST_PLUMBING_DIR_PREFIX` / `_is_host_plumbing`; `:2085` `commit_paths` (no plumbing filter); `:2095` `check_dispatch_covers_tasks`; `loom-code/scripts/codex_scaffold.py` (`SHIM_TEMPLATE`, `_checker_copy_content`, `CONTRACT_COPY`) is what the scaffold writes.
- Boundary：no push-rule semantics change; the trailer rule's exemption source changes, its semantics do not; the ship station's step order changes (docs); `.codex/hooks/loom-checker` command string untouched; the checkpoint coefficient is measured, not changed; five nits at `test_check_mechanisms.py:664/670/672`, `test_session_start_words.py:49`, `loom_checker.py:455`.

## UI flows
N/A — no surface a user reads or types into changes; the touched template is a file agents fill in, and the `closed` value is written by the ship station, never typed by the user.
