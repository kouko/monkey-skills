# user-declared lanes: full / express / gate-only, declared at ① or switched mid-flight, a three-option switch prompt, a repo default, gate deltas always full — plan
intent: 2026-09-05-user-declared-express-lane@b9413f18

## Current State Evidence
- Forward: `loom-code/scripts/loom_checker.py:2946-2990` — `change_lane_detail()`
  recomputes `small | full` from the classified delta (tests-only, docs-only,
  CI/config, no interface surface, no gate/skill type, one plugin dir) and
  `:2984` `change_lane()` wraps it; `:3828-3839` `check_verdicts()` sets the
  reader floor `1 if lane == "small" else 2`; `:3574` uses the lane once more
  (probe floor). Nothing reads a declaration: the lane is never the user's.
- Reverse: `loom-code/skills/review/SKILL.md:118-137` — "**Lane.** The checker
  recomputes `small | full` … never asked for, never chosen by an agent";
  §2 (`:139-`) dispatches by lane; no station text names a way for the user to
  ask for less verification, and the only faster path is not invoking loom
  (`~/DataspellProjects/iCHEF-dbt-pipeline`: 62 of 63 PRs since 2026-07-01
  skipped intent, review and blind run, none saying so).
- Error: `loom-code/scripts/loom_checker.py:700-717` — `check_intent_schema()`
  validates frontmatter fields from `manifest.yaml`'s intent field list
  (`values` per field); `:946-1010` `check_needs_design_reason()` requires the
  commit that last changed `needs-design:`/`status:` to carry that line
  verbatim in its message — the mechanism a `lane:` switch line reuses.
  `contract/templates/intent.md:1-8` has no `lane:` field;
  `contract/templates/KICKOFF-DEFAULTS.md:11-16` has no `default-lane:` key;
  `kickoff_defaults()` (`:485-500`) parses `- <key>: <value> — <reason>` and
  the key list lives in `manifest.yaml kickoff_defaults`.
- Data: `loom-code/scripts/test_loom_checker_push.py` and
  `test_probes_change_lane.py` build sandbox repos with recorded review.json
  shapes for the lane recompute — the pattern the new cases follow;
  `docs/loom/2026-09-05-review-sees-complexity-and-process-cost/review.json`
  is a real full-lane record with rounds continuing across checkpoints
  (the switch-timing fixture: a switch commit after round 2).
- Boundary: `--list-rules` prints 27; `push.verdicts-ge-2`'s description
  changes wording (lane floor 2/1/0), nothing added or removed. Word caps:
  review/SKILL.md 3,750 of 4,500; build 3,165 of 3,750; ship 3,475 of 3,500
  (one sentence fits, a paragraph does not — the switch prompt lives in a
  reference file). `.codex/hooks/loom_checker.py` is a byte mirror
  regenerated by `codex_scaffold.py --repo .`; `manifest.yaml` and the
  templates are mirrored under `.codex/hooks/contract/` the same way.

## Task DAG

Lane: full — `loom_checker.py` is gate-typed (this change declares no lane
and could not: its delta touches the checker). W0-01 is adversary-first for
the two checker tasks; W1-01 → W1-02 run serially in the working tree (one
3,900-line file); W1-03 and W1-04 run in parallel worktrees (templates and
station text touch disjoint files). Second vendor: Codex (①). Dispatch
records: one commit for wave 0, one for wave 1, one for wave 2, one per
verdict round.

**W0-01 Adversarial probes for the lane declaration**  after: —
- Files: `docs/loom/2026-09-05-user-declared-express-lane/evidence/probes/test_abuse_lane_declaration.py`
- Test: the probe file — sandbox repos the way `test_loom_checker_push.py`
  builds them, RED today on: (a) an intent with `lane: express`, a
  docs/skill delta, one reader at branch-end → `push.verdicts-ge-2` must
  pass (blocked today); (b) same with one line added to
  `loom-code/scripts/loom_checker.py` → still blocked (GREEN today, stays
  GREEN); (c) `lane: gate-only`, pure docs delta, zero readers, ≥3 probes +
  package tests recorded → must pass; (d) same with a SKILL.md or
  `agents/*.md` in the delta → blocked; (e) a switch commit after round 2:
  round 2 with two readers, round 3 with one → pass; (f) the switch commit's
  message missing the `lane:` line → `intent` subcommand blocks; (g) a
  `lane:` line written by an agent-looking author with no `by <name>` →
  schema block; (h) GREEN pin: `--list-rules` = 27. Three-part names,
  English docstrings, commits located by subject.
- Risk: the probes fix the grammar (`lane: <name> — switched <date> by
  <name>, from <wave|round>`) before the checker exists — W1-01 implements
  what the probes assert; a grammar change goes through the probe file
  first (agent-decided — the adversary-first contract).

**W1-01 Checker: declared lane parsing, `default-lane`, the switch-line rule**  after: W0-01
- Files: `loom-code/scripts/loom_checker.py`, `loom-code/contract/manifest.yaml`
  (intent field `lane` optional with values `express|gate-only`; kickoff key
  `default-lane`), `loom-code/scripts/test_loom_checker_push.py` (or a new
  `test_loom_checker_lane.py`), `.codex/hooks/loom_checker.py` (regenerated)
- Test: `declared_lane(repo, change_id)` returns `("full"|"express"|"gate-only",
  origin, from_round)` from the intent's `lane:` line(s) (last wins; a
  `switched … from <wave|round>` suffix sets `from_round`), falling back to
  KICKOFF's `default-lane`, else `full`; `intent` subcommand: a commit that
  changes the `lane:` line must carry it verbatim (reuse
  `check_needs_design_reason`'s mechanism); probes (f), (g), (h) GREEN.
- Risk: the `lane:` line is user-owned — the checker cannot tell a user
  from an agent, so the schema only requires `by <name>`; the station text
  (W1-04) and the user-judgment-leak lens carry the prohibition
  (agent-decided — no checker rule added).

**W1-02 Checker: effective lane, floors, switch timing, forbidden types**  after: W1-01
- Files: `loom-code/scripts/loom_checker.py`, its tests, `.codex/hooks/loom_checker.py`
- Test: `effective_lane_detail(repo, reviewed_id, change_id, round)` —
  recompute first: a recomputed `small` stays small; a gate-typed path in
  the delta forces `full` whatever is declared; `express` needs no gate
  path; `gate-only` needs no gate, skill or agent-contract path; a declared
  lane applies only to rounds after `from_round`; `check_verdicts` floor
  full 2 / small 1 / express 1 / gate-only 0; `:3574`'s probe floor unchanged
  (≥3 in every lane); `--list-rules` wording for `push.verdicts-ge-2`; probes
  (a)–(e) GREEN.
- Risk: gate-only with zero readers still needs a passing "round" for
  `push.reviewed-sha`/`push.review-only-head` — the round carries the
  adversary's probes and package tests with no verdicts; the checker treats
  an empty `verdicts[]` for that scope as satisfying a floor of 0
  (agent-decided; the probe (c) pins it).

**W1-03 Templates and defaults: `lane:` and `default-lane:`**  after: —
- Files: `loom-code/contract/templates/intent.md`,
  `loom-code/contract/templates/KICKOFF-DEFAULTS.md`,
  `docs/loom/KICKOFF-DEFAULTS.md` (`- default-lane: full — …`),
  `loom-code/scripts/test_contract_manifest.py` or a new template test,
  `.codex/hooks/contract/templates/*` (regenerated)
- Test: the intent template carries a commented `lane:` line naming both
  values and the switch grammar; the KICKOFF template carries
  `default-lane:` with the three values; this repo's KICKOFF has
  `default-lane: full`; `test_kickoff_defaults_grammar.py` still passes.
- Risk: manifest.yaml is W1-01's file for the field/key declarations — this
  task edits templates and KICKOFF only, and its tests read the manifest as
  W1-01 leaves it (agent-decided — one owner per file).

**W1-04 Station text: lane behaviour, the switch prompt, PR line, lens sentence**  after: —
- Files: `loom-code/skills/review/SKILL.md` (§1 lane paragraph: three
  declared lanes and what each drops; §2 floors; §3 blind run condition; §4
  adversary once), `loom-code/skills/review/references/lane-switch.md` (new:
  the three-option prompt in consequence form — what you lose, what stays,
  the estimate from `cost`; the disallowed option listed with its reason;
  the spoken mapping 快速模式→express, 只過閘／不用審→gate-only; "no
  estimate" when cost has no data), `loom-code/skills/build/SKILL.md` (one
  sentence: no wave-end checkpoint under express/gate-only; the switch
  prompt reference), `loom-code/skills/ship/SKILL.md` (PR body line
  `lane: <name>（第 N 輪起）`; gate-only's ③ page), `loom-code/skills/review/references/lenses.md`
  (user-judgment-leak: an agent-written `lane:` is a finding),
  `loom-code/scripts/test_review_station_text.py`, `test_build_station_text.py`,
  `test_ship_station_text.py`, `test_lane_switch_reference.py` (new)
- Test: affirmative pins (shared `prose_pin.NEGATION_RE`, ≤40 words) for:
  review §1's three-lane sentence, §2's floor sentence, the blind-run and
  adversary-once sentences, build's no-checkpoint sentence, ship's PR line,
  the lens sentence; `lane-switch.md` carries the three-option block, the
  "listed with its reason" sentence and the spoken mapping table. RED today.
- Risk: ship/SKILL.md has 25 words of headroom — one sentence, and the ③
  page description moves to `references/blind-run-report.md` if it does not
  fit (agent-decided — extract before bump; a bump needs its reason and a
  deletion candidate under the 1.5.0 rule).

**W2-01 Version, changelog, README row, codex scaffold**  after: W1-01, W1-02, W1-03, W1-04
- Files: `loom-code/CHANGELOG.md` ([1.6.0]), `loom-code/.claude-plugin/plugin.json`,
  `loom-code/.codex-plugin/plugin.json`, `README.md`, `.codex/hooks/*` (regenerated)
- Test: `python3 scripts/sync_codex_manifests.py --check --all` exits 0;
  `--list-rules` = 27; `check_mechanisms.py --baseline origin/main` all
  clear (a budget-exception line if a contract field counts as a mechanism).
- Risk: minor bump 1.5.0 → 1.6.0 (agent-decided — a new intent field, a new
  KICKOFF key, a lane floor of 0; no rule added).

**W2-memory Memory step — graduated probes and store entries**  after: W2-01
- Files: byte copies of this change's `evidence/probes/test_*.py` under
  `loom-code/scripts/` (path lines adjusted only; names checked against
  every test there); one `docs/loom/memory/` entry on declared lanes as a
  recorded choice versus "not invoking loom", plus its README index line.
- Test: `python3 scripts/check_loom_memory_integrity.py` exits 0; the
  graduated copies pass alone; no name collides.
- Risk: the closing round's own probes stay under evidence (known).

Checkpoints: wave-end:1 after W1 (more than 8 files), the single closing
round after W2-memory — two checkpoints.

## Questions asked
1 — what — 「你要的是：車道變成你能宣告的事，三格——full／express／gate-only……做完後你可以：在 ① 宣告，或做到一半說「切換模式」……對嗎？」
1 — consequence — 「不可逆的門：沒有。」（stated）
1 — consequence — 「這個 change 自己改 checker，走 full。」（stated）
1 — what — 「這次要不要用 Codex 當第二位讀者？」（answer: codex）

## Risks
1. Zero-reader rounds are a new record shape; every push rule that reads
   "the latest round" must accept a round with probes and no verdicts for a
   gate-only change — W1-02's probes (c) walk the whole `push` command, not
   `check_verdicts` alone.
2. ship/SKILL.md's cap (25 words of headroom) — extraction to
   `references/` before any bump.
3. This change is the first to write the switch prompt; its cold-read
   (Acceptance 5) is run by the blind-runner at each checkpoint with a
   synthetic "delta contains SKILL.md, user says 快速模式" scenario.
