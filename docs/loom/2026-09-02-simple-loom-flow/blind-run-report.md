# loom 重設計 — 我怎麼試的，結果怎樣

試的日期：2026-09-03。試的東西：這支 branch 的乾淨狀態（`40442b1e`，工作區沒有未存檔的改動；含 2026-09-03 的 W4 fix round）。最後試過的完整版本樹是 `1639e337`；在它之後，這個 repo 只有給人看的紀錄檔（review.json、這份報告、evidence／plan 等文件）還會變，程式、hook、skill 檔案不會再變（見文末「再補跑：最終樹 1639e337」段）。
寫這份報告的人（我）沒有參與任何實作，只照你當初寫下的七條驗收條件，自己一條一條去試。

## 一句話：可以驗收嗎

**七條裡五條做到、一條做到但有一個沒能真的試、一條沒做到。**
沒做到的是第 4 條「不比今天重」裡的一小格：拿最小的那個舊改動（#771）重走一遍，
commit 數是 34，比當初的 31 多 3 個——原因很明確，每次中途檢查固定會多出三個 commit
（先記派工、再放檢查產出、最後一個只動審查紀錄），另外兩個較大的舊改動則明顯變輕（67→37、28→18）。
沒能真的試的是第 1 條的最後一哩：**我沒有在一個真的裝好這三個外掛的對話裡，從頭到尾走一次**——
本次全部是在這個 repo 裡直接跑腳本與讀站點文件來驗證的。
其餘：兩種工具（Claude Code 與 Codex）走出來一樣、決策點數達標、瘦身目標全達標（工具 36→17、
開場字數 5281→658、文件五種）、沒看過這套東西的人 15 分鐘內零猜測看懂、
沒有配套測試的新機制會讓 CI 變紅——這幾條我都親手跑過，證據附在下面。

| 你當初寫的 | 判定 | 一句話 |
|---|---|---|
| 1 基本知識的人只答三種問題就拿到 PR | 大致做到 | 問題型別、雙審、盲跑報告都在，但少一次真環境端到端 |
| 2 兩種工具走出來一樣 | 做到 | 第二輪實走通過；只有 Codex 的授權畫面沒真按過 |
| 3 停下來問人的次數（工程 ≤2、產品 ≤3） | 做到 | 本次 3 次；#771 重走 2 次 |
| 4 重走三個舊改動不比今天重 | 部分不合格 | #771 的 commit 34 > 31；其餘各格都過 |
| 5 工具數、開場字數、文件形狀 | 做到 | 17 ≤ 18、658 ≤ 2640、5 種 |
| 6 沒看過的人 15 分鐘看懂、零猜測 | 做到 | 兩題各開 2 個檔、猜測 0 |
| 7 沒配套測試的新機制 CI 會紅 | 做到 | 我親手弄壞兩種，都變紅（結束碼 1） |

---

## 你要的第 1 條：只答三種問題，就拿到測試過、兩人審過、附驗收報告的 PR

- **我怎麼試**：這條沒辦法用一道指令證明，我拆成四件事查。
  ① 問了使用者幾個問題、屬於哪一型 → 讀 `docs/loom/2026-09-02-simple-loom-flow/review.json` 的 `questions[]`；
  ② 審查是不是機器做的、有沒有兩個以上互不相干的審查者 → 數同一份檔的審查結論；
  ③ 有沒有人專門照驗收條件盲跑 → 找出標記 `skill-blind-run` 的那幾筆；
  ④ 這些規則是不是真的有機器在管 → 跑
  `python3 loom-code/scripts/loom_checker.py --list-rules | wc -l` 與
  `python3 loom-code/scripts/check_mechanisms.py --measure`。
- **結果**：
  - 使用者被問的問題總共 **7 個**，全部落在你允許的型別裡：第一次停下來問 5 個
    （「你要的是不是這個」、「要不要找第二家模型當審查者」、「產品原則覆述對嗎」、
    「兩個獨立工具算不算數量」各屬「要什麼」；「把契約要求降到只剩一張圖」屬「後果形」），
    第二次停下來問 2 個（「你打什麼會看到什麼」屬「可見行為」；「重大選型要不要問你」屬「後果形」）。
    沒有一題是在問你 spec 好不好、計畫怎麼拆、審查該不該過。
  - 審查結論 **43 筆**：22 筆「要改」、18 筆「過但有備註」、3 筆「過」。
    每一輪都至少兩個各自獨立開新視窗的審查者，其中 spec 那幾輪還跨了兩家模型（Anthropic 與 OpenAI）。
    對抗與測試紀錄共 21 筆（17 筆對抗、3 筆整包測試、1 筆冷讀；原本 25 筆，round 24 審查者指出其中 4 筆只是文字描述、沒有可以重跑的指令，已從紀錄拿掉，它們的審查內容仍在 evidence 資料夾裡）。最後收尾的 review-only commit（只更新審查紀錄、不改程式的收尾 commit）會再把整包測試與 17 個對抗探針在最終樹上重跑一次並記進去，所以那之後這個數會變成 40。派工人次以 `review.json` 的 `dispatch[]` 為準
    （我補跑時是 79：實作 40、審查 28、盲跑 7、對抗 4；之後每多派一次審查這個數就會加一，最終數看 review-only commit 那份記錄；補跑章節在文末）。
  - 盲跑（沒寫過它的人照驗收條件試）有紀錄的 3 輪：W1 第一輪判「要改」，W1、W2 的收尾各判「過但有備註」。
  - 機器在管的規則 **27 條**；機制清點指令輸出：工具 17、文件型別 4（加上 PR 本身＝5 種）、
    開場字數 658（基線 5281，門檻 2640），結束碼 0。
- **證據**：`docs/loom/2026-09-02-simple-loom-flow/review.json`；
  `evidence/w1-checkpoint-reviews.md`、`evidence/w2-checkpoint-reviews.md`；上述兩道指令的輸出。
- **我沒能試的部分（重要）**：我沒有在一個真的把這三個外掛裝起來的對話裡，
  從「我想要一個功能」講到 PR。所有驗證都是在這個 repo 裡直接跑腳本、讀站點文件完成的。
  第一次真正的端到端，建議就拿合併後的第一個改動來走。
- **判定**：大致做到；最後一哩沒試。

## 第 2 條：同一件事在 Claude Code 和 Codex 上一樣，Codex 只多按一次授權

- **我怎麼試**：讀第二輪實走的逐字紀錄（第一輪是失敗的，已修）。
- **結果**：同一個任務（六支腳本抽共用程式）在 Codex 上重走，產生的檔、停下來問人的地方、
  擋人的規則都與 Claude Code 一致；Codex 只多兩件事：一個安裝檢查器的 commit，
  加上一次「請輸入 /hooks 授權」的停頓。沒授權時它會停下來並且不留下任何檔案（不是偷偷跳過）。
  第一輪抓到的三個機制破口（自我探測沒真的經過 Codex、安裝出來的副本被誤判、Codex 沙箱寫不進 `.codex/`）都已修好並看到修好後的行為。
- **證據**：`evidence/req2-codex-walk-r2.md` 與同目錄六個 `req2-codex-walk-r2-run*.log`。
- **沒試到的**：真正的 `/hooks` 授權畫面（那是要用手在 Codex 介面上按的），
  這次用「直接放行」代替；另有一個小瑕疵：日期剛好等於範例日期，看不出它是照抄還是算出來的。
- **判定**：做到（授權畫面待你第一次實際使用時順手確認）。

## 第 3 條：工程改動最多停 2 次問你，產品改動最多 3 次

- **我怎麼試**：數這個改動自己停了幾次，再看重走 #771 停了幾次。
- **結果**：這個改動是產品型，停了 **3 次**——① 確認你要什麼、② 確認看得見的行為、
  ③ 就是你正在讀的這份報告。#771 重走是工程型，停了 **2 次**（確認要什麼、驗收）。
- **證據**：`review.json` 的 `questions[]`（`decision_point` 只有 1 和 2，3 是本報告）；
  `evidence/replay-771.md`「計數（實測）」。
- **判定**：做到。

## 第 4 條：重走三個真實舊改動，不比今天重

- **我怎麼試**：讀三份重走紀錄與那張總表，照原樣抄下來，不替它修飾。
- **結果**（每格「今天 → 重走」）：

  | 舊改動 | commit | 審查派工 | 停下來問人 | 過了嗎 |
  |---|---|---|---|---|
  | #771 抽共用程式（真的重跑一遍） | 31 → **34** | 22 → **12** | 2 → **2** | commit **不合格**，其餘過 |
  | #772 對抗審計站（推算） | 67 → **37** | 58 → **16** | 2 → **2** | 過 |
  | #775 文字自檢（推算） | 28 → **18** | 14 → **8** | 2 → **2** | 過 |

  **#771 的 commit 這一格是真的沒過，我不替它換算法。** 原因單一而且已經找出來：
  每次中途檢查固定要三個 commit——先記「我要派誰去審」、再放「審出來的東西」、
  最後一個只動審查紀錄（這是放行條件要求的）。三次檢查就是九個 commit。
  扣掉兩項純屬這次重跑的執行失誤後是 31，跟今天打平，仍然不是「更輕」。
  也就是說：**很小的純工程改動，新做法最好只是打平。**
  另外「審查派工」那欄的三個舊數字（22／58／14）本來就只算審查、不含實作，
  照同一個定義比，重走是 12／16／8，三個都比較輕；只有把兩邊用不同定義混著比才會不合格。
- **證據**：`evidence/replay-771.md`、`evidence/replay-772.md`、`evidence/replay-775.md`、
  `evidence/ceremony-cost-old-vs-new.md`「v10 實測」節。
- **判定**：部分不合格（一格），其餘八格全過。

## 第 5 條：工具數 ≤18、開場字數減半、每個改動的文件 ≤5 種

- **我怎麼試**：自己數目錄、自己在一個全新的空專案裡跑開場腳本。
  - `ls loom-code/skills | wc -l` → 5；`loom-design` → 4；`loom-workflow` → 10（含兩個不計數的獨立工具）。
    改動前（`923fb84a`）分別是 14／10／12＝**36**。
  - 在一個新建的空 git 專案裡：`bash loom-code/hooks/session-start </dev/null | wc -w` → **658**。
    同樣方式跑舊版（從 `923fb84a` 取出整個 hooks 目錄再跑）→ **5281**。
- **結果**：計數工具 **17**（19 個目錄扣掉你裁定不計數的兩個獨立工具）≤ 18；
  開場字數 658，是舊版的 **12.5%**，遠低於「減半」的 2640；
  每個改動的文件形狀 **5 種**（想要什麼、規格、計畫、審查紀錄、PR 本身）。
- **證據**：上述指令輸出；`evidence/measurements.md`。
- **判定**：做到。

## 第 6 條：沒看過這套東西的人 15 分鐘內看懂，而且不用猜

- **我怎麼試**：讀兩份冷讀紀錄，直接看它們自己記的「開了幾個檔、猜了幾次」。
- **結果**：兩題都是 **猜測 0、只開 2 個檔**。
  A 題（六支腳本抽共用程式，只裝一個外掛、用 Codex）走了三輪才收斂（7 → 4 → 0 個猜測）；
  B 題（待辦工具加到期日，兩個外掛、用 Claude Code）兩輪收斂（3 → 0），讀檔約 1–2 分鐘。
  兩位都能講出：會產生哪些檔、誰決定什麼、哪個檢查在什麼時候擋、審查什麼時候跑。
- **證據**：`evidence/cold-read-A.md`（另有 `cold-read-A-interim.md`）、`evidence/cold-read-B.md`。
- **判定**：做到。

## 第 7 條：加了新機制卻沒有配套測試，CI 要變紅

- **我怎麼試**：先確認現在是綠的，再自己弄壞兩種，看它會不會紅。
  1. `python3 loom-code/scripts/check_mechanisms.py --baseline origin/main` → 結束碼 **0**（全綠）。
  2. 把整棵樹複製到暫存資料夾，把清單裡第一個機制的配套測試欄清空 →
     `RED [R4] capture-intent: mechanism has no eval:`，結束碼 **1**。
  3. 還原清單，改成在 `loom-code/skills/review/SKILL.md` 塞一個沒登記的閘門標記 →
     `RED [R1] review.fake-new-gate: class prose-gate: recomputed but not registered`，結束碼 **1**。
  4. 另外跑它自己的測試：`python3 -m pytest loom-code/scripts/test_check_mechanisms.py -q` → **51 passed**，
     其中就有 `test_r1_unregistered_is_red`、`test_r4_missing_eval_is_red`、
     `test_r3_net_increase_without_budget_exception_is_red` 三個把這件事釘住。
- **結果**：兩種弄壞方式都紅。**但有一個保留**：「機制總數不准變多」這一條目前只是警告，
  不是紅燈——因為要比較的舊基準（main）上還沒有這份清單，機器只能用近似值比（41 對 128，兩邊母體不同）。
  合併之後 main 帶著清單，這條才會真的開始擋。輸出裡它自己就印了這句警告，沒有假裝過關。
- **證據**：上述四道指令的輸出（第 2、3 項在暫存複本上做，本專案未被更動）。
- **判定**：做到（總數那一格合併後才真正生效）。

---

## 你會看到的七種對話，我逐一對過

「說法住在哪」是指那句話的固定措辭寫在哪個檔；「走過了嗎」是指這次有沒有實際被人走過。

| # | 對話 | 說法住在哪 | 走過了嗎 |
|---|---|---|---|
| 1 | 「你要的是 ___，做完後你可以 ___。對嗎？」 | `loom-design/skills/capture-intent/SKILL.md:219`、`loom-code/skills/write-plan/SKILL.md:251`（兩處逐字相同） | 走過：本改動的第一個決策點；Codex 實走與 A/B 兩份冷讀都指認到它 |
| 2 | 「你下 ___ 會看到 ___。對嗎？」（只有產品改動會問） | `loom-design/skills/write-spec/SKILL.md:183`，措辭被 `loom-design/scripts/spec/test_write_spec_contract.py:66` 釘住 | 走過：本改動的第二個決策點；B 題冷讀指認 |
| 3 | 驗收：「我這樣試、結果這樣、對你的資料做了什麼、我替你決定了什麼」 | `loom-code/skills/review/references/blind-run-report.md`（固定段落被 `loom-code/scripts/test_reviewer_agent_single_contract.py:301` 釘住）；呈現動作在 `loom-code/skills/ship/SKILL.md:90` | 走過：就是你正在讀的這一份 |
| 4 | 重大且難回頭的選擇（併在 1 或 2 裡問，不另外停） | `loom-code/skills/write-plan/references/one-way-door.md`（五類 (a)–(e)），副本一致性由 `loom-code/scripts/test_one_way_door_copies.py` 釘住 | 部分走過：本改動第二個決策點問過「重大選型要不要問你」；沒有真的碰到動你既有資料那一類 |
| 5 | 「請在 Codex 輸入 `/hooks` 授權一次」 | `loom-code/skills/write-plan/SKILL.md:173`（中英各一句） | 走過：Codex 第二輪實走真的印出這句並停住、零檔案產出；只有按下去那一下沒真按 |
| 6 | 「做產品功能前要先有一份產品原則，我問你幾個問題」（併在第一個決策點裡） | `loom-design/skills/capture-intent/SKILL.md:201`，訪談題目在 `loom-code/contract/templates/PRINCIPLES-interview.md` | 走過：本 repo 原本沒有產品原則，這道閘正確擋下，訪談當場補齊、由你簽名 |
| 7 | 缺文件時固定三行提示（只顯示、不擋） | `loom-code/scripts/loom_checker.py` 的 `STANDING_WARN`，規則 `standing.warn`／`standing.silence` | 走過：#771 重走時逐字印出三行 WARN 並轉給使用者，沒有擋下流程 |

---

## 對你既有的資料做了什麼

**這次不是改別的專案，是把 monkey-skills 這個 repo 自己重寫一遍，動得很大。**
具體：刪掉 315 個以上的檔（舊的站、舊的檢查腳本、舊的測試家族）；
把約 150 份文件搬進 `docs/loom/2026-09-02-simple-loom-flow/evidence/`（用 `git mv`，歷史保留得到）；
`docs/loom/` 下的 `plans/`、`specs/`、`backlog/`、`design/` 五個舊倉庫**原地封存**（各留一行說明，內容不動、不轉換）；
舊的範例與說明檔加上「已封存」標頭；另外由你親自跑過一次歷史改寫，動到 5 個 commit 的標題。
**這個 repo 以外的任何資料都沒有被碰過。** 一個例外要說明：先前為了實測 Codex 的授權行為，
在你的 `~/.codex/config.toml` 留下了一筆信任紀錄（那是 Q4 的實測，不是這次盲跑做的）——
那是這次工作在 repo 外唯一的痕跡，你可以自己刪掉。
舊格式一律不再讀寫（這是當初就定下的「硬切換」），要看舊東西就翻 git 歷史或那些封存目錄。

## 我替你決定了

以下每一件我都自己拍板了，沒有停下來問你。覺得哪一條不對，就是這裡要改。

- **對話語言錨定的兩個檔留著** — 它們是你個人的語言習慣設定，不是 loom 的流程機制，
  所以歸類成「環境衛生」保留、不計進機制總數；重複的第三個檔刪了。要拿掉隨時可以。
- **所有刪除集中到後面一次做完** — 前面幾步只新增不刪。理由：舊測試互相牽連，
  邊做邊刪會連環紅。代價是中途那幾個 commit 裡新舊並存。
- **184 條舊待辦原地凍結，不逐條轉成新格式** — 逐條轉是一整個工作天的量，
  而「硬切換」本來就是原地封存。真正還會再犯的項目，之後由維護站重新開一張。
- **三個舊改動只有 #771 真的重跑，另外兩個用推算** — #772 今天有 67 個 commit，
  真重建等於重做一次；而且它加的機制在這次已經被刪掉，沒辦法在同一棵樹上對照。
  推算規則是用 #771 的實測校準的（誤差 −3%）。你不接受的話，替代方案是三個都真跑，約三個工作段。
- **舊的兩條散文檢查（Check 22／23）沒有搬過來** — 它們附著在已刪掉的舊機制上。
- **九個閘門的配套測試指向真實測試檔，而不是每站各寫一次冷讀** — 冷讀成本高且不可重跑；
  真測試可以在 CI 每次重跑。
- **「看得見的行為」那段只做結構檢查，內容好不好交給審查者** — 機器只確認每條真的寫了
  「操作 → 反應」，不判斷寫得好不好。改成內容判定試過三輪都失敗（誤擋合法寫法）。
- **一批工作的分組以計畫寫的為準，並在站點文件裡寫死同一個定義** — 重跑 #771 時撞到
  「計畫的分組」和「站點的定義」兩種說法不一致，多派了一次工，已修。
- **對抗測試的檔案放在 `evidence/probes-` 底下** — 原本計畫沒替這種檔留位置，是重跑時發現的。
- **這個 repo 的產品原則由我起草、你批准** — 原計畫寫「不新建」，但新的閘門正確地擋下
  「產品改動卻沒有產品原則」，所以我依你歷次裁定代填，你在第一個決策點簽了名。
- **（你決定的，不是我）** 契約版本要求只綁在決策地圖這一個工具上、
  兩個獨立工具不計入數量——這兩條是你 2026-09-02 當場裁定的。

**被審查者駁回、而且嚴重度在「重要」以上的**，只有一條：

- **對抗測試的「內容」沒有機器約束，寫一個空檔也算合格**（編號 W1-13，重要）。
  由審查者 `opus-review-code-w1d` 判定「在既定的威脅模型下接受這個最弱環節」而駁回，
  理由是這套東西假設沒有人在作弊，只防手滑。若這個假設對你是錯的（例如將來多人共用這個 repo），
  這裡就是它會破的地方。已同時記在設計文件與待答問題裡。

## 我不確定你要什麼

1. **專有名詞 61 個，你當初想壓到 40 以內。** 現在數得出來是 61（舊版約 113）。
   要繼續壓，就得再合併概念；也可以就把目標改成 60。你要哪個？
   → **你決定：改成 ≤60（2026-09-03）。** 已寫進 intent。
2. **每次中途檢查固定多三個 commit，小改動因此打平而不是變輕（#771 的 34 對 31）。**
   便宜的修法有兩種：把「記派工」和「放檢查產出」併成同一個 commit，或者一批工作只記一次。
   要不要做？（我這次沒有動它。）
   → **你決定：現在不動，merge 後用真實改動量過再調（2026-09-03）。** Acceptance #4 這一格記為未達。
3. **還沒有在真的裝好外掛的對話裡從頭走過一次。** 建議合併後的第一個改動就當作真正的首跑，
   如果那次不順，代價是再修一輪站點文字。
   → **你決定：首跑題目＝第 2 點的 checkpoint commit 係數（2026-09-03）。** 已寫進 intent。
4. **Codex 的 `/hooks` 授權畫面沒有真的按過。** 你第一次在 Codex 用它時會看到，
   如果那個畫面跟文件寫的不一樣，告訴我。
5. **loom-design 不能單獨裝。** 你當初的條件是「loom-code 必須能獨立安裝」，這點成立；
   但反過來不成立——loom-design 依賴 loom-code 的契約與檢查器，單獨裝會直接擋下來並叫你去裝 loom-code。
   如果你以為三個都能各自獨立，這裡跟你的預期不一樣。
6. **「機制總數不准變多」現在只是警告。** 要等這次合併進 main、清單跟著上去，
   下一次才會真的擋。這段空窗期是一次性的。

## 補跑：W4 fix round（2026-09-03，樹 40442b1e）

上面整份報告原本是照 `c43641e6` 寫的。之後又補跑了一輪修正（W4 fix round，內部叫 W4-06～W4-10），
這裡只重試那一輪可能改到的地方，不是整份重來一次。

1. **驗收條件 2（Claude Code 跟 Codex 走出來要一樣，只差 `/hooks` 這個授權畫面）**：
   讀 `.codex/hooks.json`，確認裡面有「跑指令前檢查」（PreToolUse）指向 `.codex/hooks/loom-checker`，
   加上兩支本 repo 自己的「寫檔後檢查」（PostToolUse）。再跑一次腳手架指令
   （`codex_scaffold.py --repo .`），它回報「已經是最新，沒有改動」——代表這支腳本現在是「合併」
   不是「整檔覆寫」，不會把 repo 自己的檢查洗掉（這是上一輪審查抓到的重大問題，這次修好了）。
   接著跑它自帶的自我測試，通過。最後確認舊的、已經刪掉的授權檔案（git-guard-shim.sh）
   真的不在了。**結果：全部通過。**（互動式的 `/hooks` 畫面本身仍然沒有真的按過，見文末。）
2. **驗收條件 7（沒配套測試的新機制要讓 CI 變紅）**：跑 `check_mechanisms.py --baseline` 和
   `--measure` 兩個指令（CI 設定檔裡就是這樣呼叫）。**結果：兩者結束碼都是 0（正常）**，
   `--baseline` 多印了一行提醒（沒有基線清單可比對，用估算頂替），不影響結束碼。
3. **套件測試**：`python3 -m pytest loom-code/scripts/ scripts/ .claude/hooks/ -q`
   → **`1004 passed in 129.69s (0:02:09)`**，全綠。
4. **push 閘（使用者按下推送前，機器會不會攔）**：跑 `loom_checker.py push`，**結束碼 1，擋下 5 條**：
   `push.reviewed-sha`（記錄的審查版本跟目前要推的版本對不上）、
   `push.probes-adversarial`（對抗測試引用的證據檔不在目前這棵樹上，或跑的版本不對）、
   `push.second-vendor-honoured`（原本說好要找第二家審查工具，這輪沒找）、
   `push.dispatch-covers-tasks`（有 5 個任務找不到「誰做的」紀錄）、
   `push.frozen-store-untouched`（一個本來說「封存後不能再動」的舊檔案，這個 branch 上仍顯示被動過，
   雖然內容跟封存前一字不差）。
   **這點跟原計畫的預期不同**——`plan.md` 寫這一輪修完後，後兩條不該再擋，但我這次乾淨重試，
   它們仍然擋。這是我親手跑出來的事實，寫下來給你判斷，我沒有去改任何東西讓它變綠。
5. **計數（給第 1 條「派工共幾人次」用）**：`review.json` 裡派工紀錄我補跑時 **79 筆**
   （審查 28、實作 40、對抗 4、盲跑 7；這個數每派一次審查就加一，所以我試過之後又多了幾筆——最終數以 review-only commit 為準，那之後不會再變）；對抗與測試探針 **21 筆**（拿掉 4 筆不能重跑的文字紀錄後）；未結案的問題（`open_findings`）
   共 **88 筆，其中 10 筆仍標「未結案」（open）**。
6. **commit 數（不加判斷，純陳述）**：`923fb84a`（上一份報告的比對起點）到 `40442b1e`
   （這次試的版本）之間，共 **213 個 commit**。

**這次沒試的部分**：互動式的 Codex `/hooks` 授權畫面（第 1 點提過，需要真人操作）；
以及真正按下「推送」那個動作本身——上面第 4 點只是跑檢查指令，沒有真的去推。

## 再補跑：最終樹 1639e337（2026-09-03）

上一段補跑試的是 `40442b1e`；之後又有一輪修正動到「檢查器」本身的程式碼
（commit `f600c281`：檢查器裡一句說明文字、一個釘住行為的測試、還有 Codex 那邊用的同一份檢查器複本）。
這裡只重試這一輪可能改到的地方，用的是最終版本 `1639e337`。

1. **先確認這輪真的只改了這三個檔**：拿 `40442b1e` 跟 `1639e337` 比對，排除文件、README、CHANGELOG 之後，
   改動的檔案剛好就是「檢查器程式、它的一個釘住測試、Codex 那份複本」三個，**跟預期一致**。
2. **套件測試**：`python3 -m pytest loom-code/scripts/ scripts/ .claude/hooks/ -q`
   → **`1005 passed in 129.60s (0:02:09)`**，全綠（比上一輪多一個測試，符合新加的那一個）。
3. **腳手架自我測試**（把檢查器裝進一個假的 Codex 專案、確認它真的能擋住一次假推送）：
   `python3 loom-code/scripts/codex_scaffold.py --self-test` → **通過，結束碼 0**；
   跑完後檢查這個 repo 本身有沒有被動到（`git status` 應該乾淨）→ **乾淨，沒有任何殘留改動**。
4. **機制清點**（新機制有沒有配套測試、開場字數有沒有超標）：
   `check_mechanisms.py --baseline 923fb84a` → 結束碼 **0**（全綠，附一行「沒有舊清單可比對，用估算」的提醒，不影響結果）；
   `check_mechanisms.py --measure` → 結束碼 **0**（開場字數 658，遠低於門檻 2640）。
5. **規則清點與推送閘**：機器管的規則 **27 條**（跟上一輪一樣）。
   跑 `loom_checker.py push`，**結束碼 1，擋下 4 條**：
   `push.reviewed-sha`（記錄的審查版本跟目前要推的版本對不上）、
   `push.probes-adversarial`（對抗測試引用的證據檔在目前這棵樹上找不到，或跑的版本不對）、
   `push.second-vendor-honoured`（原本說好要找第二家審查工具，這輪沒找）、
   `push.frozen-store-untouched`（一個說好「封存後不能再動」的舊檔案，這個 branch 上仍顯示被動過）。
   這次沒有看到擋在「審查對象必須是最上面那個 commit」那一條——但我不去解讀為什麼，只記錄實際看到的結果，
   也沒有去做任何事讓它變綠。
6. **派工與問題計數**（讀 `review.json`）：派工紀錄共 **94 筆**（審查 32、實作 50、對抗 4、盲跑 8——
   比上一輪的 79 筆多，因為每多派一次審查這個數就會加一）；對抗與測試探針 **21 筆**（拿掉 4 筆不能重跑的文字紀錄後；收尾時重跑再加 19 筆）；
   未結案的問題（`open_findings`）共 **88 筆，其中 10 筆仍標「未結案」（open）**——這兩個數字跟上一輪一樣。

**這次沒試的部分**：互動式的 Codex `/hooks` 授權畫面（要真人在 Codex 介面上按一下，前面兩輪都提過）；
以及真正按下「推送」那個動作本身——上面第 5 點只是跑檢查指令，沒有真的去推。
