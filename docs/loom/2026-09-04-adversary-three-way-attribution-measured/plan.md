# Adversary three-way attribution decided by repeated measurement, not another sentence — plan
intent: 2026-09-04-adversary-three-way-attribution-measured@34683950

Change id: `2026-09-04-adversary-three-way-attribution-measured` (abbreviated `<cid>` below; the evidence directory is `docs/loom/<cid>/evidence/`).

## Current State Evidence
- Forward (the sentence under measurement): `loom-code/agents/adversary.md:12-20` — the positioning paragraph; its last sentence (`:18-20`, "Not yours either: omission, overclaim and contradiction go to the reviewer to reconcile; a positive, executable RED belongs to the implementer") was added by PR #789 and is pinned by `loom-code/scripts/test_review_station_text.py:212` (`test_adversary_agent_paragraph_has_three_way_attribution_sentence`) under a sentence cap of ≤6 sentences / ≤40 words each (`:147`) and a body cap of 600 words (`loom-code/scripts/test_reviewer_agent_single_contract.py:34`, `AGENT_CAPS`). The reviewer counterpart is `loom-code/agents/reviewer.md:13-22`, pinned by `test_review_station_text.py:166`.
- Forward (the contract before the cap change): commit `4ab5224d` is the parent of the #789 merge; `git show 4ab5224d:loom-code/agents/adversary.md` and `...:loom-code/agents/reviewer.md` are the "pre-cap" texts Acceptance 2 asks to baseline. They carry no three-way sentence.
- Reverse (the method being replaced): `docs/loom/2026-09-04-reviewer-and-adversary-positioning/blind-run-report.md:9-21` — one `claude -p --model sonnet` call per contract, the contract given by path, the 8-item list given inline, the answer asked as "mine / the other role's / the implementer's". Three single samples per side: `evidence/coldread-adversary.txt` (TRIAL 2 at `:13`, TRIAL 3 at `:26`) and `evidence/coldread-reviewer.txt` (`:14`, `:28`).
- Error (what the measurement must resolve): in TRIAL 3 the adversary reader gave items 4 and 5 to the implementer (`coldread-adversary.txt:37,39`) where the expected owner is the reviewer; items wrong differed across the three trials (7/8, 7/8, 6/8), so nothing distinguishes wording bias from sampling noise — the #789 memory line records that two samples wrong on the same item with the same reasoning is bias, one sample cannot tell.
- Data (the fixture): `docs/loom/2026-09-04-reviewer-and-adversary-positioning/evidence/coldread-findings-list.txt` — 8 quoted, numbered lines; expected owners from `blind-run-report.md:21`: reviewer 1, 4, 5, 7; adversary 2, 3, 6; implementer 8.
- Boundary (what the repo offers and forbids): no script in this repo shells out to `claude -p` yet; the closest subprocess conventions are `loom-code/scripts/git_exec.py:66-77` (argv list, `capture_output`, timeout, `(OSError, TimeoutExpired)` caught) and tests that monkeypatch `subprocess.run` on the module (`loom-code/scripts/test_git_exec.py:47-56`). Test files sit beside the script as `test_<script>.py` with a bare `import <module>`. `claude -p --help` exposes `--model` and `--output-format` but no seed flag. The intent forbids new checker rules, CI wiring, and any `docs/` citation inside the contracts; `python3 loom-code/scripts/check_contract_citations.py` is the guard. A version bump is enforced by `loom-code/scripts/test_probes_positioning.py:499` and the Codex mirror manifest follows via `scripts/sync_codex_manifests.py`.

## Task DAG

Two waves, two checkpoints (wave 1 end; wave 2 end doubles as branch end). No `after-task` markers. Wave 1 is tooling (code lane, adversary-first per build); wave 2 is measurement and the decision the measurement drives.

**W1-01 Fixture: the 8-item list and expected owners as a machine-readable file**  after: —
- Files: `docs/loom/<cid>/evidence/fixture-coldread-8.json` (fields: `items: [{n, text, expected}]` with `expected ∈ {reviewer, adversary, implementer}`; `source: "docs/loom/2026-09-04-reviewer-and-adversary-positioning/evidence/coldread-findings-list.txt"`); `docs/loom/<cid>/evidence/probes/test_fixture_items_verbatim_prior_list.py`.
- Test: `test_fixture_items_verbatim_prior_list` — each `text` equals, character for character, the quoted body of the corresponding line in the #787 list file (number and surrounding quotes stripped), and the expected map is exactly `{1,4,5,7}→reviewer, {2,3,6}→adversary, {8}→implementer`. Fails today because the JSON does not exist.
- Risk: the prior list file is the only source of truth for "verbatim"; the fixture must be derived from it, never retyped. agent-decided — JSON over YAML because the script and its tests use only the standard library.

**W1-02 Scoring core: parse one cold-read answer, score N answers**  after: W1-01
- Files: `loom-code/scripts/coldread_role_split.py` (pure functions only in this task: `parse_response(text, n_items) -> dict[int, str]`, `score(responses, fixture, role) -> dict`), `loom-code/scripts/test_coldread_role_split.py`.
- Test: `test_score_counts_labels_per_item_and_flags_systematic` — given three canned responses and the fixture, `score(...)` returns per-item counts over the label set `{mine, other, implementer, unparsed}`, `own_not_own_correct` (runs × items where `mine`-vs-not agrees with expected), `three_way_correct`, `n`, and `systematic: [n, ...]` listing every item wrong in ≥50% of runs whose wrong label is the same in ≥50% of runs. Companion tests: a line that does not start with `<n>. <label>` counts as `unparsed` and is never scored as correct; `--role reviewer` maps expected `reviewer`→`mine`, `adversary`→`other`, `implementer`→`implementer`; the symmetric mapping for `--role adversary`.
- Risk: the answer format must be parseable without an LLM judge, so the prompt (W1-03) pins the format `<n>. mine|other|implementer — <reason>` and the parser reads only the leading token. agent-decided — the "systematic" rule is computed by the script, not judged by the implementer, so W2-02's branch is mechanical; thresholds are the intent's (≥50% wrong, same wrong label ≥50%).

**W1-03 CLI and runner: N `claude -p` calls, per-run transcript, summary.json**  after: W1-02
- Files: `loom-code/scripts/coldread_role_split.py` (adds `build_prompt`, `run_once`, `main`), `loom-code/scripts/test_coldread_role_split.py`.
- Test: `test_main_writes_run_files_with_command_line_and_summary` — with `subprocess.run` monkeypatched to return canned stdout, `main(["--contract", c, "--fixture", f, "--role", "adversary", "--runs", "3", "--out", d])` writes `run-1.txt`…`run-3.txt`, each opening with a header (`# command: claude -p … --model sonnet --output-format text`, `# contract: <path> sha256 <hex>`, `# fixture: <path> sha256 <hex>`, `# run: i of N`, `# timestamp`) followed by the raw stdout, and `summary.json` carrying `argv`, `n`, `model`, `seed: null` with `seed_note: "claude -p exposes no seed flag"`, contract and fixture hashes, the per-item table and scores from W1-02, and `systematic`. Companion tests: `--runs 0` exits 2 with a message; a missing `claude` binary (`shutil.which` → None) exits 2 naming the binary; `--resume` skips run files that already exist and scores all N; a `TimeoutExpired` on one run is written as that run's transcript with `# error:` and scored `unparsed` for every item, the loop continues.
- Risk: (1) the contract is fed **inline** in the prompt rather than as a path — agent-decided — so a pre-cap text extracted from git and the current file are read the same way and the run does not depend on cwd or on the files the contract tells its reader to open; this deviates from #787's path-only method and is stated in `summary.json` (`contract_delivery: inline`). (2) Each call takes 30–90 s; N=10 sequential is 5–15 min, longer than a subagent's default Bash timeout — `--resume` exists so an interrupted batch continues, and the dispatch packet must set a 300 s tool timeout per invocation or run in batches. (3) Defaults: `--model sonnet`, `--runs 10`, `--timeout 180`, `--claude-bin` from `PATH` overridable by `COLDREAD_CLAUDE_BIN`; no `--seed` flag is offered because none exists downstream.

**W2-01 Baselines: four N=10 distributions, side by side**  after: W1-03
- Files: `docs/loom/<cid>/evidence/contract-precap-adversary.md`, `contract-precap-reviewer.md` (verbatim `git show 4ab5224d:loom-code/agents/<x>.md`); `evidence/baseline-precap-adversary/`, `baseline-precap-reviewer/`, `baseline-current-adversary/`, `baseline-current-reviewer/` (each `summary.json` + `run-1..10.txt`); `evidence/baselines.md` (one table: per item, per contract, % wrong on three-way, dominant wrong label, own/not-own %; plus the `systematic` list of each summary and the four exact command lines); `evidence/probes/test_baselines_four_dirs_n10_sonnet.py`.
- Test: `test_baselines_four_dirs_n10_sonnet` — the four `summary.json` exist, each has `n == 10`, `model == "sonnet"`, ten `run-<i>.txt` files whose first line starts with `# command:`, and the contract hash of each "current" summary equals `sha256` of the checked-in agent file at that commit. Fails today because no directory exists.
- Risk: 40 real API calls; the runs are the evidence Acceptance 2 names, so they are committed raw (transcripts are a few KB each). agent-decided — the pre-cap contract copies are committed so the baseline is re-runnable without git history. If `claude` refuses to run nested inside the implementer's session, the implementer reports BLOCKED and the orchestrator runs the four commands itself and commits them under the task trailer.

**W2-02 Decision by the numbers: fix the wording, or explain it in the README**  after: W2-01
- Files — arm A, `baseline-current-adversary/summary.json` has a non-empty `systematic` list: `loom-code/agents/adversary.md` (rewrite of the positioning paragraph's last sentence only, within ≤6 sentences / ≤40 words and the 600-word body cap, citing nothing under `docs/`), `loom-code/scripts/test_review_station_text.py:212` (the pinned literal follows the new sentence; affirmative verb, no negation token), `evidence/after-fix-adversary/` (N=10, same fixture), `evidence/baselines.md` (a third column), `evidence/probes/test_after_fix_worst_item_le_20pct.py`.
- Files — arm B, `systematic` is empty: `docs/loom/README.md` (a paragraph under `## checkpoint 的三個驗證角色什麼時候被觸發` explaining who takes what is not theirs — reviewer: omission, overclaim, contradiction, cross-document counts; adversary: probe artifacts, boundaries; implementer: a positive RED — and one line stating the measured distribution and why the wording is left alone), `docs/loom/intent/<cid>.md` (a `## Measurement record` section: the four summaries' own/not-own and three-way numbers, the worst item and its wrong rate, the sentence "measured, not a wording problem"), `evidence/probes/test_readme_role_section_has_three_way_paragraph.py`.
- Test — arm A: `test_after_fix_worst_item_le_20pct` — the item with the highest three-way wrong rate in `baseline-current-adversary` has wrong rate ≤ 20% in `after-fix-adversary`, and that baseline rate was ≥ 50%. Arm B: `test_readme_role_section_has_three_way_paragraph` — the README section names all three roles within one paragraph that contains the word "three-way"/「三方」, and the intent has the `## Measurement record` section with four numeric lines. Either probe fails today.
- Risk: the arm is chosen mechanically from `summary.json`; the implementer records which arm and the triggering numbers in its commit body. agent-decided — the README paragraph is written in the language of its surrounding section (Chinese) because the section is user-facing store documentation, not an internal artifact under the English policy; reviewers may nit this. Arm A leaves the reviewer contract untouched: Acceptance 4 uses the reviewer side as a control only. If arm A's re-measure does not reach ≤20% on the worst item, the task stops at DONE_WITH_CONCERNS with both distributions committed; a second rewording is a new intent, not a fix round (the #787 history shows "one more sentence" moves conservatism, not accuracy).

**W2-03 Version bump and changelog**  after: W2-02
- Files: `loom-code/.claude-plugin/plugin.json` (1.4.0 → 1.4.1), `loom-code/.codex-plugin/plugin.json` via `python3 scripts/sync_codex_manifests.py`, `loom-code/CHANGELOG.md` (`## [1.4.1] — <date> — cold-read role-split measurement script, adversary attribution baselined at N=10`, one line per shipped item including which arm W2-02 took).
- Test: `evidence/probes/test_changelog_carries_1_4_1.py` — `plugin.json` version is `1.4.1`, the Codex mirror agrees, and `[1.4.1]` appears in the changelog. Fails today.
- Risk: none beyond forgetting the mirror; the sync script is the guard. agent-decided — patch bump, as the intent says: a new script and evidence, no contract semantics change unless arm A, which is still wording within the same cap.

**W2-memory Memory step — graduated probes and store entries**  after: W2-03
- Files: graduated copies of the probes that outlive this change — `loom-code/scripts/test_probes_coldread_role_split.py` (from `evidence/probes/`: the fixture-verbatim check rewritten to read the fixture and the prior list by repo-relative path, the baseline-shape check, and whichever of the W2-02 probes applies) — plus one `docs/loom/memory/` entry each for: the "systematic vs noise" rule as numbers (≥50% wrong, same wrong label ≥50%, N=10), the inline-contract delivery decision, and the `claude -p` no-seed fact.
- Test: `python3 loom-code/scripts/loom_checker.py memory` (the store integrity check) exits 0 and `python3 -m pytest loom-code/scripts/test_probes_coldread_role_split.py -q` passes.
- Risk: graduated probes that read `docs/loom/...` only work in this repo; that is the established convention for graduated probes here. Implementer is the orchestrator itself (`fresh_context: false`), dispatch entry written before the work.

## Questions asked
1 — what — (the restatement for this intent was asked and answered "yes" in the session that wrote it, the same session as PR #789; that session's store shows `status: confirmed 2026-09-04` but the verbatim text was not recorded in this change's store)
1 — consequence — 這次要不要用 Codex 當第二位讀者？多花幾分鐘與額度（答：要 — second vendor codex; the review station records it in `second_vendor` at the first checkpoint）

## Risks
1. **The measurement itself may be noisy at N=10.** Ten runs resolve "≥50% wrong" against "≤20% wrong" with a wide margin but cannot resolve 30% from 40%; the intent's thresholds were chosen for exactly that gap, and `baselines.md` reports counts, not only percentages, so a reader sees 5/10 rather than "50%".
2. **Nested `claude -p` from a subagent.** No script here has done it; the blind-runner of #787 did it by hand. If the implementer's environment refuses, W2-01's fallback is the orchestrator running the commands (see the task). The four command lines are in `baselines.md` either way, so any later session can re-run them.
3. **Cost and time**: 40 calls for W2-01, 10 more on arm A, 2 more per blind-run spot check. All sequential; nothing runs in CI (intent Out of scope).
4. **Portability of the contract text**: arm A edits `adversary.md`; `check_contract_citations.py` must stay clean and the sentence caps must hold — the implementer runs both before committing.
5. **The pre-cap baseline is a different document, not only a different sentence**: #788→#789 also changed the reviewer paragraph and the word cap; `baselines.md` says so, and the comparison Acceptance 2 asks for is descriptive (where the errors cluster), not causal.
6. One-way doors: none found after decision point ①. No existing data is rewritten; the intent gains a section only on arm B, and the confirmed sections are never edited.
