# 畢業探針不得依賴本機分支歷史 — 我試了什麼、發生了什麼

2026-09-06 在一份乾淨的專案副本上試的，版本號前七碼 `6b73060b`（第一次試是 2026-09-05 在 `294d85ea`；審查提出兩個問題後，在新的版本號上重跑並補上完整輸出，見下）。

這次是第一波（wave-end:1）的檢查點：新命令跟刪測試這兩件事已經做完，但「build 站的文字要求先跑這命令」跟「版本號加一」還沒做（在下一波）。所以第 3、4 條有一部分現在還試不了，下面會分別說明能試到哪裡。

## 你要的東西，一條一條試

### 1. 在任一分支下一個命令，它自己建出像 CI 的乾淨副本跑畢業探針，列出每支失敗與每支跳過的理由；本機綠、副本紅時結束碼非 0，全綠結束碼 0

- **我怎麼試的**：先看這個新命令的說明（`--help`），再直接在乾淨副本上跑一次真正的命令（跑了全部畢業探針，385 支）。第一輪審查指出我只在一種 Python 版本下試過，而你們的自動化檢查用的是另一個版本，所以這次同一個命令用兩種直譯器各跑一次：一次用這台機器預設的版本，一次用你們自動化檢查實際用的版本。接著另外搭了兩個一次性的小型測試專案，專門用來驗證「結束碼」這件事：
  - 專案 A：只放一支故意讀「本機才有的 main 分支」的假探針，直接跑這個命令。
  - 把同一支假探針改寫成「先試遠端的 main、都沒有才跳過」，再跑一次。
  - 專案 B：完全沒有遠端 main 這種東西的情況下再跑一次，確認「跳過」這條路徑本身也會被列出來、理由也讀得到。
- **發生了什麼**：
  - 在真正的專案上、用這台機器預設的版本（3.12）：

    ```
    $ python3 --version
    Python 3.12.11
    $ python3 loom-code/scripts/rehearse_probes.py
    Rehearsed 6b73060b3eefa9406646524dce938f64cb1298fc (…/blind-we1-fix)

    FAILED (0)
    SKIPPED (0)

    ........................................................................ [ 18%]
    ........................................................................ [ 37%]
    ........................................................................ [ 56%]
    ........................................................................ [ 74%]
    ...........................................x............................ [ 93%]
    .........................                                                [100%]
    384 passed, 1 xfailed in 70.63s (0:01:10)
    EXIT:0
    ```

  - 同一個命令，換成你們自動化檢查實際用的版本（3.11，用 `uv` 借出一個乾淨的 3.11 環境跑，不動這台機器本身）：

    ```
    $ uv run --python 3.11 --with-requirements requirements-dev.txt --with wcwidth -- python --version
    Python 3.11.14
    $ uv run --python 3.11 --with-requirements requirements-dev.txt --with wcwidth -- python loom-code/scripts/rehearse_probes.py
    Rehearsed 6b73060b3eefa9406646524dce938f64cb1298fc (…/blind-we1-fix)

    FAILED (0)
    SKIPPED (0)

    ........................................................................ [ 18%]
    ........................................................................ [ 37%]
    ........................................................................ [ 56%]
    ........................................................................ [ 74%]
    ...........................................x............................ [ 93%]
    .........................                                                [100%]
    384 passed, 1 xfailed in 59.30s
    EXIT:0
    ```

    兩個版本的結果一致：0 失敗、0 跳過、結束碼 0。（前一輪審查抓到的問題本身出在版本 3.12 才有、3.11 沒有的一個寫法，已經在這個版本之前修好了；這次兩種版本都試過，不再只信任其中一種。）
  - 在專案 A（假探針讀本機 main）：

    ```
    $ python3 loom-code/scripts/rehearse_probes.py --repo <專案A路徑> loom-code/scripts/test_probes_toy_history.py
    Rehearsed 3f7c510d78c07e11f3d22bfab85452eb6d4f3297 (<專案A路徑>)

    FAILED (1)
    FAILED loom-code/scripts/test_probes_toy_history.py::test_toyprobe_readsLocalMain_isRedInCleanClone
    SKIPPED (0)

    F                                                                        [100%]
    E       AssertionError: fatal: invalid object name 'main'.
    E       assert 128 == 0
    1 failed in 0.10s
    EXIT:1
    ```

  - 改寫成先試遠端 main、都沒有才跳過之後：

    ```
    $ python3 loom-code/scripts/rehearse_probes.py --repo <專案A路徑> loom-code/scripts/test_probes_toy_history.py
    Rehearsed 7fa27e30fcf5105fbfb83f64f9055b16a287a5b1 (<專案A路徑>)

    FAILED (0)
    SKIPPED (0)

    .                                                                        [100%]
    1 passed in 0.10s
    EXIT:0
    ```

  - 專案 B（連遠端 main 都沒有）：

    ```
    $ python3 loom-code/scripts/rehearse_probes.py --repo <專案B路徑> loom-code/scripts/test_probes_toy_history.py
    Rehearsed 502d79916d9b7ff9d13a764d3537154726d036a2 (<專案B路徑>)

    FAILED (0)
    SKIPPED (1)
    SKIPPED loom-code/scripts/test_probes_toy_history.py::test_toyprobe_readsOriginMainFirst_skipsWhenNothingResolves: neither origin/main nor a local main resolves in this clone

    s                                                                        [100%]
    1 skipped in 0.11s
    EXIT:0
    ```

    跳過不會讓命令變紅——這條路徑本身也印出了理由，而且結束碼仍是 0。
- **證據**：以上六段完整終端輸出，涵蓋兩種直譯器版本（3.12、3.11）與三個情境（真正的專案、假探針紅、假探針改寫後綠含跳過路徑）。
- **結論**：做到了。

### 2. 用一支故意讀本機 main 的合成探針試它：命令要紅；把探針改成先讀遠端 main、都沒有就跳過，命令要綠

- **我怎麼試的**：同上一條的專案 A——這條驗證的正是同一組操作，這裡把它單獨列出來對照。
- **發生了什麼**：讀本機 main 的版本在乾淨副本裡讀不到，命令回報失敗、結束碼 1；改寫成先讀遠端 main、都沒有才跳過的版本，命令回報通過、結束碼 0。
- **證據**：與第 1 條相同的兩段終端輸出（`FAILED (1)` … `EXIT:1`，接著 `FAILED (0)` … `EXIT:0`）。
- **結論**：做到了。

### 3. build 站的記憶步驟文字寫明「畢業前跑這命令，紅的不得畢業」，且有測試釘住那句話

- **我怎麼試的**：打開 build 站現在的文字，找「畢業」那一段，看有沒有提到這個新命令。
- **發生了什麼**：目前那一段只講「把探針複製進永久測試目錄」，完全沒有提到要先跑這個命令、也沒有講紅了不能畢業。這件事排在下一波（版本號、站文字都還沒動）。
- **證據**：讀了 build 站文字裡「畢業」那一段的現況，裡面沒有這命令的名字。
- **結論**：現在還試不了——這是下一波要做的事，不是這一波漏做。

### 4. 這個 change 合併後，在 main 上跑這命令：0 失敗，且跳過理由裡沒有「commit 不在歷史」「change 已 shipped」這兩類

- **我怎麼試的**：在乾淨副本（也就是這個分支目前的樣子，還沒合併）上直接跑了一次完整命令，檢查失敗清單跟跳過清單的每一句理由。另外確認了以前那兩類「必定跳過」的測試程式碼是不是真的不見了。
- **發生了什麼**：0 支失敗。跳過清單只有 1 支，理由是前面提過的文字判讀邊界案例，跟「歷史讀不到」「commit 不在」「已經 shipped」這些完全無關。原本那兩類——一類是讀某個已經被壓縮合併、本機才找得到的 commit，一類是讀某個 change 是否已經出貨的分支狀態——對應的程式碼已經不在測試檔裡了。
- **證據**：完整命令的終端輸出（385 支裡 0 失敗、0 支跳過，貼在上面第 1 條——這次重跑用的版本比第一次試的版本又新，原本那 1 支跟歷史無關的跳過也已經在這個版本被處理掉，跳過清單是空的，結束碼 0）；在測試檔裡搜尋舊有兩個判斷函式的名字，沒有找到：

  ```
  $ grep -rn "_confirm_intent_sha\|_skip_if_language_policy_shipped" loom-code/scripts/test_probes_complexity_wave_end.py loom-code/scripts/test_probes_language_policy.py loom-code/scripts/test_probes_language_policy_branch_end.py
  (no output)
  ```
- **結論**：以現在的分支狀態（尚未合併）試，結果符合要求；等真正合併到 main 之後，還需要在 main 上再跑一次做最終確認——這件事排在關閉這個 change 的那一步，不是現在能做的。

## 對你既有的資料做了什麼

沒有——這個 change 不會動到你已經有的任何東西。這個新命令只會在系統的暫存資料夾裡建一份完整專案的乾淨副本來跑測試，跑完就把那份副本收掉；你的工作目錄本身只被讀取，不會被寫入。刪掉的那幾支測試是版本控制內的測試檔案，仍然能從歷史紀錄裡找回來。

## 我幫你決定的事

- **乾淨副本要「完整歷史、有遠端 main、沒有本機 main」這個形狀，而不是更嚴格的淺層副本** — 因為這正是你們的自動化檢查在真正跑的形狀；改用更嚴格的形狀不在這次的範圍內，之後想測那個形狀要另外開一次改動。
- **命令的結束碼只看失敗、不看跳過** — 我判斷跳過是「值得你自己讀一下理由」的資訊，而不是自動判定失敗，理由是這次要抓的問題本來就是「探針靠不住的東西」，跳過就是探針老實承認靠不住，跟测试真的紅了不是同一件事。如果之後發現這個判斷太寬鬆（跳過的東西其實根本沒被驗證到），可以改成讓某些跳過也算紅。
- **原本三次事故裡累積的舊測試，選擇刪掉而不是改寫成新形式** — 因為它們本來就只驗證「本機才有的東西」，改寫等於重寫；被刪的測試名稱跟被刪的理由都寫在那次改動的紀錄裡，可以核對。
- 到目前為止，沒有任何審查意見被判定為「重要以上但不處理」——這個檢查點的審查記錄還沒有收到具體意見。

## 你可能還沒想清楚的地方

- 這個新命令目前只是「文字上要求跑」，不是強制擋下推送的機制——如果下一次改動又在真正的自動化檢查裡出現紅字，那就代表光靠文字提醒不夠，得考慮把它做成真正擋得住人的機制。
- 第 3、4 條要等下一波（文字加上去、版本號加一、合併到 main 之後）才能完整驗收；這份報告目前只能證明「命令本身做得對」，還不能證明「大家真的會照著做」。
