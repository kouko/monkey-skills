# Graduated probes rehearsed in a CI-shaped clone before graduation — plan
intent: 2026-09-05-graduated-probes-independent-of-local-history@52b2ac76

Change id: `2026-09-05-graduated-probes-independent-of-local-history` (`<cid>` below; evidence lives in `docs/loom/<cid>/evidence/`). Branch: `graduated-probes-clean-clone`.

## Current State Evidence
- Forward (the step that graduates probes): `loom-code/skills/build/SKILL.md:336-344`, the **Probe graduation** paragraph of §6.5 — copy `evidence/probes/` into the permanent test directory as byte copies, rename on a name collision, cold-read reports never graduate. No sentence there, and no script anywhere in `loom-code/scripts/` or `scripts/`, runs the copies in an environment other than the working tree. The paragraph is pinned by `loom-code/scripts/test_build_station_text.py:95` (`test_memorysection_names_graduation_and_memory_store`); the file's word count is 3165 against a soft cap of 3750 (`:50`).
- Reverse (what a rehearsal is supposed to look like today): `docs/loom/memory/pre-branch-end-ci-rehearsal-uses-full-history-without-a-local-main.md` — prose only: full-history `git clone --no-local`, no local `main`, `origin/main` first and skip when nothing resolves. CI checks out with `fetch-depth: 0` (`.github/workflows/loom-code-ci.yml:97`) and runs `python3 -m pytest loom-code/scripts/ scripts/ .claude/hooks/ -v -n auto` (`:115`); the PR head is detached, so `origin/main` resolves and a local `main` does not.
- Error (measured 2026-09-05 at `8a1e3fa1`, the merge of #795): in a CI-shaped clone (`git clone --no-local`, `git checkout --detach origin/main`, `git update-ref -d refs/heads/main`) the 29 graduated files give **389 passed, 5 skipped, 1 xfailed** — and every one of the 5 skips is history-class: `loom-code/scripts/test_probes_complexity_wave_end.py:99` (the intent-confirmation commit of #794 lived on its local branch and was squash-merged; 5 of the file's 21 tests call `_confirm_intent_sha()`), `loom-code/scripts/test_probes_language_policy_branch_end.py:87` and `loom-code/scripts/test_probes_language_policy.py:381` (`_skip_if_language_policy_shipped`, 5 call sites across the two files: "its branch-scope pin applies only to its own branch"). Those tests verify nothing on CI and never will. In a depth-1 single-branch clone two more tests fail (`test_probes_coldread_abuse_coldread_branch_end.py` reading `4ab5224d`, `test_probes_language_policy_wave_end.py:184` base ref) — out of scope by the intent, CI is not depth-1.
- Data: the change touches no user data. The rehearsal writes only into a temporary directory the script creates and removes itself; the working tree is read, never written. Deleting the dead tests removes repo test files that stay recoverable from git history.
- Boundary: subprocess convention `loom-code/scripts/git_exec.py:50` (`run_git`: argv list, `capture_output`, timeout); tests sit beside a script as `test_<script>.py` with a bare `import <module>`; temp git repos in tests follow `loom-code/scripts/test_loom_checker_cli.py`. The agent shell's guard blocks `rm -rf` (`core.filesystem:rm-rf-general`) and `git branch -D`, so the script must clean up through `tempfile.TemporaryDirectory` and drop the trunk ref with `git update-ref -d` — both proven this session. A subagent's Bash call times out at 120 s; a full clone plus the 29 probe files took 17–25 s on this machine. Station text under `loom-code/skills/` may not cite `docs/` (`loom-code/scripts/check_contract_citations.py`); the checker prefix in `build/SKILL.md:27` is `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/loom_checker.py`, so the rehearsal command is written with the same prefix. The version is stamped in six places and pinned by `loom-code/scripts/test_probes_positioning_branch_end.py:439` (`test_every_place_the_version_is_stamped_agrees`); the Codex manifest follows via `python3 scripts/sync_codex_manifests.py loom-code`.

## Task DAG

Two waves, two checkpoints: wave-end:1 after the script and the deletions (code lane, adversary-first for W1-01), and the wave-2 round that doubles as branch-end. No `after-task` markers.

**W1-01 Rehearsal script: run the graduated probes in a CI-shaped clone**  after: —
- Files: `loom-code/scripts/rehearse_probes.py`, `loom-code/scripts/test_rehearse_probes.py`, `docs/loom/<cid>/evidence/probes/` (the adversary's probes, written first).
- Test: `test_probe_reading_local_main_is_red_in_rehearsal_and_green_after_origin_main_skip` — in a temporary git repo with a `main` branch and a bare `origin` remote, a probe file that runs `git show main:README` fails under `rehearse_probes.main([...])` (returns non-zero, its name appears under `FAILED`), and the same probe rewritten to try `origin/main` then `pytest.skip` returns zero. Companions: the clone has full history (an old commit's sha resolves), `origin/main` resolves, `refs/heads/main` (and `master`) is absent, HEAD is detached at the caller's HEAD sha; skipped tests are listed one per line as `SKIPPED <nodeid>: <reason>` under a `SKIPPED (<n>)` header; uncommitted changes in the working tree are absent from the clone and the report's first line says which sha was rehearsed; a repo with no `origin` remote is rehearsed with `origin` pointing at the source repo (the clone itself creates it); `--help` documents the arguments; `pytest` missing in the clone's interpreter is reported, not swallowed. Fails today because the script does not exist.
- Risk: the clone must be made with `git clone --no-local file://<repo-root>` so hardlinks and alternates are absent and the clone carries every local ref, then `git checkout --detach <HEAD sha>` and `git update-ref -d refs/heads/<trunk>` for `main` and `master` — agent-decided — because that is the shape the memory entry names and the shape measured green on main today. The pytest invocation inside the clone is `python3 -m pytest <paths…> -q -rs -p no:cacheprovider --junit-xml <tmp>/rehearsal.xml` with the script's own interpreter, `-n auto` added only when `xdist` imports, so the rehearsal does not need CI's plugin set; the FAILED and SKIPPED lists are read from the junit XML (nodeid and reason, width-independent), since `-rs` prints no nodeid and `-v` truncates the reason at the terminal width (adversary finding, 2026-09-05) — the `-q -rs` text stays as human output only. Arguments: positional test paths (default `loom-code/scripts/test_probes_*.py` expanded by the script, relative to the repo root), `--repo` (default: `git rev-parse --show-toplevel` of cwd), `--keep` to leave the clone for inspection. Exit code is pytest's; skips never change it — agent-decided — a skip is information the paragraph in W2-01 tells the reader to read, and the intent keeps the rehearsal out of the push gate. Cleanup goes through `tempfile.TemporaryDirectory`, never a shell `rm -rf` (the agent shell guard blocks it). The script uses only the standard library.

**W1-02 Delete the tests that can only run on their own branch**  after: —
- Files: `loom-code/scripts/test_probes_complexity_wave_end.py` (remove `_confirm_intent_sha` and the 5 tests that call it; keep the other 16), `loom-code/scripts/test_probes_language_policy.py` and `loom-code/scripts/test_probes_language_policy_branch_end.py` (remove `_skip_if_language_policy_shipped`, `_language_policy_intent_closed`, `_LANGUAGE_POLICY_INTENT`, every test that calls the guard, and the guard's own self-test `test_LanguagePolicyGuard_syntheticIntentTexts_decidesSkip`), `docs/loom/<cid>/evidence/probes/test_no_history_class_skips_on_main.py`.
- Test: `test_no_history_class_skips_on_main` — running `python3 -m pytest loom-code/scripts/test_probes_*.py -q -rs -p no:cacheprovider` in a CI-shaped clone of HEAD (made the way W1-01's script makes it; the probe may import `rehearse_probes` once W1-01 has landed, or build the clone itself before that) reports zero `SKIPPED` lines whose reason contains `history`, `squash-merged`, `shipped` or `its own branch`, and zero `FAILED`. Fails today: 5 such skips.
- Risk: the evidence originals under each shipped change's `docs/loom/<its-cid>/evidence/probes/` stay untouched — agent-decided — they are frozen records of that change's review, while the graduated copies are the permanent suite; a copy that can never run is dead weight there and nothing else. Unused imports and helpers left behind by the removals are cleaned in the same commit (orphan rule), nothing else in those files moves. Runs in parallel with W1-01: disjoint files, and the probe's fallback clone keeps it independent of the script's arrival.

**W2-01 Build station text: rehearse before graduating; pin; memory entry names the command**  after: W1-01, W1-02
- Files: `loom-code/skills/build/SKILL.md` (§6.5 **Probe graduation** paragraph gains two sentences, checker-prefix style: `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/rehearse_probes.py <test paths>` clones the repo the way CI checks it out — full history, `origin/main`, no local trunk branch — and runs the copies there; a red rehearsal blocks graduation until the probe reads `origin/main` first and skips when nothing resolves; read every skip it lists, since a skip that names a commit or a branch only this tree has verifies nothing on CI), `loom-code/scripts/test_build_station_text.py` (pin), `docs/loom/memory/pre-branch-end-ci-rehearsal-uses-full-history-without-a-local-main.md` (the **What matches CI** paragraph names the script as the command that makes the clone; the frontmatter `description` gains the same clause), `docs/loom/memory/README.md` (regenerated index).
- Test: `test_graduation_paragraph_names_rehearsal_and_red_blocks_graduation` — the paragraph under the **Probe graduation** bold lead names `rehearse_probes.py` with the plugin-root prefix, contains an affirmative sentence in which a red rehearsal blocks graduation, and that sentence carries no negation token (`prose_pin.has_negation` is false); the word count stays ≤ 3750; `python3 loom-code/scripts/check_contract_citations.py` exits 0. Fails today.
- Risk: the sentence must survive the prose-pin rule (affirmative, no `not`/`never`/`no`/`cannot`/`without`), so the wording is "a red rehearsal blocks graduation", never "do not graduate a red probe". The memory entry is edited in place rather than a new entry written — agent-decided — the lesson is the same, only its carrier changed from prose to a command; `python3 scripts/check_loom_memory_integrity.py --write` regenerates the index in the same commit.

**W2-02 Version bump and changelog**  after: W2-01
- Files: `loom-code/.claude-plugin/plugin.json` (1.5.1 → 1.5.2 as planned; loom-code 1.6.0 landed on main while this branch was open and the rebase moved the bump to 1.6.0 → 1.6.1), `loom-code/.codex-plugin/plugin.json` via `python3 scripts/sync_codex_manifests.py loom-code`, `loom-code/CHANGELOG.md` (`## [1.6.1] — <date> — graduated probes rehearsed in a CI-shaped clone before graduation; five dead history-bound tests removed`), root `README.md` plugin table row, `.codex/hooks/loom-checker` and `.codex/hooks/loom_checker.py` version stamps (whatever `test_every_place_the_version_is_stamped_agrees` enumerates), `docs/loom/<cid>/evidence/probes/test_changelog_carries_1_5_2.py`.
- Test: `test_changelog_carries_1_6_1` (named `_1_5_2` before the rebase) — `plugin.json` version is `1.6.1`, the Codex mirror agrees, `[1.6.1]` appears in the changelog, and `test_every_place_the_version_is_stamped_agrees` passes. Fails today.
- Risk: main may move before this lands (three other loom changes are in flight); if another bump reaches main first, the rebase moves this to the next patch number and the probe file is renamed with it — the same recovery #795 used. agent-decided — patch bump: a new script, station text, and test deletions; no contract semantics change.

**W2-memory Memory step — graduated probes and store entries**  after: W2-02
- Files: graduated copies of this change's probes under `loom-code/scripts/test_probes_rehearsal_*.py` (byte copies from `docs/loom/<cid>/evidence/probes/`, path lines adjusted, renamed on a name collision); any `docs/loom/memory/` entry the build teaches beyond W2-01's edit (an honest "nothing" is allowed); `docs/loom/memory/README.md`.
- Test: `python3 scripts/check_loom_memory_integrity.py --check` exits 0; `python3 loom-code/scripts/rehearse_probes.py` exits 0 on this branch with zero history-class skips — the script rehearsing its own graduation; the package test command passes.
- Risk: the graduated rehearsal probe clones the repo inside pytest (17–25 s) and doubles the wall clock of the probe run if it is collected twice (evidence original plus copy); acceptable — agent-decided — the evidence original is under `docs/` and outside the package-test paths. Implementer is the orchestrator itself (`fresh_context: false`), dispatch entry written before the work.

## Questions asked
1 — what — 你要的是：畢業探針本機綠、合併後才在 CI 紅這件事不要再靠人記得排練；做完後你可以：在任何分支下一個命令，它自己做出像 CI 的乾淨 clone 跑畢業探針並列出 fail 與 skip 的理由，紅就 exit 非 0；拿一支故意讀本機 main 的探針試它會紅、改成先讀 origin/main 就綠；build 站的畢業步驟寫明「紅的不得畢業」並有測試釘住；main 上永遠 skip 的探針刪掉或改寫。對嗎？（答：對）
1 — what — 這次要不要用 Codex 當第二位讀者？（答：要 — second vendor codex for this change; the review station records it in `second_vendor` at the first checkpoint）

## Risks
1. **The rehearsal is advisory, by the intent's own constraint.** Nothing blocks a push when the memory step skips it; the station sentence and its pin are the whole enforcement. If the next two changes still reach CI red, that is the evidence for moving it into the checker — a separate intent.
2. **Cloning inside a test.** W1-02's probe and W2-memory's graduated copy make a full clone of this repository (132 MB of git history today) on every package-test run — measure the wall clock in W1-01 and, if the clone exceeds 30 s, the graduated copy carries a `pytest.mark.slow`-free alternative: rehearse only when `REHEARSE_PROBES=1` is set and skip otherwise, with the skip reason naming the variable. agent-decided in favour of always running unless the measurement forces the switch.
3. **Deleting tests looks like weakening the suite.** The 10 removed tests have skipped on every CI run since their changes merged; their evidence originals remain under `docs/loom/`, and W1-02's commit body lists each removed test name with its skip reason so the reviewer can check none of them could ever run on main.
4. **Version collision with in-flight changes** (express-lane, slim-plan-doc, charter): the same rebase-and-renumber path #795 walked; rebase before branch-end, re-run the probes at the reviewed tree.
5. One-way doors: none after decision point ①. No existing data is touched; every deletion is of repo test files recoverable from git.
