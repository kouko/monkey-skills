# 審查看得見複雜度與流程成本 — 我試了什麼、發生了什麼

2026-09-05 在一份乾淨副本上試的，版本是 `40817ff6`（這是本次分支上「第一波五個任務」做完後的狀態；「版本號調高」與「補記憶條目」這兩個任務還沒做，下面不會把它們當缺失來提）。

這次改的東西沒有畫面可以點——它改的是「審查這個角色」讀的規則文字，跟審查結束後留下的紀錄格式。所以我用的方法是：自己扮演審查者的角色，照規則文字讀一遍、套一個我自己編的假案例，看規則會不會逼出我要的結論；再把這個專案自己規定要跑的檢查指令，原封不動跑一遍。

## 你要的東西，一條一條試

### 1. 「審查文件／規則文字」這道審查，現在也要問「這段有必要嗎、能不能刪」

- **怎麼試的**：我沒有另外找人來測，而是自己編了一段「新規則」——一段從沒出現過的話：「派工逾時就悄悄重試三次」，這段話在真正的規則庫裡找不到任何一次失敗紀錄或討論在背後撐它。接著我照著審查規則文字本身，推演審查者看到這段話該給什麼結論。
- **發生了什麼**：規則文字寫得很清楚——文件與規則類的審查現在多了一個項目：新加的一段話、一個機制、一條備援路徑，如果在改動裡找不到「它取代了什麼」或「它防的是哪次已經發生過的失敗」，就要被記一筆問題，而且這筆問題必須附上「更小的寫法」才算數，不能空講「這樣不好」。我編的那段「悄悄重試三次」正好踩中這一條——它是新機制，卻沒有任何佐證,規則會逼審查者把它列成問題並想一個更小的替代寫法。這條規則本身也有自動化的釘子測試在檢查它「有沒有說到重點」，我實際執行過，全部通過。
- **證據**：我自己編的測試檔案在我的暫存區（不隨這次改動進倉庫），推演依據是規則文件裡明講「新加的一段話……在改動裡找不到取代了什麼或防了哪次失敗,就是一個問題,而且這問題要附上更小的寫法」這句話；另外跑了規則庫裡釘住這條規則的自動測試（`test_lenses_deletion_first.py`、`test_review_station_text.py`，合計 46 項）全部綠燈。
- **我需要用猜的部分**：規則文字要求引用「已發生的失敗紀錄」,我編的假案例故意什麼佐證都不給,這點是我自己決定的,不是規則含糊。
- **判定**：符合。

### 2. 同一份文件連續兩次調高字數上限,要被當成一個警訊、逼審查者列出可以刪的東西

- **怎麼試的**：我編了一個假的紀錄,說「同一份文件在兩次改動裡都調高了字數上限」,照規則文字推演審查者該怎麼反應。
- **發生了什麼**：規則文字寫著——同一份文件的字數上限連續兩次調高,是一種「設計上的壞味道」,審查者在這個項目上一定要列出至少一個可以刪的候選,即使最後這個候選被否決也沒關係,但不能不列。另外我也在「審查怎麼收尾」那段規則裡找到:凡是有一個 commit 調高了字數上限,審查者要在這一輪的筆記裡寫一行理由,說明為什麼需要多的空間——這條也已經被自動測試釘住,我實際跑過,綠燈。
- **證據**：規則文件裡「同一份文件連續兩次調高上限……一定要列出可以刪的候選」這句話,以及「調高上限的 commit 要在這一輪筆記裡寫一行理由」這句話;釘住這兩句話的自動測試（同一組 46 項)全部通過。
- **判定**：符合。

### 3. 每個改動都要留下「這次流程花了多少力氣」的紀錄,而且要出現在對外的說明裡

- **怎麼試的**：我打開這個專案用來記錄「花多少力氣」的空白範本,看它有沒有輪數、派工次數、字數上限異動、從動工到送出這幾項；接著打開這次改動自己留下的紀錄,看同樣的欄位有沒有填;最後找「準備送出改動時要寫的說明」範本,看裡面有沒有一段講流程花費。
- **發生了什麼**：空白範本裡有這一組欄位（輪數、派工數、字數上限異動、耗時),而且這次改動自己的紀錄裡也真的填了(輪數 0、派工 12 次、上限異動 0 次、耗時尚未填,因為分支還沒結束)。對外說明的範本裡,「流程花費」這一段確實排在「收尾紀錄」之後、「給下一個人的話」之前,順序符合設計。
- **證據**：範本檔案（`loom-code/contract/templates/review.json`)確實有 `cost` 區塊;這次改動自己的紀錄（`docs/loom/2026-09-05-review-sees-complexity-and-process-cost/review.json`)的 `cost` 欄位已填 `{"rounds": 0, "dispatches": 12, "cap_changes": [], "hours_plan_to_pr": null}`;對外說明範本裡 `## Process cost` 確實夾在 `## Closing log` 與 `## Memory` 之間。
- **判定**：符合。

### 4. 同一次審查如果卡到第三輪,一定要留一行「重新想過設計」的結論

- **怎麼試的**：找出規則文字裡「第三輪」那一段,看它有沒有要求留這一行、以及三選一是哪三個選項;再確認這次改動目前跑到第幾輪。
- **發生了什麼**：規則文字寫得明確——同一張查核表如果進到第三輪,這一輪的筆記一定要有一行「重新想過設計」的結論,三選一是「繼續修」「換設計」「當成小問題接受」,沒有這一行,這一輪就不算完成。這條規則已經被自動測試釘住,我跑過是綠燈。但這次改動自己目前只在第一輪,還沒有真的卡到第三輪過,所以「這一行真的出現在這次改動的紀錄裡」這件事我沒辦法驗——規則存在、測試通過,但沒有實例可看。
- **證據**：規則文件（`loom-code/skills/review/references/fix-rounds.md`)「第三輪」那一段的原句;釘住它的自動測試（`test_fix_rounds_third_round_carries_design_relook_line`)綠燈;這次改動自己紀錄裡的輪數目前是 0。
- **判定**：規則本身符合;「這次改動真的用到它」這件事還沒發生,不算失敗——沒到第三輪。

### 5. 補上一個已知的測試漏洞,且送出前要檢查的項目要跟自動化檢查對得上

- **怎麼試的**：我把這個專案文件裡寫死的「應該要跑的測試指令」原封不動貼進終端機跑了一次;接著把「送出改動前要手動檢查一遍」的那份清單,一條一條照著跑;最後數了一下「審查規則」總共有幾條。
- **發生了什麼**：
  - 測試指令跑起來是兩段:第一段（本體程式碼)1519 個測試通過、3 個跳過、1 個預期失敗,58.83 秒;第二段（另一個子專案的測試)182 個通過、1 個跳過,1.83 秒。兩段都沒有紅燈,漏掉的那個子專案確實已經被補進指令裡了。
  - 送出前清單的九個項目裡,八個乾乾淨淨通過。**第五個項目(比對這次改動跟「基準版本」之間,系統裡的機制總數有沒有兜起來)出現一個紅字**:這次改動新增的「流程花費」欄位,沒有登記進「機制清單」那份總帳裡——意思是這個新欄位存在,但沒有被計入「系統總共有幾種機制」的帳本,帳本跟實際對不上。這不是測試沒過,是這道檢查本身回報了一個落差。
  - 規則總數確實是 27 條,跟預期一致。
- **證據**：兩段測試指令的完整輸出（1519 passed, 3 skipped, 1 xfailed / 182 passed, 1 skipped);清單第五項指令 `python3 loom-code/scripts/check_mechanisms.py --baseline origin/main` 的輸出最後一行 `RED [R1] artifact:review.cost: class contract: recomputed but not registered in mechanisms.yaml`,離開代碼是 1（不是 0);其餘八項指令離開代碼都是 0;規則清單指令輸出行數 27。
- **判定**：部分符合——測試補漏跟規則數都對,但送出前清單自己的第五項在這次改動上是紅的,這件事應該在正式送出前處理掉。

### 6. 同一波、同一輪的派工紀錄要合成一次 commit,不要一筆一筆分開

- **怎麼試的**：找出「同一波的派工紀錄要一次寫、一次送出」跟「同一輪的審查派工紀錄也要一次寫、一次送出」這兩句規則文字,以及「探針作者自己修自己探針的錯,還沒被人看過就直接補進原本那次送出裡」這句規則文字;接著數這個分支上,標記「派工紀錄」的送出總共有幾筆。
- **發生了什麼**：三句規則都找到了,而且各自都有自動測試釘著,我實際跑過都是綠燈。這個分支上標記「派工紀錄」的送出一共 3 筆:第一筆一次送出了第一波五個實作任務的紀錄;第二筆一次送出了「修第一波第五個任務的紀錄」跟「第二波兩個任務的紀錄」;第三筆一次送出了這次審查（一個抓漏、一個像我這樣的盲跑、兩個讀者)四個角色的紀錄。跟「派工包」給我的算法(第一波算 1、這次審查算 1、加起來上限是 2)相比,實際是 3 筆,多了 1 筆——多出來的那 1 筆,是因為「修第一波第五個任務」跟「還沒做完的第二波」被合寫進同一筆裡。這件事本身沒有違反「同一波一次寫」的規則(它確實沒有變成一波五筆),只是這次改動還沒走完全程,所以拿「已完成的波數＋已完成的輪數」去對總筆數,現在還兜不起來,要等分支結束才看得出最終筆數對不對。
- **證據**：規則原句在 `loom-code/skills/build/SKILL.md`（第一波紀錄的那句)、`loom-code/skills/review/SKILL.md`（第 155 行,審查角色的那句)、`loom-code/agents/adversary.md`（第 65 行,探針自己修自己的那句);釘著這三句的自動測試都跑過,綠燈;`git log db7d44f9..HEAD --format=%s | grep -c 'chore(loom): dispatch'` 結果是 3。
- **判定**：規則本身符合、機制確實在運作(沒有退回「一筆一個任務」的老毛病);但拿這次改動自己的進度去對公式,現在的數字（3)比預期上限（2)多 1,原因是「還沒做完的第二波」提早被合寫進紀錄——這個落差建議在分支收尾時再核對一次。

## 對你既有的資料做了什麼

沒有動到——這次改動只碰了它自己創造出來的文件（規則文字、範本、這次改動自己的紀錄檔)。我確認過:除了這次改動自己的紀錄檔是新增的之外,沒有任何一份舊的、其他改動留下的紀錄檔被回頭補寫或修改過。

## 我幫你決定的地方

- **調高規則文字裡某個字數上限之前,先想辦法在檔案裡騰出空間** —— 有兩個任務都遇到「這份文件字數快滿了」,選的做法是先在檔案裡找地方壓縮或搬去別的參考檔案,不夠才調高上限。這是為了呼應這次改動自己要求的「調高上限要有代價」,選了比較保守的一邊。如果之後想放寬,代價是要回頭把「先縮減再調高」這句判斷改掉。
- **一個任務卡住時,選了寫一個十幾行的小工具腳本,而不是動測試指令本身** —— 遇到「兩個子專案的測試不能塞進同一次執行」的技術限制,選擇補一個小工具腳本讓它們各自跑一次,而不是放寬測試范圍或跳過某個子專案。換句話說是「多寫一點膠水程式碼」換「不犧牲涵蓋範圍」。如果之後想改回一次執行到底,要先確認兩個子專案的測試設定能不能合併。
- **版本號要不要調高、調多少** —— 計畫裡先寫好「這次算小改版」,因為只是兩個審查項目多一個判斷、紀錄格式多一個欄位,沒有任何規則的行為改變。這件事還沒真的執行(屬於還沒做的「第二波」),之後如果覺得改動比預期大,可以調整版本等級。
- **目前為止沒有出現「審查者提了問題、又被人否決」的紀錄** —— 這次改動的審查紀錄裡,審查者實際打的分數跟意見欄位都還是空白模板,代表真正的審查意見還沒寫進來(我這份盲跑報告本身就是審查流程的一部分,正在進行中)。所以沒有「重要或更嚴重的問題被否決」需要在這裡列給你看。

## 我不確定你想要的東西

- 送出前要跑的檢查清單裡,有一項（比對機制總帳)在這次改動上目前是紅的:新加的「流程花費」欄位還沒登記進機制總帳。這算不算要在送出前修好,還是可以留到下一次改動再補,我沒有把握替你決定。
- 這次改動上,「派工紀錄要一次寫一次送出」算出來的送出筆數(3 筆)比預期公式(上限 2 筆)多 1 筆,原因是「還沒做完的第二波」被提前合寫進紀錄。這是不是這條規則本身該把「進行中的波」也算進公式,還是純粹因為這次改動還沒走完全程才會兜不起來,需要你或設計這條規則的人決定。
- 「同一次審查卡到第三輪要留一行結論」這條規則,這次改動本身還沒有機會被驗證過(因為只跑到第一輪),需要之後真的卡到第三輪才看得出來規則會不會被確實遵守。
