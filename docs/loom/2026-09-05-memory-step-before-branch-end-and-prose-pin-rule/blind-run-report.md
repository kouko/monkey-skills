# 記憶步驟搬到收尾審查之前；散文釘規則寫進契約 — 我試了什麼、結果如何

測試日期 2026-09-05，在乾淨複製的專案上測，測的版本是 `a478f7d2`（分支
`memory-step-before-branch-end-and-prose-pin-rule` 當時的最新一筆）。之後
對抗測試那關抓到「記憶任務的派工紀錄排在工作紀錄之後」的順序問題，未推送的
六筆紀錄被重新排序修正，導致這一筆之後每一筆版本代號都變了；現在這個版本代
號叫 `46ad0b7a`。內容樹與我測的完全一樣，只是提交順序不同，所以以下結果仍然
成立。

## 你要的東西，一條一條試

### 1. 這次改動走完之後，收尾那段紀錄裡不該再混進別的東西
- **怎麼試的**：把「上一次審查通過的版本」到「現在」之間的紀錄整段印出來看。
- **結果**：這段裡有審查通過的那一筆、後續兩個任務各自的紀錄、一筆版本號更新、
  「把探測補進正式測試＋寫進記憶庫」的那一筆（這正是本項要求的「記憶步驟先做」）、
  兩筆派工紀錄，以及一筆命名衝突的小修正——七筆，且「記憶步驟」那一筆確實排在
  這輪收尾審查的派工紀錄之前，不是之後。目前分支還沒收尾（沒有走到合併／關閉
  這一步），所以還看不到「只剩兩筆」的最終樣子；但排序已經對了，等真正收尾、
  只留下這輪審查與關閉兩筆時，才算完全達成。
- **證據**：`git log c2a5a82e..HEAD` 的七行紀錄（見上）。
- **結論**：部分做到——排序已驗證正確，最終「只剩兩筆」的樣子要等實際收尾才能看到。

### 2. 收尾與建置流程的文字裡，各有一句講清楚「記憶步驟」該在哪一步做
- **怎麼試的**：跑這兩段流程文字自己的測試。
- **結果**：20 項全部通過。
- **證據**：測試指令 `test_build_station_text.py` + `test_ship_station_text.py`，20 passed。
- **結論**：works。

### 3. 兩份工程契約文件（給「找漏洞的人」跟「寫程式的人」看的）各加一句「怎麼寫出可靠的釘文字測試」的規則
- **怎麼試的**：直接讀那兩份文件裡新加的句子，並跑它們的釘測試。
- **結果**：兩份文件都找到那一句——要求測試要求「肯定語氣」「同一句不能有否定詞」
  「附一個正例、一個反例的自我測試」。73 項相關測試全部通過。
- **證據**：`references/engineering-baseline.md` 第 149 行、`agents/adversary.md`
  第 56 行；測試指令 73 passed。
- **結論**：works。

### 4. 建置流程裡「檢查有沒有漏簽名」的兩個地方要能直接複製貼上執行，而且真的抓得到漏簽名
- **怎麼試的**：把流程文件裡第 4 節、第 5 節的檢查指令原封不動複製到一個全新的
  臨時倉庫，故意做一筆「有簽名的一般變更」、一筆「沒簽名的一般變更」、一筆
  「純文件變更（本來就不用簽名）」，跑一次看它抓到誰。
- **結果**：沒簽名的那一筆被準確點名；有簽名的那一筆沒被誤報；純文件變更那一筆
  正確地被跳過不檢查。
- **證據**：臨時倉庫測試輸出：`no Task trailer: <該筆沒簽名的版本號>`，其餘兩筆
  無輸出（代表通過）。
- **結論**：works。

### 5. 規則檢查工具列出的規則數量不變（27 條）
- **怎麼試的**：跑規則清單指令數行數。
- **結果**：27。
- **證據**：`--list-rules | wc -l` → 27。
- **結論**：works。

## 對你既有的資料做了什麼

Nothing — 這次改動只動了流程文字（收尾、建置兩段）與兩份工程契約文件，加上
對應的釘測試；沒有讀寫任何你原本就有的資料或設定。

## 我幫你決定的事

- **文件裡新增一句的措辭怎麼寫**——依 W1-02 任務自己選字句表達，沒有另外問你。
  之後想換說法，只要改那一句並讓對應的釘測試跟著改。
- **釘句子的位置**（釘住哪一句、抓哪個定位點）在修正輪裡幾次重新對準——因為第一輪
  審查抓到「釘得到否定句也會過」的漏洞，改了兩三次才穩，屬於執行細節上的來回，
  不是規則本身變過。
- **加開一個原計畫沒有的任務（W1-04）**——執行過程中臨時追加，用來把某個檢查修
  得更嚴謹。
- **「記憶步驟」排在這輪收尾審查之前完成**——這正是本項要達成的目標，不是意外；
  在這份報告開頭第 1 條已經證實排序正確。
- **接受了審查中一個「無傷大雅」等級的小意見不再修**——三輪審查下來，最後一輪
  兩位讀者都給「通過，但有小提醒」，提醒的內容是「某個防呆的邊界條件還能再挑
  剔一點」，沒人認為需要擋下這次改動，我選擇不再多花一輪去修。**沒有任何「重要」
  或更嚴重的意見被跳過不修**——三輪審查全程都沒有被駁回或忽略的重大意見。
- **版本號加一個小版號（1.3.1）**——照專案慣例，內容變了就得標版本，屬於機械性
  的例行動作。
- **把一個測試函式改名**——因為新舊兩個測試撞名，保留邏輯只改名字避免衝突。
- **這次改動自己的記憶任務，派工紀錄一度排在工作紀錄之後**——正好違反本項要
  建立的規矩（記憶步驟該先做、紀錄該照順序來），被對抗測試那一關抓到；修法是
  在還沒推送出去、沒有人看過之前，把這幾筆未推送的紀錄重新排好順序，不是事後
  補救，也沒有留下錯誤順序被任何讀者看到。

## 你可能還想確認的事

Nothing —— intent 文件裡列的「待確認問題」是「none」，我測試下來也沒有發現需要
你額外拍板的新問題。

---

補充：一致性檢查（給下一位讀這份報告的人核對用，識別碼只放在括號裡）

| 檢查對象 | 是否照「英文寫死」的規矩 |
|---|---|
| 計畫文件（plan） | 是——內容是英文 |
| 規格文件（spec） | 不適用——這次改動不需要規格文件 |
| 審查紀錄的意見文字 | 是——每條意見都用「issue / nitpick」這種固定格式開頭（review.json） |
| 我這次收集的證據 | 是——本次臨時倉庫測試的輸出是英文 |
| 測試檔案裡的說明文字 | 是——都是英文 |
| 測試函式的命名 | 是——都照「對象_情境_預期結果」三段式命名（如 `test_full_lane_adversary_first_covers_code_and_gate`） |
| 提交紀錄（commit）訊息 | 是——都是英文 |

全部跑過的自動檢查（供追查用）：`test_build_station_text.py` /
`test_ship_station_text.py`（20 passed）、`test_prose_pin_rule_text.py` +
`test_reviewer_agent_single_contract.py` + `test_engineering_baseline_reference.py`
（73 passed）、整個工程腳本與掛勾測試（1402 passed, 3 skipped, 1 xfailed）、
`loom-design` 測試（182 passed, 1 skipped）、記憶庫完整性檢查（OK）、版本號
檢查（OK）、已畢業的「語言政策」探測在這個更晚的分支上全部正確跳過（3 個
跳過——代表那個舊改動已經關閉收工，不需要再對這個分支重跑，是預期行為，不是
漏測）。
