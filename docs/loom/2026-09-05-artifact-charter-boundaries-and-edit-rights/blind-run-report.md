# loom 文件憲章：邊界與定稿後修改權 — 我試了什麼、發生了什麼

於 2026-09-05 在一份乾淨副本上試跑，版本 36a71b29。

這次是 wave 0 的檢查點：只有「憲章表」這一塊落地，後面幾波（欄位字數帽、定稿後修改權的強制、reviewer 收窄、A/B 縮寫證據）都還沒做。以下逐條照你的驗收條件試,還沒做到的我會老實說「還沒做」。

## 你要求的東西，一條一條看

### 1. 我可以打開一張表，每種 loom 文件一列，四欄都不空
- **我怎麼試的**：在乾淨副本裡跑了產生這張表的指令。
- **發生了什麼**：跳出一張表，九種文件（intent、spec、plan、review、blind-run 報告、memory、預設值清單、派工紀錄）各一列，每列都寫了「該寫什麼」「不該寫什麼、該去哪」「在哪一站定稿」「定稿後准許改什麼」，沒有一格是空的；「不該寫什麼」那欄的每一條都指名該搬去哪一份文件。另外有一條檢查規則會擋下任何欄位空白的列。
- **證據**：指令 `python3 loom-code/scripts/loom_checker.py charter`，輸出九列表格；檢查規則清單裡有一條 `contract.charter-complete` 對應這張表。
- **判定**：做到了。

### 2. 拿一份 3,945 字的舊 plan 給檢查工具，它能指出「事後補寫的日誌」和「已完工後加的任務」不該留在 plan 裡，並說該搬去哪
- **我怎麼試的**：找這個能判斷「定稿後只准許哪些改動」的檢查規則。
- **發生了什麼**：找不到。這一段機制（判斷 plan 定稿後的改動是否合法）規畫在後面的波次，目前的程式裡沒有對應的規則名稱，也沒有相關程式碼。
- **證據**：檢查規則清單裡沒有任何「plan 定稿後編輯」相關的規則。
- **判定**：還沒做——這是下一個檢查點的工作，這次沒有東西可以試。

### 3. 三欄都在字數帽內的 plan 通過；Test 欄寫成情境清單後被擋
- **我怎麼試的**：找「plan 欄位字數上限」的檢查規則。
- **發生了什麼**：找不到，plan 模板裡也沒看到字數帽的說明。
- **證據**：檢查規則清單裡沒有欄位字數帽的規則；plan 模板檔內沒有相關文字。
- **判定**：還沒做。

### 4. 拿一份舊的長 plan 依憲章縮寫，兩次冷派工比較「需要回頭問清楚」的次數
- **我怎麼試的**：找縮寫後與原版的比較證據檔案。
- **發生了什麼**：這次檢查點的證據資料夾裡只有一份對抗測試的程式檔，沒有「縮寫版 plan」「原版 plan」「兩次派工的狀態回報」這三份比較用的檔案。
- **證據**：證據資料夾內容只有 `probes/test_abuse_charter.py` 一份檔案。
- **判定**：還沒做——這一條驗收要等這幾份比較檔案生出來才能試。

### 5. 冷派 reviewer 判斷「plan 少列一個測試情境」不算缺漏；「plan 內重複寫規格內容」算不一致
- **我怎麼試的**：查看目前對「缺漏」的定義文字有沒有把 plan 的判準收窄。
- **發生了什麼**：定義文字還是舊的——「缺漏」仍然是「文字裡少了一個讀者需要的步驟或項目」這種寬鬆講法，沒有看到針對 plan 收窄成「implementer 拿到單子無法開工才算缺漏」的新版本。
- **證據**：審查判準說明檔裡的「缺漏」定義段落文字未變。
- **判定**：還沒做。

### 6. review.json 第二輪只有允許的節點能長，允許替換的節點被追加第二份時會被擋
- **我怎麼試的**：找「review.json 累積規則」的檢查規則。
- **發生了什麼**：找不到相關規則；review.json 目前只有一輪的派工紀錄，沒有第二輪可以測試「只能新增/只能替換」的行為。
- **證據**：檢查規則清單裡沒有 review 累積相關的規則名稱。
- **判定**：還沒做。

### 7. 整包測試通過；write-plan 的說明文件仍在 4,500 字帽內
- **我怎麼試的**：跑整包自動測試；用程式數 write-plan 說明文件的字數。
- **發生了什麼**：整包測試 1553 個通過、5 個跳過、1 個預期失敗（沒有真的失敗）；說明文件字數 4,308 字，在 4,500 字帽以內。
- **證據**：指令 `python3 -m pytest loom-code/scripts/ scripts/ .claude/hooks/ -q -n auto`，結果 `1553 passed, 5 skipped, 1 xfailed in 49.43s`；字數指令輸出 `4308 OK`。
- **判定**：做到了。

## 對你既有的資料做了什麼

沒有——這次改動只新增檢查規則與一張表，沒有讀寫你原本就有的任何檔案內容。

## 我幫你決定的事

沒有——這次檢查點沒有被駁回的重要或以上等級的發現（審查紀錄裡目前的發現清單是空的），驗收 1 與 7 之外的判斷都照驗收條件本身的字面走，沒有另外幫你做選擇。

## 你可能沒料到的事

- 驗收 2–6 這五條要等的機制（欄位字數帽、定稿後改動檢查、review.json 累積檢查、reviewer 收窄判準、縮寫前後比較證據）目前都還不存在，不是我漏試——這是這次檢查點本來就只做第一波的緣故。之後每一波做完都應該重新盲跑一次這幾條。
