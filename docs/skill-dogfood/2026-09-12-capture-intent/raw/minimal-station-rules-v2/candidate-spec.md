## Requirements

REQ-1 — Stop subsequent project activity emails  
  WHEN a user mutes one project, the system shall stop subsequent activity emails for that project → Acceptance #1

REQ-2 — In-app notifications unchanged  
  The system shall leave in-app notifications unchanged → Acceptance #2

REQ-3 — Global settings unchanged  
  The system shall leave global settings unchanged → Acceptance #3

## Out of scope

- Designing, implementing, or verifying unmute behaviour.

## UI flows

N/A — the confirmed intent does not identify or authorize a user interface, control, or surface.

After you mute one project, subsequent activity emails for that project stop. In-app notifications remain unchanged, and global settings remain unchanged. Is that right?
