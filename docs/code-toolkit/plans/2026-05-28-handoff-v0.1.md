# Plan: handoff v0.1

**Source brief**: `docs/code-toolkit/specs/2026-05-28-handoff-v0.1-brief.md`
**Total tasks**: 4 (≤5 ✓)
**Execution order**: sequential (T1 → T2 → T3 → T4)
**Plan-document-reviewer verdict**: PASS (2026-05-28, 11/11 applicable checks; 3 informational notes — see end of file)

## Task 1 — Author `references/handoff-schema.md` bundle (self-contained, no recap-bundle dependency)

- **Description**: Create the bundled reference shipping the **own** HANDOFF L2 schema per Anthropic skill-independence convention. Contents: 10-block schema (Frontmatter / Situation / Background / All user messages verbatim / Recent decisions + WHY / Pending / Critical files / Do Not Touch / Verification commands / Confidence flags) + 5 共通核心原則 (4 shared with recap but with L2-tailored definitions: structured-schema / quote-not-paraphrase / all-user-messages / synthesis-check + 5th L2-specific: **technical-precision** mirror-flip of recap's `plain-language`) + good-example block + bad-example block. NO file edits outside `dev-workflow/skills/handoff/references/`.
- **Module**: `dev-workflow/skills/handoff/references/handoff-schema.md` (new file)
- **Files touched**: `dev-workflow/skills/handoff/references/handoff-schema.md`, `dev-workflow/skills/handoff/scripts/test_handoff_schema.py`
- **Context paths**:
  - `/Users/kouko/kouko-obsidian-vault/research/2026-05-25 AI Agent Cross-session HANDOFF 框架研究——從 softaworks 10 段到 Don't Sleep On AI 8 段.md` (v1.1; primary design source — Schema v1.1 §4 + Confidence Flags §13 + Do Not Touch §10 + Verification §12)
  - `/Users/kouko/kouko-obsidian-vault/research/2026-05-25 AI Agent 對話接續綜論——Recap 與 HANDOFF 共通核心與分歧軸線.md` (4 共通核心原則 source + L2-vs-L3 audience distinction)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/docs/code-toolkit/specs/2026-05-28-handoff-v0.1-brief.md` (success criteria, §Smallest End State 10-block table + Decision §mirror-principle)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/recap/references/seven-block-schema.md` (READ-ONLY structural reference — copy the SHAPE of bundle layout, NOT the content; per Anthropic skill independence, do not import or link)
- **Acceptance**:
  - **RED**: `pytest dev-workflow/skills/handoff/scripts/test_handoff_schema.py::test_all_ten_blocks_and_five_principles_present -v` fails initially (file does not exist)
  - **GREEN**: file exists; all 10 block headings present (case-insensitive); all 5 principle anchors present (`structured-schema` / `quote-not-paraphrase` / `all-user-messages` / `synthesis-check` / **`technical-precision`**); one good-example block + one bad-example block; bad-example demonstrates BOTH paraphrase-creep AND plain-language-creep (L3-creep into L2 — agent over-simplifying for AI reader who needs precision); no Markdown links pointing to `dev-workflow/skills/recap/` (skill independence guarantee).
- **Dependencies**: none
- **Independent**: false  # lone wave-1 task per [[independent-true-lone-wave-leader]]
- **Brief item covered**: "Author own bundle `dev-workflow/skills/handoff/references/handoff-schema.md` — self-contained 10-block L2 schema + 4 shared 共通核心原則 (L2-tailored definitions) + 5th L2-specific `technical-precision` + good/bad-example pair. Zero file edits outside the handoff skill folder." — Handoff to writing-plans §1.

## Task 2 — Author `SKILL.md` (prepare-mode + resume-mode + multilingual triggers + double disambiguation)

- **Description**: Write `dev-workflow/skills/handoff/SKILL.md` with valid YAML frontmatter (`name: handoff`, `version: 0.1.0`, description with multilingual triggers for BOTH prepare-mode and resume-mode + disambiguation from sister `recap` skill (L3 in-session) AND from OpenAI-style agent-to-agent handoff (different problem)). Body covers: (a) prepare-mode behavior — HARD-GATE: agent MUST use Bash to gather `git rev-parse HEAD` + `git status --short` + `git log --oneline -5` + `claude --version`, then use Write to author `.claude/handoffs/HANDOFF-YYYY-MM-DD-HHMMSS-<slug>.md` with all 10 blocks per `references/handoff-schema.md`; (b) resume-mode behavior — HARD-GATE: agent MUST use Bash to find latest `ls -t .claude/handoffs/ | head -1`, Read the file, run EACH Verification Command via Bash and report verbatim output, REFUSE TO CONTINUE if any command output mismatches expected (state drifted); (c) what NOT to do (no replacing built-in /recap; no agent-to-agent delegation; no compacting context). Body ≤6k tokens per CLAUDE.md.
- **Module**: `dev-workflow/skills/handoff/SKILL.md` (new file)
- **Files touched**: `dev-workflow/skills/handoff/SKILL.md`, `dev-workflow/skills/handoff/scripts/test_skill_md.py`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/handoff/references/handoff-schema.md` (produced by Task 1)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/recap/SKILL.md` (structure precedent — copy the SHAPE not the content; per Anthropic skill independence, no cross-skill imports)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/brief-before-asking/SKILL.md` (frontmatter precedent)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/CLAUDE.md` (Skill Structure rules: flat subfolder, ~6k token cap, relative paths)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/docs/code-toolkit/specs/2026-05-28-handoff-v0.1-brief.md` (Decision + Smallest End State + Out of Scope)
- **Acceptance**:
  - **RED**: `pytest dev-workflow/skills/handoff/scripts/test_skill_md.py::test_frontmatter_and_routing -v` fails initially (file does not exist)
  - **GREEN**: all assertions pass: (a) YAML frontmatter has `name: handoff`, `version: 0.1.0`, non-empty `description`; (b) description contains `handoff` + multilingual triggers (zh-TW: `wrap up` 中文等價如「收尾」/「明天繼續」; ja: `今日はここまで` or `引き継ぎ`; en: `wrap up` or `save state` or `pick up where we left off`); (c) description disambiguates from sister `recap` (contains both `cross-session` AND `in-session` markers OR equivalent contrast) AND disambiguates from OpenAI-style agent-handoff (contains `cross-session` AND something like `not agent-to-agent` or `not delegation`); (d) body relative-path references `references/handoff-schema.md`; (e) body token count ≤6500 (chars/4 proxy); (f) body cites all 5 共通核心原則 by name including `technical-precision`; (g) body contains BOTH `prepare` mode AND `resume` mode language; (h) body contains the literal path `.claude/handoffs/` (HARD-GATE for path consistency). Hook `.claude/hooks/validate-skill-folder-structure.sh` does not block.
- **Dependencies**: Task 1 completes first
- **Independent**: false  # depends on Task 1's bundle
- **Brief item covered**: "HANDOFF SKILL.md — prepare-mode + resume-mode prompt instructions (HARD-GATE: agent MUST run every Verification Command via Bash and report verbatim output before continuing on resume; agent MUST author all 10 blocks per schema on prepare); multilingual triggers; reuse bundle SSOT; disambiguate from `/recap` (recap is L3 in-session; this is L2 cross-session) + OpenAI-style agent-handoff" — Handoff to writing-plans §2.

## Task 3 — Tri-language READMEs (EN / JA / zh-TW) covering prepare + resume modes

- **Description**: Write three READMEs — `README.md` (EN), `README.ja.md`, `README.zh-TW.md` — each with the same 5-section skeleton: (1) overview / what the skill does (cross-session continuation); (2) when to use vs sister `recap` (in-session); (3) example invocation phrases for BOTH prepare-mode AND resume-mode in that language; (4) `.gitignore` recommendation (user-policy guidance — opt-in `.claude/handoffs/` if you don't want handoff files committed); (5) what's deferred to v0.2 (forced trigger + Interruption Snapshot + per-tool variants). EN is source-of-truth for concepts; JA + zh-TW are native-register translations not machine translations.
- **Module**: `dev-workflow/skills/handoff/` (the README triplet is one logical content unit)
- **Files touched**: `dev-workflow/skills/handoff/README.md`, `dev-workflow/skills/handoff/README.ja.md`, `dev-workflow/skills/handoff/README.zh-TW.md`, `dev-workflow/skills/handoff/scripts/test_readmes.py`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/handoff/SKILL.md` (produced by Task 2)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/recap/README.md` (tri-lang README precedent — EN)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/recap/README.ja.md` (precedent — JA)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/skills/recap/README.zh-TW.md` (precedent — zh-TW)
- **Acceptance**:
  - **RED**: `pytest dev-workflow/skills/handoff/scripts/test_readmes.py::test_tri_lang_readmes_consistent -v` fails initially (files do not exist)
  - **GREEN**: all 3 README files exist; each contains the 5 section concepts (overview / when-to-use-vs-recap / example-invocation-prepare-AND-resume / gitignore-recommendation / v0.2-deferred); EN README's prepare-mode example phrase matches ≥1 prepare trigger in SKILL.md description (consistency oracle); JA + zh-TW READMEs each list ≥2 native trigger phrases for prepare-mode AND ≥2 for resume-mode that ALSO appear in SKILL.md description.
- **Dependencies**: Task 2 completes first
- **Independent**: false  # depends on Task 2's SKILL.md trigger declarations
- **Brief item covered**: "Tri-language READMEs (EN / JA / zh-TW) — same pattern as recap; cover prepare-mode + resume-mode usage with concrete example invocation phrases per language" — Handoff to writing-plans §3.

## Task 4 — Bump `dev-workflow` plugin version + register `handoff` keyword

- **Description**: Bump `dev-workflow/.claude-plugin/plugin.json` `version` from current main HEAD's value to next minor (currently `2.10.0`; bump to `2.11.0`; implementer reads current value at run-time and bumps minor to absorb any concurrent merges). Add `handoff` to the `keywords` array. Skill auto-discovery means no explicit registration in plugin.json; the keyword is for discoverability only.
- **Module**: `dev-workflow/.claude-plugin/plugin.json`
- **Files touched**: `dev-workflow/.claude-plugin/plugin.json`, `dev-workflow/.claude-plugin/test_plugin_manifest.py` (extending the existing test file from recap v0.1 with one more assertion for the `handoff` keyword)
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/.claude-plugin/plugin.json` (current manifest — worktree base shows `2.10.0`; main may have advanced; bump from whatever the file actually shows at implementation time)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-handoff-v0.1/dev-workflow/.claude-plugin/test_plugin_manifest.py` (existing test from recap v0.1; add assertion for `handoff` keyword without breaking existing `recap` assertion)
- **Acceptance**:
  - **RED**: `pytest dev-workflow/.claude-plugin/test_plugin_manifest.py::test_version_and_keywords -v` fails initially (no `handoff` keyword present, version not bumped)
  - **GREEN**: all assertions pass: (a) `plugin.json` parses as valid JSON; (b) `version` strictly greater than the PRE_BUMP recorded at task-start (current = 2.10.0); (c) bump is minor (major unchanged, minor ≥1 increased, patch == 0); (d) `keywords` array contains BOTH `recap` (preserved from recap v0.1) AND `handoff` (newly added); `jq . dev-workflow/.claude-plugin/plugin.json > /dev/null` exits 0.
- **Dependencies**: Tasks 1, 2, 3 complete first (version bump should land last after content is locked)
- **Independent**: false  # logical sequencing
- **Brief item covered**: "Plugin manifest bump + `handoff` keyword — `dev-workflow` 2.10.0 → 2.11.0 (`.gitignore` recommendation moved to READMEs as user-policy guidance)" — Handoff to writing-plans §4.

## Notes

- **Dogfood meta-test is NOT a numbered task** — per recap precedent, dogfood the new skill on the current session (prepare-mode: write a HANDOFF.md for THIS handoff-v0.1 ship session; resume-mode: simulate loading it) at finishing-a-development-branch time. If dogfood reveals schema gaps, that's a v0.1.1 hotfix signal, not a v0.1 blocker.
- **External surfaces**: none. All 4 tasks are pure-internal-logic — markdown / YAML / JSON authoring with stdlib-only pytest. No HTTP API / SDK / MCP / CLI / sibling-team contracts touched. Per `plan-format.md` §External surfaces, field omitted from all task blocks. (Note: SKILL.md body INSTRUCTS the agent to invoke `git` + `claude --version` via Bash at runtime — but THIS task plan doesn't ship code that calls those; the instructions in SKILL.md are prompt text for the runtime agent to execute via its native Bash tool, not artifacts in the plan's diff.)
- **Parallel-dispatch eligibility**: zero. T1→T2→T3 form a sequential content-dependency chain. T4 could theoretically parallel with T1 (disjoint files) but logically lands last for clean bump semantics. All tasks `Independent: false`. Plan executes purely sequentially via SDD's standard per-task triad.
- **Plan size vs brief estimate**: brief suggested 4 tasks; this plan ships 4 (`.gitignore` was conditionally a 5th in earlier brief draft but moved into T3 README as user-policy guidance to keep plan at 4).
- **Skill-independence guarantee**: T1+T2+T3 touch ONLY files under `dev-workflow/skills/handoff/`. T4 touches only `dev-workflow/.claude-plugin/`. ZERO files under `dev-workflow/skills/recap/` are read for content (only structural reference). Anthropic CLAUDE.md §Skill Structure honored.
- **Rebase risk on T4**: same pattern as recap v0.1 — branch off `e87e7c8c` (post-recap-merge main HEAD); main may advance during this branch's life; T4 implementer reads current version at run-time, bumps minor accordingly.

## Plan-document-reviewer notes (informational only — no checks failed)

1. T3 Module field is the directory `dev-workflow/skills/handoff/` (not a single file path) because the README triplet is one logical content unit. Acceptable given the three READMEs share a content-consistency test; if SDD per-task scoping prefers a single-file Module, narrow to `README.md` with JA/zh-TW listed only in Files touched.
2. T4 Dependencies string `"Tasks 1, 2, 3 complete first"` is a multi-task sequential extension of the documented singular form `"Task N completes first"`; schema §Dependencies enumerates only singular + parallel forms. Intent is unambiguous (T4 lands last) and DAG acyclic; surface for awareness.
3. Plan correctly omits `Independent: true` on the lone wave-1 T1 per [[independent-true-lone-wave-leader]] memory pattern.
