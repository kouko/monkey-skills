# Plan: recap v0.1

**Source brief**: `docs/code-toolkit/specs/2026-05-26-recap-v0.1-brief.md`
**Total tasks**: 4 (≤5 ✓)
**Execution order**: sequential (T1 → T2 → T3 → T4)
**Plan-document-reviewer verdict**: PASS (2026-05-26, 11/11 applicable checks; 3 informational notes — see end of file)

## Task 1 — Bundle `references/seven-block-schema.md`

- **Description**: Create the bundled reference that ships the Recap-research V1 7-block template + **5 共通核心原則** (4 from synthesis research + 5th skill-specific "Plain language by default" per brief 2026-05-26 amendment, with one-line WHY per principle) + one good-example / one bad-example pair where the bad example demonstrates jargon-creep AND paraphrase-creep (paired vices the 5th principle counters). The file is the SSOT for the recap schema; SKILL.md routes to it.
- **Module**: `dev-workflow/skills/recap/references/seven-block-schema.md` (new file)
- **Files touched**: `dev-workflow/skills/recap/references/seven-block-schema.md`, `dev-workflow/skills/recap/scripts/test_seven_block_schema.py`
- **Context paths**:
  - `/Users/kouko/kouko-obsidian-vault/research/2026-05-25 AI Agent Recap 框架研究——從醫療 handoff 到 Claude Code 9 段壓縮.md` (V1 template SSOT, §我的 Recap Prompt 模板（V1）)
  - `/Users/kouko/kouko-obsidian-vault/research/2026-05-25 AI Agent 對話接續綜論——Recap 與 HANDOFF 共通核心與分歧軸線.md` (4 共通核心原則 SSOT, §共通核心：四大原則)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/docs/code-toolkit/specs/2026-05-26-recap-v0.1-brief.md` (success criteria, §Smallest End State)
- **Acceptance**:
  - **RED**: `test_seven_block_schema.py::test_all_seven_blocks_and_five_principles_present` — asserts file contains all 7 H2/H3 block headings (Situation / Background / Assessment / User messages / Why-this-question / Pending / Synthesis-check) AND **all 5 共通核心原則 anchors** (structured-schema / quote-not-paraphrase / all-user-messages / synthesis-check / **plain-language**) AND a good-example block AND a bad-example block; fails initially because file does not exist.
  - **GREEN**: file exists; all 7 block headings present (case-insensitive, multilingual-friendly); **5 principle anchors present**; one good-example + one bad-example pair where bad-example demonstrates BOTH paraphrase-creep AND jargon-creep so the 5th principle has visible coverage.
- **Dependencies**: none
- **Independent**: false  # lone wave-1 task per `[[independent-true-lone-wave-leader]]`
- **Brief item covered**: "Bundle `references/seven-block-schema.md` (extracts the V1 template + 4 共通核心原則 + WHY for each block, so SKILL.md body stays under ~6k token budget)" — Handoff to writing-plans §3.

## Task 2 — Author `SKILL.md` with frontmatter + triggers + routing to bundle

- **Description**: Write `dev-workflow/skills/recap/SKILL.md` with valid YAML frontmatter (name=`recap`, version=`0.1.0`, description with multilingual triggers + disambiguation from built-in `/recap`), a SKILL body that defines: when to fire (multilingual trigger list), what to do (read seven-block-schema.md, produce 7-block output in chat-inline, end with soft-gate Synthesis-check), what NOT to do (no file write, no cross-session, no replace built-in `/recap`). SKILL.md body ≤6k tokens per project rule.
- **Module**: `dev-workflow/skills/recap/SKILL.md` (new file)
- **Files touched**: `dev-workflow/skills/recap/SKILL.md`, `dev-workflow/skills/recap/scripts/test_skill_md.py`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/skills/recap/references/seven-block-schema.md` (produced by Task 1)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/skills/brief-before-asking/SKILL.md` (frontmatter + structure precedent)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/skills/distill-sessions/SKILL.md` (sibling pattern reference)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/CLAUDE.md` (Skill Structure rules: flat subfolder, ~6k token cap, relative paths)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/docs/code-toolkit/specs/2026-05-26-recap-v0.1-brief.md` (Decision + Smallest End State + Out of Scope as authoring inputs)
- **Acceptance**:
  - **RED**: `test_skill_md.py::test_frontmatter_and_routing` — asserts: (a) YAML frontmatter parses cleanly with `name: recap`, `version: 0.1.0`, non-empty `description`; (b) description contains the strings `recap` AND at least one zh-TW trigger (e.g. `我們` or `剛剛`) AND at least one ja trigger (e.g. `振り` or `振り返り`) AND at least one en trigger (e.g. `where were we` or `I'm lost`); (c) description explicitly disambiguates from built-in `/recap` (contains both `away-summary` and `in-session` markers OR equivalent); (d) body relative-path references `references/seven-block-schema.md`; (e) body token count ≤6500 (sanity threshold around 6k cap); (f) body explicitly cites **all 5 共通核心原則** by name (structured-schema / quote-not-paraphrase / all-user-messages / synthesis-check / **plain-language**) so the SKILL.md routing reinforces the 5th principle rather than hiding it inside the bundle. Fails initially because SKILL.md does not exist.
  - **GREEN**: all six assertions pass; `.claude/hooks/validate-skill-folder-structure.sh` does not block (flat subfolder rule honored).
- **Dependencies**: Task 1 completes first
- **Independent**: false  # depends on Task 1's reference file existing
- **Brief item covered**: "Create `dev-workflow/skills/<name>/SKILL.md` with V1 7-block template embedded + multilingual triggers + Synthesis-check rule + description disambiguation from built-in `/recap`" — Handoff to writing-plans §1.

## Task 3 — Tri-language READMEs (EN / JA / zh-TW)

- **Description**: Write three READMEs — `README.md` (EN), `README.ja.md`, `README.zh-TW.md` — each with the same 4-section skeleton: (1) overview / what the skill does, (2) when to use vs built-in `/recap`, (3) example invocation phrases for that language, (4) what's deferred to v0.2. Treat EN as the source of truth; JA + zh-TW are translations.
- **Module**: `dev-workflow/skills/recap/` (the README triplet is one logical content unit)
- **Files touched**: `dev-workflow/skills/recap/README.md`, `dev-workflow/skills/recap/README.ja.md`, `dev-workflow/skills/recap/README.zh-TW.md`, `dev-workflow/skills/recap/scripts/test_readmes.py`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/skills/recap/SKILL.md` (produced by Task 2; READMEs document its behavior)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/skills/brief-before-asking/README.md` (EN README precedent)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/skills/brief-before-asking/README.ja.md` (JA README precedent)
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/skills/brief-before-asking/README.zh-TW.md` (zh-TW README precedent)
- **Acceptance**:
  - **RED**: `test_readmes.py::test_tri_lang_readmes_consistent` — asserts: (a) all 3 README files exist; (b) each contains 4 section headings (overview / when to use vs `/recap` / example invocation / v0.2 deferred); (c) the EN README's example invocation phrase matches at least one trigger declared in SKILL.md description (consistency check); (d) JA and zh-TW READMEs each contain at least 3 language-native trigger phrases listed in SKILL.md description. Fails initially because READMEs do not exist.
  - **GREEN**: all four assertions pass; READMEs read naturally in their respective languages (no machine-translation artifacts).
- **Dependencies**: Task 2 completes first
- **Independent**: false  # depends on Task 2's SKILL.md description for trigger consistency check
- **Brief item covered**: "Tri-language READMEs (EN / JA / zh-TW) per CLAUDE.md project rule for new skills" — Handoff to writing-plans §2.

## Task 4 — Bump `dev-workflow` plugin version + register `recap` keyword

- **Description**: Bump `dev-workflow/.claude-plugin/plugin.json` `version` from current main HEAD's value to the next minor (assume `2.8.0` → `2.9.0` at rebase time; implementer reads current value and bumps minor). Add `recap` to the `keywords` array. Skill auto-discovery means no explicit registration in plugin.json; the keyword is for discoverability only.
- **Module**: `dev-workflow/.claude-plugin/plugin.json`
- **Files touched**: `dev-workflow/.claude-plugin/plugin.json`, `dev-workflow/.claude-plugin/test_plugin_manifest.py`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/.claude/worktrees/explore-recap-handoff-research/dev-workflow/.claude-plugin/plugin.json` (current manifest — worktree base is 2.7.1; main may be ahead at 2.8.0 per PR #336; bump from whatever the file actually shows at implementation time)
- **Acceptance**:
  - **RED**: `test_plugin_manifest.py::test_version_and_keywords` — asserts: (a) `plugin.json` parses as valid JSON; (b) `version` is strictly greater (semver-compare) than the version recorded in a frozen snapshot at task-start time; (c) bump is minor (Y bumps by 1, Z resets to 0) per semver convention for adding a new skill; (d) `keywords` array contains the string `recap`. Fails initially because version is not yet bumped and `recap` keyword not added.
  - **GREEN**: all four assertions pass; manifest valid; `jq . plugin.json > /dev/null` exits 0.
- **Dependencies**: Tasks 1, 2, 3 complete first (version bump should be last, after content lands)
- **Independent**: false  # logically gated on prior tasks for sane bump semantics
- **Brief item covered**: "`dev-workflow/plugin.json` version bump + skill registration" — Handoff to writing-plans §4. (Note: actual path is `.claude-plugin/plugin.json`, not `plugin.json` at plugin root — verified 2026-05-26.)

## Notes

- **Dogfood meta-test (brief §5) is NOT a numbered task** — it's a finishing-a-development-branch validation step. Per the brief's spirit ("the skill should be able to recap *this* brainstorming session"), once T1-T4 ship, the orchestrator invokes the new skill on the active session as a smoke test before opening the PR. If it cannot produce a clean 7-block output, that's a v0.1.1 hotfix signal, not a v0.1 blocker.
- **External surfaces**: none. All 4 tasks are pure-internal-logic — markdown / YAML / JSON authoring with stdlib-only pytest. No external API / SDK / MCP / CLI / sibling-team contracts touched. Per `plan-format.md` §External surfaces, field omitted from all task blocks.
- **Parallel-dispatch eligibility**: zero. T1→T2→T3 form a sequential content-dependency chain (T2 reads T1, T3 reads T2's description). T4 could theoretically parallel with T1 (disjoint files) but logically should land last for clean bump semantics. All tasks `Independent: false`. Plan executes purely sequentially via SDD's standard per-task triad.
- **Plan size vs brief estimate**: brief suggested 3-5 tasks; this plan ships 4. The 5th item from the brief (dogfood meta-test) folds into finishing-a-branch per the rationale above.
- **Rebase risk on T4**: this branch (`explore/recap-handoff-research`) is off `fc220a0c` (pre-PR #336). When we rebase onto main at finishing-a-branch time, main will have advanced to 2.8.0 (or later). T4's implementer must read the current `version` value at run-time, not assume `2.7.1`.

## Plan-document-reviewer notes (informational only — no checks failed)

1. T4's RED check (b) uses a "frozen snapshot at task-start time" for semver-compare — this implies the implementer must capture current version before editing. Workable but adds a runtime step; SDD orchestrator should ensure the implementer reads + records current version as the first action of T4 (the plan's §Notes "Rebase risk on T4" already flags the dynamic-read pattern, so this is consistent).
2. Test files under `dev-workflow/skills/recap/scripts/` and `dev-workflow/.claude-plugin/test_plugin_manifest.py` are not explicitly named in the brief's Smallest End State, but fall under the standard skill scaffold pattern (CLAUDE.md §Skill Structure allows `scripts/` subfolder) and are required by the TDD iron law for the RED step — coverage is implicit and acceptable.
3. Dogfood meta-test (brief Handoff §5) intentionally folded into finishing-a-branch per plan §Notes — surfaced as design choice, not a missed brief item. Brief item 5 is documented as not-a-task with rationale, which the schema permits.
