# Plan: gws-toolkit v0.6.0 Workspace support — Part 2 (docs + release)

**Source brief**: `docs/gws-toolkit/specs/2026-05-19-v0.6.0-workspace-support.md` (ACCEPTED)
**Sibling plan**: `docs/gws-toolkit/plans/2026-05-19-v0.6.0-workspace-support-part-1.md`
**Total tasks**: 3 (≤5 ✓)
**Execution order**: All 3 tasks edit different files; eligible for `dispatching-parallel-agents` (each marked `Independent: true` with disjoint `Files touched`).
**Precondition**: Part 1 (T1-T4) PASS via SDD — `auto-setup.sh` and `credential-check.sh` already aware of account-type. Encoded as plan-level precondition (not per-task `Dependencies` — schema only permits within-plan task IDs).
**Plan-document-reviewer verdict**: PASS (2026-05-19, round 2, 14/14)

## Time-box rationale

Each task is a doc-edit on a single file. T1 SKILL.md is the heaviest (3 sections to update with dual-path narrative); ~5-7 min. T2 TECH-SPEC.md is a search-and-update for "Phase 2+" caveats; ~3-5 min. T3 lock-step bump + CHANGELOG entry follows the established v0.5.1 / v0.5.2 precedent; ~3-5 min.

Same Beck (2002) Child-Test fallback escape hatch applies if any task overruns.

## Task 1 — Update `skills/gws-setup/SKILL.md` for dual-path narrative

- **Description**: Three sections need dual-path treatment:
  1. **Prerequisites table** (line 31-38) — remove the personal-Gmail-only restriction; add a row or note that Workspace accounts now supported (auto-detected by gcloud active-account email domain).
  2. **Frontmatter description** (line 3) — remove `MVP 只處理個人 @gmail.com（macOS）；Google Workspace 企業帳號 Phase 2+`; add Workspace-supported note.
  3. **"Every 7 days maintenance" section** (~line 188-208) — rename heading to `## Every 7 days maintenance (personal-account only)` and add explicit "Workspace accounts: no 7-day refresh limit; this section does not apply" note at the top. Keep existing personal-account body verbatim under the renamed section.
  4. **State-detection branch table** (line 84-91) — add an `account_type` column or row reflecting the new `credential-check.sh` JSON field (consumer of Part 1 T4).
- **Brief item covered**: Smallest-End-State #5 ("SKILL.md narrative dual-pathed — Prerequisites + Setup flow + Maintenance sections account-type-aware").
- **Module**: `gws-toolkit/skills/gws-setup/`
- **Files touched**:
  - `gws-toolkit/skills/gws-setup/SKILL.md`
- **Context paths**:
  - `gws-toolkit/skills/gws-setup/SKILL.md` — entire file (~240 lines, read in full for the 4 sections)
  - `docs/gws-toolkit/specs/2026-05-19-v0.6.0-workspace-support.md` §Decision (auto-detect + flag)
- **Acceptance**:
  - RED: `grep -nE 'MVP 只處理個人|Workspace.*Phase 2\+|Workspace accounts.*Phase 2\+' gws-toolkit/skills/gws-setup/SKILL.md` → ≥1 match (current state)
  - GREEN:
    - `grep -nE 'MVP 只處理個人|Workspace.*Phase 2\+|Workspace accounts.*Phase 2\+' gws-toolkit/skills/gws-setup/SKILL.md` → 0 matches
    - `grep -nE '## Every 7 days maintenance \(personal' gws-toolkit/skills/gws-setup/SKILL.md` → 1 match (heading renamed)
    - `grep -nE 'Workspace.*no 7-day refresh limit|Workspace.*does not apply' gws-toolkit/skills/gws-setup/SKILL.md` → ≥1 match (Workspace bypass note)
    - `grep -nE 'account_type' gws-toolkit/skills/gws-setup/SKILL.md` → ≥1 (state-detection table updated to consume new credential-check field)
- **Dependencies**: none (cross-plan precondition encoded at plan header)
- **Independent**: true
- **Files touched (for parallel-dispatch oracle)**: `gws-toolkit/skills/gws-setup/SKILL.md`

## Task 2 — Remove Workspace "Phase 2+" caveats from `TECH-SPEC.md`

- **Description**: Search `gws-toolkit/TECH-SPEC.md` for any occurrence of "Workspace ... Phase 2+" or similar deferral phrases. Replace with updated language ("Workspace accounts supported as of v0.6.0; auto-detected by gcloud active-account email domain"). Do not touch unrelated `Phase 2+` mentions (Linux / CI / WSL — those remain Phase 2+).
- **Brief item covered**: Smallest-End-State #6 ("TECH-SPEC.md updated — remove `Workspace accounts are Phase 2+` caveats").
- **Module**: `gws-toolkit/` (TECH-SPEC at plugin root)
- **Files touched**:
  - `gws-toolkit/TECH-SPEC.md`
- **Context paths**:
  - `gws-toolkit/TECH-SPEC.md` — entire file (~1200 lines; targeted search needed)
  - Initial grep before editing: `grep -nE 'Workspace.*Phase 2|Phase 2.*Workspace|@gmail.com.*Phase 2|MVP.*personal' gws-toolkit/TECH-SPEC.md` (locate all touch sites)
- **Acceptance**:
  - RED: `grep -nE 'Workspace.*Phase 2\+|Phase 2\+.*Workspace' gws-toolkit/TECH-SPEC.md` → ≥1 match
  - GREEN:
    - `grep -nE 'Workspace.*Phase 2\+|Phase 2\+.*Workspace' gws-toolkit/TECH-SPEC.md` → 0 matches
    - `grep -nE 'Workspace.*v0\.6\.0|v0\.6\.0.*Workspace' gws-toolkit/TECH-SPEC.md` → ≥1 match (replacement language landed)
    - Linux / CI / WSL `Phase 2+` mentions UNCHANGED (those are separate scope deferrals): `grep -nE 'Linux.*Phase 2\+|CI.*Phase 2\+|WSL.*Phase 2\+' gws-toolkit/TECH-SPEC.md` → same hit count as before this task
- **Dependencies**: none (cross-plan precondition encoded at plan header)
- **Independent**: true
- **Files touched (for parallel-dispatch oracle)**: `gws-toolkit/TECH-SPEC.md`

## Task 3 — Lock-step version bump 0.5.2 → 0.6.0 + CHANGELOG `[0.6.0]` entry

- **Description**: Bump `gws-toolkit/.claude-plugin/plugin.json` version field 0.5.2 → 0.6.0. Insert `[0.6.0] - 2026-05-19` entry into `gws-toolkit/CHANGELOG.md` immediately above existing `[0.5.2]` entry, documenting Workspace-support changes. Follow established precedent set by 0.5.1 / 0.5.2 bumps in `gws-toolkit/CHANGELOG.md`.
- **Brief item covered**: Smallest-End-State #7 ("Lock-step version bump 0.5.2 → 0.6.0") + #8 ("CHANGELOG.md `[0.6.0]` entry documenting Workspace support").
- **Module**: `gws-toolkit/` (manifest + CHANGELOG at plugin root)
- **Files touched**:
  - `gws-toolkit/.claude-plugin/plugin.json`
  - `gws-toolkit/CHANGELOG.md`
- **Context paths**:
  - `gws-toolkit/.claude-plugin/plugin.json` — version field
  - `gws-toolkit/CHANGELOG.md` — top section (Unreleased + [0.5.2] / [0.5.1] / [0.5.0])
  - Precedent: `[0.5.2]` and `[0.5.1]` entries already in the CHANGELOG (committed in PR #316 / #315)
- **Acceptance**:
  - RED:
    - `jq -r '.version' gws-toolkit/.claude-plugin/plugin.json` → `0.5.2` (current)
    - `grep -nE '^## \[0\.6\.0\]' gws-toolkit/CHANGELOG.md` → 0 matches
  - GREEN:
    - `jq -r '.version' gws-toolkit/.claude-plugin/plugin.json` → `0.6.0`
    - `grep -nE '^## \[0\.6\.0\] - 2026-05-19' gws-toolkit/CHANGELOG.md` → 1 match
    - CHANGELOG `[0.6.0]` entry contains: "Workspace account support" + reference to Part 1 changes (detect_account_type, dual-path 5a/5b, credential-check account_type field) + reference to Part 2 doc changes
    - 4 convention scripts PASS (`check-skill-structure.py gws-toolkit`, `check-plugin-description-skill-coherence.py`, `check-marketplace-description-sync.py`, `check-shared-conventions-drift.py`)
- **Dependencies**: none (cross-plan precondition encoded at plan header)
- **Independent**: true
- **Files touched (for parallel-dispatch oracle)**: `gws-toolkit/.claude-plugin/plugin.json`, `gws-toolkit/CHANGELOG.md`

## Post-Part-2 verification (handled by `verification-before-completion` skill, not a plan task)

After T1-T3 PASS:
- `python scripts/check-skill-structure.py gws-toolkit` → 9/9 skills PASS
- `python scripts/check-plugin-description-skill-coherence.py` → 18 plugins coherent
- `python scripts/check-marketplace-description-sync.py` → marketplace description in sync (if updated)
- `python scripts/check-shared-conventions-drift.py` → 0 drift
- **Real-machine dogfood** (kouko's Workspace account `yihan.chang@ichef.com.tw`):
  1. `gcloud auth login` with Workspace account
  2. `bash gws-toolkit/scripts/gws/auto-setup.sh --dry-run` → expect Workspace-path output
  3. (Optional) full real-mode setup against iChef Workspace project — validates Open Q1 (issue #119) + Q2 (GCP org policy)

These belong to Stage 7 (`verification-before-completion`) dispatched after SDD finishes both parts.
