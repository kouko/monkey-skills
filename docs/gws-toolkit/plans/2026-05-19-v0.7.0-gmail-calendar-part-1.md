# Plan: gws-toolkit v0.7.0 — Part 1 (Scripts & vendoring)

**Source brief**: docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md
**Total tasks**: 5 (≤5 ✓)
**Execution order**: T1–T4 parallel-eligible (disjoint Files touched, no shared symbol); T5 sequential after T4
**Plan-document-reviewer verdict**: PASS (2026-05-19 14:00 — `domain-teams:evaluator` dispatched as plan-document-reviewer; 14/14 checks passed; 3 advisory notes on Check 4 / Check 5 / brief-coverage boundary, none blocking)

> **Scope locked from brief**: 6 OAuth scopes = `presentations,drive,documents,spreadsheets,gmail,calendar` (all full read-write, restricted-tier where applicable); 6 enabled APIs = `slides + drive + docs + sheets + gmail + calendar`; 4 new vendored skills = `gws-gmail`, `gws-gmail-read`, `gws-calendar`, `gws-calendar-agenda`. Upstream pin held at `googleworkspace/cli@v0.22.5 (705fb0ec)`. The 2 multi-file tasks (T4) intentionally batch files that **must move atomically** to prevent drift — see per-task Module field for the conceptual unit.

---

## Task 1 — Extend auto-setup.sh to 6 APIs + 6 OAuth scopes

- **Description**: Add `GMAIL_SCOPE` + `CALENDAR_SCOPE` constants. Extend `ensure_apis()` (both dry-run echo + real `gcloud services enable` + the 4-line `grep -q` idempotency check) to include `gmail.googleapis.com` + `calendar.googleapis.com`. Extend `ensure_gws_auth()` `--scopes=...` (dry-run + real) to a 6-URL comma-separated string. Extend `emit_result()` `scopes:` JSON array to 6 elements. Update the file-header docstring (lines 4-75) so step 3 says "6 Workspace APIs" and step 8 lists 6 scopes. Preserve all v0.6.0 idempotency + Workspace dual-path logic verbatim — this task is pure additive scope expansion.
- **Module**: `gws-toolkit/scripts/gws/auto-setup.sh`
- **Files touched**: `gws-toolkit/scripts/gws/auto-setup.sh`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/gws/auto-setup.sh` (lines 86-89 scope consts, 350-380 ensure_apis, 735-773 ensure_gws_auth, 799-826 emit_result)
  - `/Users/kouko/GitHub/monkey-skills/docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md` (§Decision + §Smallest End State)
- **Acceptance**:
  - **RED**: `bash gws-toolkit/scripts/gws/auto-setup.sh --dry-run` stderr currently prints `slides.googleapis.com drive.googleapis.com docs.googleapis.com sheets.googleapis.com` (4 APIs) in `ensure_apis` dry_echo and `--scopes=` (4 URLs) in `ensure_gws_auth` dry_echo. A test that asserts `… | grep -o 'gmail\.googleapis\.com\|calendar\.googleapis\.com' | sort -u | wc -l` returns `2` is currently RED.
  - **GREEN**: same dry-run produces output where the grep returns `2` (Gmail + Calendar API names appear); a parallel grep for `gmail\b\|calendar\b` against the `--scopes=` line returns `2`; stdout JSON `scopes:` array has length 6. Re-running with no flags on a previously-configured machine still hits the `already enabled, skip` + `gws already authed` short-circuits (idempotency preserved).
- **Dependencies**: none
- **Independent**: true
- **Brief item covered**: "auto-setup.sh ensure_apis() enables 6 APIs (add gmail.googleapis.com + calendar.googleapis.com)" + "auto-setup.sh step_8a requests 6 OAuth scopes (add gmail + calendar — full read-write, restricted-tier, matching the v0.4.0 precedent)"

---

## Task 2 — Extend refresh-auth.sh DEFAULT_SCOPES to 6 scopes

- **Description**: Update `DEFAULT_SCOPES=` constant (line 54) from the 4-URL `presentations,drive,documents,spreadsheets` string to the 6-URL set adding Gmail + Calendar. Update the file-header docstring §Default (lines 28, 49-54) so it says "Default scope set: 6 APIs covered (Slides, Drive, Docs, Sheets, Gmail, Calendar)". No behavioral change beyond the scope-list extension; helper script's `gws auth login --scopes=` invocation already consumes `$DEFAULT_SCOPES` so no further code change needed.
- **Module**: `gws-toolkit/scripts/gws/refresh-auth.sh`
- **Files touched**: `gws-toolkit/scripts/gws/refresh-auth.sh`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/gws/refresh-auth.sh` (lines 28, 49-54)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/gws/auto-setup.sh` (lines 86-89; mirror those scope URLs)
- **Acceptance**:
  - **RED**: `grep -E '^readonly DEFAULT_SCOPES=' gws-toolkit/scripts/gws/refresh-auth.sh | grep -o 'gmail\|calendar' | wc -l` currently returns `0`.
  - **GREEN**: same grep returns `2`; full `DEFAULT_SCOPES=` value parses as comma-separated list of exactly 6 URLs; docstring §Default uses the word "6" not "4".
- **Dependencies**: none (file-scope-independent of T1; reviewer will cross-check scope-list alignment)
- **Independent**: true
- **Brief item covered**: "refresh-auth.sh DEFAULT_SCOPES mirrors the new 6-scope set so the weekly re-auth ritual stays in lock-step"

---

## Task 3 — Extend gws-login.sh JSON contract + docstring to 6 scopes

- **Description**: Extend both `scopes: [...]` JSON-array emit sites (lines 152, 164) from 4 elements to 6 (add `"gmail","calendar"`). Update docstring contract example (line 43) `"scopes":["presentations","drive","documents","spreadsheets"]` → 6-element version. Update docstring narrative (lines 96, 108) "(presentations, drive, documents, spreadsheets)" → 6-API listing + "Override scopes (default: 6 APIs as full URLs, …)". Default `GWS_TOOLKIT_SCOPES` env-var value (if hardcoded) extended to match. No change to the `--switch` semantics — pure list extension.
- **Module**: `gws-toolkit/scripts/gws/gws-login.sh`
- **Files touched**: `gws-toolkit/scripts/gws/gws-login.sh`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/gws/gws-login.sh` (lines 43, 96, 108, 152, 164)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/gws/auto-setup.sh` (line 822 emit_result; mirror)
- **Acceptance**:
  - **RED**: `grep -c '"gmail"' gws-toolkit/scripts/gws/gws-login.sh` currently returns `0`; same for `"calendar"`.
  - **GREEN**: both greps return `≥2` (both emit sites updated); docstring §contract example shows a 6-element array; `grep '6 APIs' gws-toolkit/scripts/gws/gws-login.sh` returns a match.
- **Dependencies**: none (file-scope-independent of T1, T2; reviewer will cross-check the 3-way scope-array alignment across auto-setup.sh / refresh-auth.sh / gws-login.sh)
- **Independent**: true
- **Brief item covered**: "gws-login.sh JSON contract + docs reflect 6 scopes"

---

## Task 4 — Update vendored skill list (UPSTREAM_GWS_VERSION + sync-upstream-skills.sh — lock-step pair)

- **Description**: Two-file atomic edit because **drift between these two lists is silent and breaks vendoring**. (a) `gws-toolkit/UPSTREAM_GWS_VERSION` — extend the `synced_skills:` YAML list from 5 elements to 9 (append `gws-gmail`, `gws-gmail-read`, `gws-calendar`, `gws-calendar-agenda`). Bump `synced_at:` to `2026-05-19`. Hold `gws_cli_release` + `gws_cli_commit` unchanged (additive vendoring; no upstream version bump per brief §Out of Scope). (b) `gws-toolkit/scripts/sync-upstream-skills.sh` — extend the `VENDORED_SKILLS=( … )` Bash array (line 46) by the same 4 names in the same order. **No** materialization of new SKILL.md in this task — that's T5.
- **Module**: `vendored skill list (lock-step pair)` — conceptual unit; both files must change together to preserve UPSTREAM_GWS_VERSION as the single source of truth.
- **Files touched**: `gws-toolkit/UPSTREAM_GWS_VERSION`, `gws-toolkit/scripts/sync-upstream-skills.sh`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/UPSTREAM_GWS_VERSION` (lines 9-19)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/sync-upstream-skills.sh` (lines 9 docstring, 46 VENDORED_SKILLS array, 162 grep-able diff hint)
- **Acceptance**:
  - **RED**: `awk '/^synced_skills:/,/^[^-[:space:]]/' gws-toolkit/UPSTREAM_GWS_VERSION | grep -c '^  - gws-'` currently returns `5`; `bash -c 'source <(grep "^readonly VENDORED_SKILLS" gws-toolkit/scripts/sync-upstream-skills.sh); echo ${#VENDORED_SKILLS[@]}'` currently returns `5`.
  - **GREEN**: both counts return `9`; the 4 names match exactly in both files (`diff <(awk … UPSTREAM…) <(grep … sync-upstream-skills.sh)` returns no diff after normalization); `synced_at:` is `2026-05-19`; `gws_cli_release` still `v0.22.5`.
- **Dependencies**: none
- **Independent**: true
- **Brief item covered**: "UPSTREAM_GWS_VERSION synced_skills: adds 4 names (gws-gmail, gws-gmail-read, gws-calendar, gws-calendar-agenda); sync-upstream-skills.sh VENDORED_SKILLS=(…) adds the same 4"

---

## Task 5 — Run sync-upstream-skills.sh to materialize 4 new vendored SKILL.md files

- **Description**: After T4 lands the updated `VENDORED_SKILLS` array, run `bash gws-toolkit/scripts/sync-upstream-skills.sh` (NOT `--dry-run`) which: (1) fetches `skills/gws-{gmail,gmail-read,calendar,calendar-agenda}/SKILL.md` from `googleworkspace/cli@705fb0ec` via `gh api`; (2) injects provenance frontmatter (`vendored_from`, `vendored_release`, `vendored_at`, `upstream_license`) via the existing `inject_provenance()` AWK transformation; (3) writes 4 new files: `gws-toolkit/skills/gws-gmail/SKILL.md`, `gws-toolkit/skills/gws-gmail-read/SKILL.md`, `gws-toolkit/skills/gws-calendar/SKILL.md`, `gws-toolkit/skills/gws-calendar-agenda/SKILL.md`. **Implementer responsibility**: only runs the script + verifies the 4 expected files appeared + spot-checks one file's frontmatter has the v0.7.0-correct `vendored_at: 2026-05-19`. **Does NOT** hand-edit the materialized content (upstream-managed, byte-identical + provenance only per Apache-2.0 §4(a) compliance).
- **Module**: `vendored skill materialization (script run + file presence check)`
- **Files touched**: `gws-toolkit/skills/gws-gmail/SKILL.md`, `gws-toolkit/skills/gws-gmail-read/SKILL.md`, `gws-toolkit/skills/gws-calendar/SKILL.md`, `gws-toolkit/skills/gws-calendar-agenda/SKILL.md` (all 4 generated by the script — implementer does not hand-edit)
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/sync-upstream-skills.sh`
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/UPSTREAM_GWS_VERSION`
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/skills/gws-shared/SKILL.md` (example of expected provenance frontmatter shape)
- **Acceptance**:
  - **RED**: `ls gws-toolkit/skills/gws-gmail/SKILL.md gws-toolkit/skills/gws-gmail-read/SKILL.md gws-toolkit/skills/gws-calendar/SKILL.md gws-toolkit/skills/gws-calendar-agenda/SKILL.md 2>/dev/null | wc -l` currently returns `0`.
  - **GREEN**: same `ls` returns `4`; `grep -l 'vendored_at: "2026-05-19"' gws-toolkit/skills/gws-{gmail,gmail-read,calendar,calendar-agenda}/SKILL.md | wc -l` returns `4`; `grep -l 'vendored_release: "v0.22.5"' …` returns `4`; the script's stdout summary line reads `[sync] all 9 skill(s) synced` (matches new total).
- **Dependencies**: Task 4 completes first (the `VENDORED_SKILLS` array must contain the 4 new names before the loop runs)
- **Independent**: false  # depends on Task 4
- **Brief item covered**: "One `bash scripts/sync-upstream-skills.sh` run materializes the 4 new vendored SKILL.md files"

---

## Notes for SDD orchestrator

- **Parallel dispatch eligibility**: Tasks 1, 2, 3, 4 are all `Independent: true` with disjoint `Files touched` sets — eligible for `dispatching-parallel-agents` fan-out in **one** assistant message (4 concurrent implementer subagents). Task 5 must run **after** Task 4 (depends on the materialized `VENDORED_SKILLS=( … )` content). Sequential SDD per-task triad is always the safe fallback.
- **Cross-task coherence to flag for whole-branch reviewer (Stage 6)**:
  - **Scope-list alignment**: Tasks 1, 2, 3 each touch a scope list in a different file. The 6 scope URLs (or 6-element JSON arrays) must match **byte-for-byte across all 3 places**. Brief §"Brief metadata" calls this out as the #1 cross-task drift risk.
  - **Vendored-skill-list alignment**: Task 4 updates 2 files (`UPSTREAM_GWS_VERSION` + `sync-upstream-skills.sh`) — the 4 new names + their order must match exactly. Task 5's actual file materialization is the proof of consistency.
- **Hand-off to Part 2**: After Part 1 lands (5 tasks DONE), the doc layer (commands/SKILL.md/README.md/TECH-SPEC.md/plugin.json/CHANGELOG.md) is the next plan. See `docs/gws-toolkit/plans/2026-05-19-v0.7.0-gmail-calendar-part-2.md`.
