# Plan: gws-toolkit v0.7.1 — Part 4 — Release marker

**Source brief**: docs/gws-toolkit/specs/2026-05-20-v0.7.1-write-asymmetry.md
**Total tasks**: 2 (≤5 ✓)
**Execution order**: parallel-where-possible (Tasks 1 + 2 `Independent: true` with disjoint Files touched)
**Plan-document-reviewer verdict**: PASS (2026-05-20)

> The two tasks materialize the v0.7.1 release: lock-step version bump (plugin.json + marketplace.json mirror) and CHANGELOG entry. Per memory `feedback_plugin_marketplace_sync`, plugin.json + marketplace.json MUST be edited together (single logical "version-metadata-lock-step" module across 2 files); CHANGELOG.md is a separate `release-notes` concern. Cross-task coherence: version string `0.7.1` appears in 3 places (plugin.json `.version`, marketplace.json `.plugins[].description` matching, CHANGELOG.md `[0.7.1]` heading) — whole-branch reviewer diffs these.

## Task 1 — Lock-step version bump 0.7.0 → 0.7.1 (plugin.json + marketplace.json)

- **Description**: Bump `gws-toolkit/.claude-plugin/plugin.json` `.version` field from `"0.7.0"` to `"0.7.1"`. Update `.description` field to surface write-side capability (suggested append: `... / write-safety wrappers`; final phrasing decided by implementer to fit existing v0.7.0 description style). Mirror description change byte-identically into `.claude-plugin/marketplace.json` `.plugins[] | select(.name == "gws-toolkit") | .description` (the repo-root marketplace manifest's gws-toolkit plugin entry) per memory `feedback_plugin_marketplace_sync` (Required CI check). Keep `.keywords` array unchanged (already has `gmail` + `google-calendar` from v0.7.0; write helpers don't introduce new top-level keywords).
- **Module**: `version-metadata-lock-step` (logical module spanning plugin.json + marketplace.json SSOT mirror per memory rule)
- **Files touched**: `gws-toolkit/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/.claude-plugin/plugin.json` (current `.version: "0.7.0"`)
  - `/Users/kouko/GitHub/monkey-skills/.claude-plugin/marketplace.json` (current gws-toolkit plugin entry)
  - `/Users/kouko/.claude/projects/-Users-kouko-GitHub-monkey-skills/memory/MEMORY.md` (lock-step sync rule)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/CHANGELOG.md` §[0.7.0] (style ref for v0.7.0 description edit)
- **Acceptance**:
  - **RED**: `jq -r .version gws-toolkit/.claude-plugin/plugin.json` returns `"0.7.0"`; `diff <(jq -r .description gws-toolkit/.claude-plugin/plugin.json) <(jq -r '.plugins[] | select(.name=="gws-toolkit") | .description' .claude-plugin/marketplace.json)` exits 0 (descriptions match at v0.7.0 baseline)
  - **GREEN**: `jq -r .version gws-toolkit/.claude-plugin/plugin.json` returns `"0.7.1"`; same `diff` exits 0 (descriptions match at v0.7.1); both JSON files pass `jq empty <path>` (valid JSON); description string mentions "write" or "safety" or equivalent surfacing of the v0.7.1 capability
- **Dependencies**: none (could run any time after Parts 1-3; SDD typically sequences last)
- **Independent**: true (disjoint Files touched from Task 2)
- **Brief item covered**: "Lock-step bump 0.7.0 → 0.7.1 across `gws-toolkit/.claude-plugin/plugin.json` (version + description) + `.claude-plugin/marketplace.json` (mirror description)" §Smallest End State Lock-step bump

## Task 2 — Add CHANGELOG.md [0.7.1] - 2026-05-20 entry

- **Description**: Add new `## [0.7.1] - 2026-05-20` section at top of `gws-toolkit/CHANGELOG.md` (above existing `## [0.7.0] - 2026-05-19`), mirroring v0.7.0's verbose Keep-a-Changelog format. Required subsections: `### Added` (5 new vendored skills + 4 new safety wrappers + §Quota awareness sub-section), `### Changed` (lock-step bump files + ASCII tree annotation + routing table additions), `### Notes` (purpose: close write asymmetry; no scope change; pin held at v0.22.5/705fb0ec; 2026-05-01 API quota change context), `### Discharged from v0.7.0 backlog` (5 of 11 upstream write helpers + write-side safety wrappers — discharging the brief §What Becomes Obsolete v0.7.0 backlog line), `### Open follow-ups (v0.8.0 backlog)` (remaining 6 gmail-* helpers + sheets-read + 51 recipe-* skills + 10 persona-* skills + Tasks/Meet/People services + the v0.7.0 "find-free-slots → recipe-find-free-time correction" finding from brief §Axis 5).
- **Module**: `gws-toolkit/CHANGELOG.md`
- **Files touched**: `gws-toolkit/CHANGELOG.md`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/CHANGELOG.md` (v0.7.0 entry as style reference — verbose Keep-a-Changelog with multiple subsections including Notes + Open follow-ups)
- **Acceptance**:
  - **RED**: `grep -c '^## \[0.7.1\]' gws-toolkit/CHANGELOG.md` returns 0
  - **GREEN**: returns 1; `[0.7.1]` heading appears before `[0.7.0]` in file order (`grep -nE '^## \[0\.7\.[01]\]' gws-toolkit/CHANGELOG.md` shows `[0.7.1]` first); all 5 required subsections present (`grep -c '^### \(Added\|Changed\|Notes\|Discharged\|Open follow-ups\)' gws-toolkit/CHANGELOG.md` post-edit ≥ 5+pre-count for the new section); date string `2026-05-20` present in the `[0.7.1]` heading line
- **Dependencies**: none
- **Independent**: true (disjoint Files touched from Task 1)
- **Brief item covered**: "`gws-toolkit/CHANGELOG.md` — new `[0.7.1]` entry mirroring v0.7.0 verbose style" §Smallest End State Lock-step bump

## Notes

- Both tasks `Independent: true` with **disjoint Files touched** — eligible for `dispatching-parallel-agents` fan-out.
- Lock-step interpretation of `MEMORY.md feedback_plugin_marketplace_sync`: plugin.json + marketplace.json are SSOT mirrors and edited together as **one** atomic task with 2 files touched. The `Module` field uses the logical name `version-metadata-lock-step` to surface this to code-quality-reviewer. The 2 files are NOT split into 2 tasks because (a) per-memory the edit MUST be atomic, (b) splitting would risk drift between RED and GREEN states across tasks.
- After Part 4 closes, `finishing-a-development-branch` runs: requesting-code-review (whole-branch) → verification-before-completion (package-level test invocation; brief notes test infra backfill is Unreleased backlog so verification reduces to `bash auto-setup.sh --dry-run` shape assertion + each wrapper's `--help` smoke test) → dev-workflow:git-memory delegation → push → optional PR.
- Cross-task coherence (whole-branch reviewer attention):
  - Task 1 `plugin.json .version: "0.7.1"` ↔ Task 2 `CHANGELOG.md [0.7.1] - 2026-05-20` heading (version string match)
  - Task 1 `plugin.json .description` ↔ Task 2 `CHANGELOG.md ### Added` (description surfaces v0.7.1 capability; CHANGELOG details what was added)
  - Task 1 `marketplace.json` description = `plugin.json` description (memory-enforced byte-identical)
