# Plan: gws-toolkit v0.6.0 Workspace support — Part 1 (detection + console-flow dual-path)

**Source brief**: `docs/gws-toolkit/specs/2026-05-19-v0.6.0-workspace-support.md` (ACCEPTED)
**Sibling plan**: `docs/gws-toolkit/plans/2026-05-19-v0.6.0-workspace-support-part-2.md`
**Total tasks**: 4 (≤5 ✓)
**Execution order**: T1 first (introduces `ACCOUNT_TYPE` global + `--audience` flag); T2 / T3 sequentially edit `step_5a_branding` / `step_5b_test_user` in the same file (no parallel-on-same-file); T4 independent file (`credential-check.sh`).
**Branch**: `feat/gws-toolkit-v0.6.0-workspace-support` (already cut from main).
**Plan-document-reviewer verdict**: PASS (2026-05-19, round 2, 14/14)

## Time-box rationale

T1 introduces a new helper + flag plumbing in one file — tight 5 min.
T2 / T3 each rewrite a single function body in `auto-setup.sh` with branching prose for the alternate path — borderline 5-7 min each (real-prose ≈ stage-2/4 Console copy). T4 is a JSON-shape extension in a small script — ≤5 min.

If any task overruns the 5-min budget mid-execution, the SDD implementer may invoke **Beck (2002) Child-Test fallback** per writing-plans §BLOCKED fallback (re-split into smaller children, re-attempt). This is the established escape hatch.

## Task 1 — Add `detect_account_type()` helper + `--audience` flag to `auto-setup.sh`

- **Description**: Introduce account-type detection plumbing without changing the visible Console-walkthrough behaviour yet (T2 / T3 wire the branching). Detection rule: `gcloud auth list --filter=status:ACTIVE --format='value(account)'` → email → if `*@gmail.com` or `*@googlemail.com` → `personal`; else → `workspace`. Override via new CLI flag `--audience=internal|external` (env-var alternative `GWS_AUDIENCE` not added — keep flag-only for v0.6.0; precedent `SLIDES_TOOLKIT_PROJECT_ID` is env-var but `--audience` is more discoverable).
- **Brief item covered**: Smallest-End-State #1 ("auto-setup.sh detects account type by email domain"); also #7 (`--audience` flag for edge cases).
- **Module**: `gws-toolkit/scripts/gws/auto-setup.sh`
- **Files touched**:
  - `gws-toolkit/scripts/gws/auto-setup.sh`
- **Context paths**:
  - `gws-toolkit/scripts/gws/auto-setup.sh:125-162` — existing `parse_args` function (where to add `--audience` parsing)
  - `gws-toolkit/scripts/gws/auto-setup.sh:705-742` — existing `main()` (where to invoke detection after `ensure_gcloud_auth`)
  - `docs/gws-toolkit/specs/2026-05-19-v0.6.0-workspace-support.md` §Decision (Approach 1 + Approach 2 combined)
- **Acceptance**:
  - RED: `grep -nE 'detect_account_type|--audience|ACCOUNT_TYPE' gws-toolkit/scripts/gws/auto-setup.sh` → 0 matches (current state)
  - GREEN:
    - `grep -nE 'detect_account_type\(\)|ACCOUNT_TYPE=' gws-toolkit/scripts/gws/auto-setup.sh` → ≥2 matches (function defined + variable assigned)
    - `grep -nE '\-\-audience' gws-toolkit/scripts/gws/auto-setup.sh` → ≥1 match (flag parsed)
    - `bash gws-toolkit/scripts/gws/auto-setup.sh --dry-run` still emits valid JSON + reports `account` (no regression)
    - `bash gws-toolkit/scripts/gws/auto-setup.sh --audience=internal --dry-run` accepted without error; new field/echo confirms internal mode selected
- **Dependencies**: none (kicks off Part 1)
- **Independent**: false (T2 / T3 depend on this)

## Task 2 — Dual-path `step_5a_branding` (Internal vs External)

- **Description**: Branch the Stage-2/4 Audience instructions on `ACCOUNT_TYPE`. Personal path: keep current "Select [External]" walkthrough verbatim. Workspace path: write new "Select [Internal]" walkthrough (no Test User downstream, no 7-day refresh limit, simpler scope review). Add a one-line confirmation echoing the chosen path before the prompt block.
- **Brief item covered**: Smallest-End-State #2 ("Step 5a Branding instructions split — Internal picks [Internal] audience").
- **Module**: `gws-toolkit/scripts/gws/auto-setup.sh`
- **Files touched**:
  - `gws-toolkit/scripts/gws/auto-setup.sh`
- **Context paths**:
  - `gws-toolkit/scripts/gws/auto-setup.sh:295-340` — existing `step_5a_branding` body (the External-only walkthrough)
  - `developers.google.com/workspace/guides/configure-oauth-consent` — Internal-app consent flow reference (brief §Sources)
- **Acceptance**:
  - RED: `grep -nE 'Select \[Internal\]|account_type == .workspace.' gws-toolkit/scripts/gws/auto-setup.sh` → 0 (current state, External-only)
  - GREEN:
    - `grep -nE 'Select \[Internal\]' gws-toolkit/scripts/gws/auto-setup.sh` → ≥1 (Workspace path documented)
    - `grep -nE 'Select \[External\]' gws-toolkit/scripts/gws/auto-setup.sh` → ≥1 (Personal path preserved)
    - Both paths gated by `ACCOUNT_TYPE` check (`grep -n 'ACCOUNT_TYPE.*workspace\|ACCOUNT_TYPE.*personal' .../auto-setup.sh` → ≥2)
    - `bash auto-setup.sh --audience=internal --dry-run` prints the Internal walkthrough; `--audience=external --dry-run` prints the External walkthrough
- **Dependencies**: Task 1 completes first
- **Independent**: false (same file as T1, T3)

## Task 3 — Conditional `step_5b_test_user` (skip for Workspace/Internal)

- **Description**: For `ACCOUNT_TYPE=workspace`, skip the Test User instructions entirely and emit a one-line confirmation (`[auto-setup] step 5/8: 5b — Test User SKIPPED (Internal app — no Test User needed)`). For `personal`, keep current behaviour verbatim. Critically: invert the warning message — when Workspace path skips, do NOT print the legacy "Skipping this = step 8 fails with 403 access_denied" warning (that warning is only true for External + Testing).
- **Brief item covered**: Smallest-End-State #3 ("Step 5b Test User becomes conditional — skip entirely for Internal").
- **Module**: `gws-toolkit/scripts/gws/auto-setup.sh`
- **Files touched**:
  - `gws-toolkit/scripts/gws/auto-setup.sh`
- **Context paths**:
  - `gws-toolkit/scripts/gws/auto-setup.sh:340-400` — existing `step_5b_test_user` body (unconditional Test User addition)
- **Acceptance**:
  - RED: `bash auto-setup.sh --audience=internal --dry-run 2>&1 | grep -c 'Add users'` → ≥1 (current state: Test User instructions still printed)
  - GREEN:
    - `bash auto-setup.sh --audience=internal --dry-run 2>&1 | grep -c 'Add users'` → 0 (Test User block suppressed)
    - `bash auto-setup.sh --audience=internal --dry-run 2>&1 | grep -c 'SKIPPED.*Internal'` → 1 (confirmation message printed instead)
    - `bash auto-setup.sh --audience=external --dry-run 2>&1 | grep -c 'Add users'` → ≥1 (External path preserved unchanged)
    - Legacy `Skipping this = step 8 fails with 403 access_denied` warning only emitted on External path
- **Dependencies**: Task 1 completes first
- **Independent**: false (same file as T1, T2)

## Task 4 — Extend `credential-check.sh` JSON output with `account_type` field

- **Description**: Read active gcloud account email (re-use same detection logic as T1's `detect_account_type()`; ideally factor into a sourceable helper later but v0.6.0 can inline-duplicate). Add `account_type` field to the JSON output contract: `"account_type": "personal" | "workspace"`. No behaviour change to existing fields (`backend`, `token_valid`, `expires_in_days`).
- **Brief item covered**: Smallest-End-State #4 (`credential-check.sh` adds `account_type` field to JSON output).
- **Module**: `gws-toolkit/scripts/gws/credential-check.sh`
- **Files touched**:
  - `gws-toolkit/scripts/gws/credential-check.sh`
- **Context paths**:
  - `gws-toolkit/scripts/gws/credential-check.sh` — entire file (~120 lines per earlier read)
  - `gws-toolkit/scripts/gws/auto-setup.sh` (after T1) — reference detection logic to mirror
  - `gws-toolkit/skills/gws-setup/SKILL.md:84-91` — state-detection branch table (will need update in Part 2 but is the consumer of the new field)
- **Acceptance**:
  - RED: `bash gws-toolkit/scripts/gws/credential-check.sh 2>/dev/null | jq -e '.account_type'` → exit non-zero (field absent)
  - GREEN:
    - `bash gws-toolkit/scripts/gws/credential-check.sh 2>/dev/null | jq -er '.account_type'` → prints `"personal"` or `"workspace"` (matches active gcloud account)
    - Existing fields (`backend`, `token_valid`, `expires_in_days`) still present + unchanged shape
    - Script exit code unchanged for the all-other-fields-OK case
- **Dependencies**: Task 1 completes first
- **Independent**: false (sequential to T1 so the implementer can reference T1's `detect_account_type()` helper as the canonical detection pattern when adding the equivalent logic here — convention alignment, not hard code dependency)

## Notes for Part 2

After Part 1 (T1-T4 PASS via SDD), Part 2 plan handles **docs + release**:
- SKILL.md narrative dual-path
- TECH-SPEC.md remove Phase 2+ caveats
- plugin.json bump 0.5.2 → 0.6.0
- CHANGELOG.md `[0.6.0]` entry

Part 2 tasks are largely doc-edit tasks on different files; eligible for `dispatching-parallel-agents` per v0.8.0 markup if reviewer confirms disjoint Files touched.

## Open Question deferral

Brief Open Questions deferred:
- Q1 (Issue #119 workaround per account type) — defer to dogfood after Part 2 ships (real-machine test with kouko's Workspace account)
- Q2 (GCP project creation under Workspace org policy) — same (real-machine dogfood)
- Q3 (`credential-check.sh` JSON enum exact values) — RESOLVED in Part 1 T4 as `"personal" | "workspace"`
- Q4 (`--audience` flag vs env var) — RESOLVED in Part 1 T1 as flag-only
- Q5 (error UX when wrong account type active) — defer to v0.6.x patch if surfaces in dogfood
- Q6 (`@googlemail.com` domain handling) — RESOLVED in Part 1 T1 detection regex
