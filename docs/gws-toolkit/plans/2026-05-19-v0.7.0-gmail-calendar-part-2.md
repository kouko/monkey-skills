# Plan: gws-toolkit v0.7.0 — Part 2 (Docs & lock-step bump)

**Source brief**: docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md
**Predecessor plan**: docs/gws-toolkit/plans/2026-05-19-v0.7.0-gmail-calendar-part-1.md (MUST be DONE before Part 2 dispatches — docs reference 6-API/6-scope state that Part 1 establishes)
**Total tasks**: 5 (≤5 ✓)
**Execution order**: T1–T4 parallel-eligible (disjoint Files touched); T5 sequential after T1–T4 land (lock-step semver bump is the closing act)
**Plan-document-reviewer verdict**: PASS (2026-05-19 14:00 — `domain-teams:evaluator` dispatched as plan-document-reviewer; 14/14 checks passed; 4 advisory notes on Check 4 / Check 5 / Check 14 / Check 8, none blocking)

> **Doc-layer responsibility**: every user-facing narrative referencing "4 APIs" / "4 scopes" / "presentations,drive,documents,spreadsheets" / "5 vendored skills" gets updated to the 6/6/9 numbers. The single source of truth for which strings appear where is the 5 Part-1 script edits + brief. Two intentionally multi-file tasks (T4, T5) batch tightly-coupled-by-meaning files (router+README = discoverability surface; plugin.json+CHANGELOG = lock-step semver bump) — splitting them creates the exact drift this plan defends against.

---

## Task 1 — Update commands/gws-setup.md (slash command body) for 6 APIs + 6 scopes

- **Description**: In `gws-toolkit/commands/gws-setup.md`: (a) Line 33 step 4 table row — change "Enable 4 APIs | Slides / Drive / Docs / Sheets" → "Enable 6 APIs | Slides / Drive / Docs / Sheets / Gmail / Calendar"; mirror the parenthetical "must match the OAuth scope set used in step 8". (b) Line 37 step 8 narrative — change `gws auth login --scopes=presentations,drive,documents,spreadsheets` → 6-comma-separated version with `gmail` + `calendar` appended; "then verify gws auth status …" preserved verbatim. (c) Line 53 re-auth fast-path narrative — same 6-scope command extension. (d) Add a single line to troubleshooting (or §"When to use the manual fallback" if more natural) noting: "Workspace accounts: if your org admin restricts Gmail/Calendar OAuth scopes you'll see `access_denied` after consenting — escalate to your Workspace admin." Preserve everything else verbatim.
- **Module**: `gws-toolkit/commands/gws-setup.md`
- **Files touched**: `gws-toolkit/commands/gws-setup.md`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/commands/gws-setup.md` (lines 33, 37, 53)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/scripts/gws/auto-setup.sh` (post-Part-1 truth — lines 86-89 scope consts + line 822 emit_result)
  - `/Users/kouko/GitHub/monkey-skills/docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md` (§Axis 4 Q1 — quote the admin-policy probe finding in the troubleshooting note)
- **Acceptance**:
  - **RED**: `grep -c '6 APIs' gws-toolkit/commands/gws-setup.md` currently returns `0`; `grep -c 'gmail' gws-toolkit/commands/gws-setup.md` currently returns `0`.
  - **GREEN**: `grep -c '6 APIs' …` returns `≥1`; `grep -c 'gmail' …` returns `≥3` (table row + 2 scope-string mentions); `grep 'Workspace admin' …` returns `≥1` (troubleshooting line landed); no surviving mention of "4 APIs" or the 4-element scope list outside historical / changelog context.
- **Dependencies**: none (parallel-eligible with T2, T3, T4 in Part 2)
- **Independent**: true
- **Brief item covered**: "commands/gws-setup.md … updated in one lock-step bump 0.6.0 → 0.7.0" + brief §Current State Evidence "Forward" sub-bullet

---

## Task 2 — Update skills/gws-setup/SKILL.md (Path A / Path B / re-auth narrative + error table)

- **Description**: In `gws-toolkit/skills/gws-setup/SKILL.md`: (a) Frontmatter `description:` line — append Gmail/Calendar to the (Slides + Drive + Docs + Sheets API) enumeration. (b) Line 109 Path A description — "enable 4 APIs" → "enable 6 APIs". (c) Line 142 Step 9 of Path B manual walkthrough table — `--scopes=presentations,drive,documents,spreadsheets` → 6-scope version; the "never use the recommended preset" cautionary aside preserved. (d) Line 250 every-7-days re-auth helper docstring — same 6-scope extension. (e) Error messages table (lines 269-280) — add ONE new row: `| 403 Forbidden + access_denied (Workspace) | Your Workspace admin restricts Gmail/Calendar OAuth scopes | Escalate to your Workspace admin; see [Axis 4 Q3 in v0.7.0 brief](../../../docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md) |`. Preserve all v0.6.0 dual-path narrative (Workspace Internal / personal External+Testing) verbatim — no logic change.
- **Module**: `gws-toolkit/skills/gws-setup/SKILL.md`
- **Files touched**: `gws-toolkit/skills/gws-setup/SKILL.md`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/skills/gws-setup/SKILL.md` (frontmatter line 3, lines 109, 142, 250, 269-280)
  - `/Users/kouko/GitHub/monkey-skills/docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md`
- **Acceptance**:
  - **RED**: `grep -c '6 APIs' gws-toolkit/skills/gws-setup/SKILL.md` returns `0`; `grep -c 'gmail' gws-toolkit/skills/gws-setup/SKILL.md` returns `0`; error table currently has 8 rows.
  - **GREEN**: `grep -c '6 APIs' …` returns `≥1`; `grep -c 'gmail' …` returns `≥4` (frontmatter + step 9 + every-7-days + error row); error table now has 9 rows (the new "Workspace admin OAuth restriction" row landed); frontmatter `description:` mentions Gmail + Calendar.
- **Dependencies**: none (parallel-eligible with T1, T3, T4)
- **Independent**: true
- **Brief item covered**: "skills/gws-setup/SKILL.md … updated in one lock-step bump" + brief §Current State Evidence "Error" sub-bullet (Workspace-admin-blocked Gmail/Calendar scopes row)

---

## Task 3 — Update skills/using-gws-toolkit/SKILL.md routing table (add Gmail + Calendar branches)

- **Description**: In `gws-toolkit/skills/using-gws-toolkit/SKILL.md`: locate the routing table (line ~28 region — table mapping user-language utterances to which vendored skill / toolkit-original skill to load). Add 2 new rows matching the existing row style verbatim (EN + JP + ZH trigger keywords + 1-line "→ load …" right-column): (a) Gmail row — keywords like "read my inbox / mail / メール / inbox triage / 信件 / 收信" → load `gws-gmail` (parent reference) + `gws-gmail-read` (general read ops); (b) Calendar row — "agenda / today's schedule / 行事曆 / カレンダー / free slot / event search" → load `gws-calendar` + `gws-calendar-agenda`. Preserve the existing 4 vendored-skill rows verbatim. No other section touched.
- **Module**: `gws-toolkit/skills/using-gws-toolkit/SKILL.md`
- **Files touched**: `gws-toolkit/skills/using-gws-toolkit/SKILL.md`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/skills/using-gws-toolkit/SKILL.md` (routing table circa line 28; existing row format is the style template)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/skills/gws-gmail/SKILL.md` (post-Part-1; confirms vendored skill name spelling)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/skills/gws-calendar/SKILL.md` (post-Part-1)
- **Acceptance**:
  - **RED**: `grep -c 'gws-gmail' gws-toolkit/skills/using-gws-toolkit/SKILL.md` currently returns `0`.
  - **GREEN**: `grep -c 'gws-gmail' …` returns `≥1`; `grep -c 'gws-calendar' …` returns `≥1`; routing table row-count for vendored-skill branches went from 4 to 6 (`grep -cE 'gws-(shared|drive|docs|slides|sheets|gmail|calendar)' …` returns ≥6 distinct rows); trilingual trigger keywords present on the new rows (EN + JP + ZH columns each non-empty per existing convention).
- **Dependencies**: none (parallel-eligible with T1, T2, T4; reads-only-dependency on Part 1 Task 5 for vendored skill names existing on disk — does not edit those files)
- **Independent**: true
- **Brief item covered**: "skills/using-gws-toolkit/SKILL.md routing table currently has 4 branches; v0.7.0 must add Gmail + Calendar branches" (brief §Current State Evidence "Reverse")

---

## Task 4 — Update README.md + TECH-SPEC.md (discoverability surface + plugin layout)

- **Description**: Two-file pair grouped because both surface the **vendored-skill inventory + scope-set narrative** for different audiences — README for end users / marketplace, TECH-SPEC for architecture readers. They MUST move together to prevent the user-facing-vs-architecture-doc drift v0.6.0 §[0.6.0] Part 2 T2 specifically warned against ("TECH-SPEC.md: no changes — plan stage expected to find Workspace caveats and remove them; grep found 0 matches" — same drift-prevention pattern applies to v0.7.0 doc updates). **(a) README.md**: line 5 banner — "drive / docs / slides / sheets" → "drive / docs / slides / sheets / gmail / calendar" (matches plugin.json description in Task 5). Line 153 — "First-time GCP Console / OAuth (4 scopes …)" → "(6 scopes: presentations + drive + documents + spreadsheets + gmail + calendar)". Lines 161-165 vendored skill table — add 2 new rows for `gws-gmail` (Gmail API v1 — messages / labels / threads / etc.) and `gws-calendar` (Calendar API v3 — events / calendars / freeBusy). Optionally add row for `gws-gmail-read` + `gws-calendar-agenda` if existing rows include sibling read-skills; otherwise mention them in the row text. Lines 230-231 ASCII tree — append `gws-gmail / gws-gmail-read / gws-calendar / gws-calendar-agenda` to the vendored-skills enumeration. **(b) TECH-SPEC.md**: §2.1 plugin layout tree — append the 4 new vendored skill paths under `skills/`. Append a new row to the Revision History table at the top documenting v0.7.0 (date 2026-05-19, scope: "Gmail + Calendar absorption: 6 APIs + 6 OAuth scopes (full r/w); 4 new vendored skills (gws-gmail / gws-gmail-read / gws-calendar / gws-calendar-agenda); pure-vendor strategy per v0.4.0 strangler-fig precedent; lock-step bump 0.6.0 → 0.7.0"). Existing prose unchanged.
- **Module**: `discoverability surface + architecture doc (lock-step pair)` — conceptual unit; both files describe "what services this toolkit covers" for two different audiences and drift between them is a documented v0.6.0 lesson.
- **Files touched**: `gws-toolkit/README.md`, `gws-toolkit/TECH-SPEC.md`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/README.md` (lines 5, 153, 161-165, 230-231)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/TECH-SPEC.md` (Revision History table near top, §2.1 plugin layout tree)
  - `/Users/kouko/GitHub/monkey-skills/docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md`
- **Acceptance**:
  - **RED**: `grep -c 'gws-gmail\|gws-calendar' gws-toolkit/README.md gws-toolkit/TECH-SPEC.md` currently returns `0` total across both files.
  - **GREEN**: same grep returns `≥6` total (README table rows ≥2 + ASCII tree ≥2; TECH-SPEC plugin layout ≥2); README banner + line 153 + table all say "6 scopes" / mention Gmail + Calendar; TECH-SPEC Revision History has a `v0.7.0` row dated `2026-05-19`; no `[OPEN]` markers introduced.
- **Dependencies**: none (parallel-eligible with T1, T2, T3)
- **Independent**: true
- **Brief item covered**: "README.md … TECH-SPEC.md … updated in one lock-step bump 0.6.0 → 0.7.0" (brief §Smallest End State item 6)

---

## Task 5 — Lock-step semver bump (plugin.json + CHANGELOG.md)

- **Description**: Closing-act task; gates the release marker. Two-file atomic edit because **a CHANGELOG entry without a matching `plugin.json` version (or vice versa) is the classic semver drift footgun**. (a) `gws-toolkit/.claude-plugin/plugin.json` — `"version": "0.6.0"` → `"version": "0.7.0"`; `description:` field append `/ gmail / calendar` to the service enumeration (mirrors README banner from T4); `keywords` array — append `"gmail"`, `"google-calendar"` (or `"calendar"`; pick whichever matches the existing kebab-case convention in the array). (b) `gws-toolkit/CHANGELOG.md` — add a new `## [0.7.0] - 2026-05-19` entry at the top (right under `[Unreleased]`), structured per Keep-a-Changelog with sections **Added** (4 new vendored skills + 2 OAuth scopes + 2 enabled APIs), **Changed** (auto-setup.sh + refresh-auth.sh + gws-login.sh + UPSTREAM_GWS_VERSION + sync-upstream-skills.sh + 5 doc files; itemize per-file like v0.6.0 did), **Notes** (full r/w scope rationale citing v0.4.0 precedent + iChef admin-policy probe finding pulled from brief §Axis 4 Q1; macOS Mail `https://mail.google.com/` was the green-light signal), **Open follow-ups** (v0.7.x backlog: `find-free-slots` + `shared-calendar-read` per brief §Axis 5 — no native upstream equivalent; toolkit-original wrappers + write-side vendored skills + `gmail-confirm-send.sh` / `calendar-confirm-insert.sh` app-layer safety wrappers when first write JTBD lands). Match v0.6.0's verbose entry style — readable as a self-contained release note 6 months later.
- **Module**: `release lock-step (semver bump + changelog)`
- **Files touched**: `gws-toolkit/.claude-plugin/plugin.json`, `gws-toolkit/CHANGELOG.md`
- **Context paths**:
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/.claude-plugin/plugin.json` (all fields)
  - `/Users/kouko/GitHub/monkey-skills/gws-toolkit/CHANGELOG.md` (lines 11-114 are `[Unreleased]` + `[0.6.0]` — model the v0.7.0 entry on v0.6.0's structure)
  - `/Users/kouko/GitHub/monkey-skills/docs/gws-toolkit/specs/2026-05-19-v0.7.0-gmail-calendar.md` (source for Added / Changed / Notes content)
- **Acceptance**:
  - **RED**: `jq -r '.version' gws-toolkit/.claude-plugin/plugin.json` returns `0.6.0`; `head -50 gws-toolkit/CHANGELOG.md | grep -c '\[0.7.0\]'` returns `0`.
  - **GREEN**: `jq -r '.version' …` returns `0.7.0`; CHANGELOG has a `## [0.7.0] - 2026-05-19` heading inside the first 50 lines; CHANGELOG `[0.7.0]` entry contains all 4 sections (Added / Changed / Notes / Open follow-ups); `jq -r '.description' …` mentions `gmail` AND `calendar`; `jq -r '.keywords | map(. == "gmail") | any' …` returns `true`; `jq -r '.keywords | map(test("calendar")) | any' …` returns `true`.
- **Dependencies**: Tasks 1, 2, 3, 4 complete first (CHANGELOG "Changed" section must enumerate per-file changes accurately; semver bump is the closing-act)
- **Independent**: false  # serializes after all 4 other Part-2 tasks
- **Brief item covered**: "plugin.json + CHANGELOG.md … one lock-step bump 0.6.0 → 0.7.0" (brief §Smallest End State item 6) + brief §"Brief metadata" "Version-bump drift between plugin.json and CHANGELOG.md [0.7.0] heading — 2 places, must match exactly"

---

## Notes for SDD orchestrator

- **Parallel dispatch eligibility**: Tasks 1, 2, 3, 4 are all `Independent: true` with disjoint `Files touched` sets — eligible for `dispatching-parallel-agents` fan-out in one assistant message (4 concurrent implementer subagents). Task 5 is the **serial closing act** — runs after all 4 other tasks DONE because the CHANGELOG "Changed" enumeration is most accurate when the implementer can `git diff --stat` the 4 earlier-landed file changes.
- **Cross-task coherence flags for whole-branch reviewer (Stage 6)**:
  - **6-API / 6-scope narrative alignment across docs**: Tasks 1, 2 (and partly 4) all carry the "6 APIs / 6 scopes" string in different framings (Path A bullet vs Path B table cell vs README banner). All must say "6", none must accidentally still say "4".
  - **Vendored-skill name spelling across docs**: T3 (router) + T4 (README+TECH-SPEC) + T5 (CHANGELOG) all enumerate the 4 new vendored skill names. Spelling must match the upstream filenames exactly (`gws-gmail`, `gws-gmail-read`, `gws-calendar`, `gws-calendar-agenda`).
  - **Version-bump drift**: T5's `plugin.json` `"version": "0.7.0"` and CHANGELOG `## [0.7.0]` heading must match. Whole-branch reviewer should diff `jq` vs `grep` heading.
- **Hand-off to Stage 4 (Verification)**: after Part 2 lands, run `bash gws-toolkit/scripts/gws/auto-setup.sh --dry-run` (asserts 6 APIs + 6 scope URLs); run all 4 convention scripts (`.claude/hooks/validate-skill-folder-structure.sh` etc.); dogfood `gws gmail users messages list --params '{"userId":"me","maxResults":1}'` + `gws calendar events list --params '{"calendarId":"primary","maxResults":1}'`. Refer to `verification-before-completion` skill at Stage 4.
