# Plan: wiki-ingest language policy — part 2 (commit 2 scope)

**Source brief**: docs/superpowers/specs/2026-05-18-wiki-ingest-language-policy-design.md
**Total tasks**: 5 (≤5 ✓)
**Execution order**: parallel-where-possible (3 waves; see Dependency graph)
**Plan-document-reviewer verdict**: PASS (2026-05-18 round-1, 14/14 checks; T1 Independent flipped to false post-PASS for semantic precision)

## Scope of this plan

**Commit 2** per brief §7. Builds on part-1 (commits `72922de..b216439`, all DONE on this branch — 4/4 PASS round 1). Adds SSOT infrastructure (`distribute.py` + `verify-drift.py`) + 4 functional copies of `language-policy.md` to sibling wiki-* skills + CI enforcement + wiki-lint new rule (slug ≠ body language → aliases required).

**Commit 2 deliverables**:
- `obsidian/scripts/distribute.py` (new) — sync `wiki-ingest/references/language-policy.md` byte-identical to 4 sibling skill `references/` directories, prepend 5-line SSOT header per `code-toolkit/_baseline.md` convention
- `obsidian/scripts/verify-drift.py` (new) — CI enforcer; exit non-zero if any sibling copy drifts from canonical
- 4 functional copies (auto-generated by running `distribute.py` once in T1's commit):
  - `obsidian/skills/wiki-cross-linker/references/language-policy.md`
  - `obsidian/skills/wiki-query/references/language-policy.md`
  - `obsidian/skills/wiki-lint/references/language-policy.md`
  - `obsidian/skills/wiki-auto-research/references/language-policy.md`
- `.github/workflows/test-obsidian.yml` — add `verify-drift` step
- `obsidian/skills/wiki-lint/SKILL.md` — add new lint rule: when slug language ≠ body language, frontmatter `aliases` is required; warning severity
- pytest smoke test for `distribute.py` + `verify-drift.py` round-trip

**Out of part-2 scope** (→ part-3):
- pytest `test_language_policy.py` CC-LL-01..05 (behavioral tests for SKILL.md prose)
- Dogfood on `~/kouko-obsidian-vault`
- Version bump `obsidian/.claude-plugin/plugin.json` 3.10.0 → 3.11.0
- PR title / body composition (handled by finishing-a-branch phase)

## Dependency graph

```
                  T1 (distribute.py + 4 functional copies)
                 /              |              \
                /               |               \
         T2 (verify-drift.py)   |        T5 (wiki-lint new rule)
              /                 |
             /                  |
       T3 (smoke pytest)    T4 (CI verify-drift step)
```

- **Wave 1**: T1 alone (foundation — script + initial copies)
- **Wave 2 (parallel)**: T2 (verify-drift.py, depends T1) + T5 (wiki-lint rule, depends T1) — different modules, disjoint files
- **Wave 3 (parallel)**: T3 (smoke test, depends T1 + T2) + T4 (CI step, depends T2) — different modules, disjoint files

---

## Task 1 — distribute.py + run-once to generate 4 functional copies

- **Description**: Create `obsidian/scripts/distribute.py` (Python ≥ 3.10, stdlib only — pathlib, sys, optional argparse): canonical source = `obsidian/skills/wiki-ingest/references/language-policy.md`; targets = `obsidian/skills/{wiki-cross-linker,wiki-query,wiki-lint,wiki-auto-research}/references/language-policy.md`. For each target: write canonical content + prepend a 5-line SSOT header (HTML-comment marker, mirroring `code-toolkit/scripts/_baseline.md` convention) that names canonical source path + tells reader "do not edit in place — managed by distribute.py". Run `python3 obsidian/scripts/distribute.py` once IN THIS TASK's commit; the resulting 4 functional copies are part of the same commit. Idempotent: re-running on already-distributed copies should produce no diff.
- **Module**: `obsidian/scripts/distribute.py`
- **Files touched**: `obsidian/scripts/distribute.py`, `obsidian/skills/wiki-cross-linker/references/language-policy.md`, `obsidian/skills/wiki-query/references/language-policy.md`, `obsidian/skills/wiki-lint/references/language-policy.md`, `obsidian/skills/wiki-auto-research/references/language-policy.md`
- **Context paths**:
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/skills/wiki-ingest/references/language-policy.md (canonical source — part-1 T1 output, commit `bfd8659`)
  - /Users/kouko/GitHub/monkey-skills/code-toolkit/scripts/distribute.py (mirror this pattern — pluggable canonical→targets table, SSOT header injection)
  - /Users/kouko/GitHub/monkey-skills/code-toolkit/scripts/_baseline.md (mirror SSOT header format — `<!-- BEGIN baseline-v1 — managed by code-toolkit/scripts/distribute.py from code-toolkit/scripts/_baseline.md — do not edit in place -->` style)
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/docs/superpowers/specs/2026-05-18-wiki-ingest-language-policy-design.md (§5 row 6 + row 9-12, §Alternatives §SSOT C)
- **Acceptance**:
  - **RED**: `ls obsidian/scripts/distribute.py` returns "No such file"; `ls obsidian/skills/wiki-cross-linker/references/language-policy.md` returns "No such file" (and same for wiki-query, wiki-lint, wiki-auto-research)
  - **GREEN**:
    - `ls obsidian/scripts/distribute.py` resolves; `python3 obsidian/scripts/distribute.py --help` exits 0 (script runs)
    - All 4 functional copies exist; each starts with the SSOT header
    - `diff <(tail -n +6 obsidian/skills/wiki-cross-linker/references/language-policy.md) obsidian/skills/wiki-ingest/references/language-policy.md` returns empty (byte-identical content after header strip)
    - Same diff PASSES for wiki-query, wiki-lint, wiki-auto-research
    - Re-running `python3 obsidian/scripts/distribute.py` produces no `git diff` (idempotent)
- **Dependencies**: none
- **Independent**: false  # wave-1 alone; no parallel sibling
- **Brief item covered**: §5 row 6 (distribute.py); §5 rows 9-12 (4 functional copies via distribute.py); §Alternatives "SSOT C distribute.py byte-identical"

## Task 2 — verify-drift.py CI enforcer

- **Description**: Create `obsidian/scripts/verify-drift.py` (Python stdlib only): reads canonical `obsidian/skills/wiki-ingest/references/language-policy.md`; for each of 4 sibling copies, strips the SSOT header (top 5 lines) and compares byte-identity against canonical. Exits 0 if all match; exits non-zero with diff summary if any drift. CI workflow (T4) will run this on every PR touching `obsidian/**`.
- **Module**: `obsidian/scripts/verify-drift.py`
- **Files touched**: `obsidian/scripts/verify-drift.py`
- **Context paths**:
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/scripts/distribute.py (T1 — read its canonical→targets table or hardcode same target list)
  - /Users/kouko/GitHub/monkey-skills/code-toolkit/scripts/verify-drift.py (mirror this pattern — exit-code-driven enforcer)
- **Acceptance**:
  - **RED**: `ls obsidian/scripts/verify-drift.py` returns "No such file"
  - **GREEN**:
    - `ls obsidian/scripts/verify-drift.py` resolves; `python3 obsidian/scripts/verify-drift.py --help` exits 0
    - `python3 obsidian/scripts/verify-drift.py` on the post-T1 worktree exits 0 (all copies in sync)
    - Manually corrupt one copy (e.g. echo "junk" >> wiki-cross-linker/.../language-policy.md), re-run verify-drift.py → exits non-zero with diff summary; revert the corruption before commit
- **Dependencies**: Task 1 completes first
- **Independent**: true
- **Brief item covered**: §5 row 7 (verify-drift.py)

## Task 3 — pytest smoke test for distribute.py + verify-drift.py

- **Description**: Create `obsidian/tests/scripts/test_distribute_smoke.py` (pytest, stdlib + pytest only). Tests:
  - (a) `test_distribute_idempotent`: run `distribute.py` twice in a tmp_path clone of the canonical + targets layout; assert no diff between runs
  - (b) `test_verify_drift_passes_on_sync`: run distribute.py once, then verify-drift.py → exit 0
  - (c) `test_verify_drift_catches_drift`: distribute.py once, then manually mutate one copy, run verify-drift.py → exit non-zero
  - (d) `test_ssot_header_present`: post-distribute.py, every target file's first 5 lines contain the SSOT marker ("managed by distribute.py")
- **Module**: `obsidian/tests/scripts/test_distribute_smoke.py`
- **Files touched**: `obsidian/tests/scripts/test_distribute_smoke.py`, `obsidian/tests/scripts/__init__.py` (if needed for pytest discovery; verify via running)
- **Context paths**:
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/scripts/distribute.py (T1)
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/scripts/verify-drift.py (T2)
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/tests/wiki_ingest/test_select_batch.py (sibling test pattern — pytest harness from v3.10.0)
- **Acceptance**:
  - **RED**: `pytest obsidian/tests/scripts/ -v` collects 0 items
  - **GREEN**: `pytest obsidian/tests/scripts/ -v` shows 4 PASSED tests (a/b/c/d above)
- **Dependencies**: Task 1 completes first; Task 2 completes first
- **Independent**: true
- **Brief item covered**: §6 Test plan "distribute.py + verify-drift.py 自帶 smoke test"

## Task 4 — CI workflow add verify-drift step

- **Description**: Edit `.github/workflows/test-obsidian.yml` (created in v3.10.0 PR #307, currently runs only pytest). Add a new step BEFORE the existing pytest step:
  ```yaml
  - name: Verify SSOT drift (language-policy.md sync)
    run: python3 obsidian/scripts/verify-drift.py
  ```
  Reuses same Python 3.11 setup. No additional deps needed (stdlib only).
- **Module**: `.github/workflows/test-obsidian.yml`
- **Files touched**: `.github/workflows/test-obsidian.yml`
- **Context paths**:
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/.github/workflows/test-obsidian.yml (current state — single pytest step)
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/scripts/verify-drift.py (T2)
- **Acceptance**:
  - **RED**: `grep -c 'verify-drift' .github/workflows/test-obsidian.yml` returns 0
  - **GREEN**:
    - `grep -c 'verify-drift' .github/workflows/test-obsidian.yml` ≥ 1
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/test-obsidian.yml'))"` exits 0 (valid YAML); or via `yq` if PyYAML unavailable
    - Existing pytest step preserved
- **Dependencies**: Task 2 completes first
- **Independent**: true
- **Brief item covered**: §5 row 8 (CI workflow verify-drift step)

## Task 5 — wiki-lint new rule (slug ≠ body language → aliases required)

- **Description**: Edit `obsidian/skills/wiki-lint/SKILL.md`: add a new lint rule per design brief §5 row 5 + §4 frontmatter conditional aliases. Rule semantics: scan every wiki/*.md page; detect slug language vs body language; when they differ (typical case: ASCII slug + zh-TW/ja/ko body), check frontmatter contains non-empty `aliases:`; if missing, report a warning (severity SHOULD per design Open Q3). The lint rule prose should reference `references/language-policy.md` (functional copy already in wiki-lint/references/ from T1) for slug vs body authority definitions. Don't redesign wiki-lint's checks list architecture — just add a new check entry per existing convention.
- **Module**: `obsidian/skills/wiki-lint/SKILL.md`
- **Files touched**: `obsidian/skills/wiki-lint/SKILL.md`
- **Context paths**:
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/skills/wiki-lint/SKILL.md (current state — 11 health checks per skill description)
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/obsidian/skills/wiki-lint/references/language-policy.md (T1 functional copy — for rule prose to reference)
  - /Users/kouko/GitHub/monkey-skills/.worktrees/wiki-ingest-language-policy/docs/superpowers/specs/2026-05-18-wiki-ingest-language-policy-design.md (§4 aliases conditional MUST; Open Q3 severity SHOULD vs MUST — MVP picks SHOULD)
- **Acceptance**:
  - **RED**: `grep -ciE 'slug language|aliases required|language-policy.md' obsidian/skills/wiki-lint/SKILL.md` returns 0
  - **GREEN**:
    - `grep -ciE 'slug.*language.*body.*language|aliases.*required' obsidian/skills/wiki-lint/SKILL.md` ≥ 1 (new rule prose present)
    - `grep -c 'language-policy.md' obsidian/skills/wiki-lint/SKILL.md` ≥ 1 (rule references the policy doc)
    - `grep -ciE 'warning|SHOULD' obsidian/skills/wiki-lint/SKILL.md` (severity context — warning preferred per Open Q3 MVP decision) — implementer's call to verify presence
    - Lint rule total count goes from 11 → 12 (per existing skill description; if SKILL.md has explicit count it should bump, otherwise just verify a new check entry was added in the lint checks list)
- **Dependencies**: Task 1 completes first
- **Independent**: true
- **Brief item covered**: §5 row 5 (wiki-lint new lint rule); §4 aliases conditional MUST (lint enforcement); Open Q3 (severity SHOULD/warning)

---

## Notes

- **Wave 1 alone**: T1 is the foundation; everything depends on it (script + initial copies must land before others touch verify / lint / CI).
- **Wave 2 parallel**: T2 + T5 both depend on T1 only; different modules (`obsidian/scripts/verify-drift.py` vs `obsidian/skills/wiki-lint/SKILL.md`); disjoint Files touched. SDD via `dispatching-parallel-agents` MAY dispatch both implementers in one message.
- **Wave 3 parallel**: T3 + T4 both depend on T2 (T3 also on T1, T4 also on T1 transitively); different modules (`obsidian/tests/scripts/test_distribute_smoke.py` vs `.github/workflows/test-obsidian.yml`); disjoint Files touched.
- **T1 has multi-file Files touched** (script + 4 copies) but conceptually atomic: distribute.py landing without its initial copies wouldn't make sense (CI would fail immediately). The 4 copies are deterministic outputs of T1's script; no human-author content in them.
- **No pytest CC-LL fixtures in part-2** — those test SKILL.md behavior (which mostly lives in language-policy.md prose) and go to part-3 alongside dogfood.
- This plan covers **commit 2 only** of the brief's 3-commit plan (§7). part-3 will cover pytest CC-LL-01..05 + dogfood + version bump v3.11.0.

## Out of scope (for part-3 plan)

| Brief item | Deferred to |
|---|---|
| pytest `test_language_policy.py` CC-LL-01..05 (behavioral fixtures for language policy semantics) | part-3 |
| Dogfood on `~/kouko-obsidian-vault` (real-vault verification of language-policy + lint behavior) | part-3 |
| Version bump `obsidian/.claude-plugin/plugin.json` 3.10.0 → 3.11.0 | part-3 |
| PR title / body composition + finishing-a-branch close-out | finishing-a-branch phase |

## Open questions surfaced (track to part-3)

1. **SSOT header format precise lines** (T1 implementer choice) — exact wording of the 5-line `<!-- BEGIN ... -->` ... `<!-- END ... -->` markers. Mirror `code-toolkit/scripts/_baseline.md` style but the BEGIN line names language-policy + canonical path. T1 implementer authority — surface in self_review.
2. **Wiki-lint check-list count semantics** (T5) — current SKILL.md says "12 health checks" (or 11 — varies; recon needed). If the count is explicit, T5 must bump by 1. If implicit, T5 just adds the new check entry.
3. **wiki-lint severity for aliases-missing** (brief Open Q3) — MVP per design = SHOULD (warning). Implementer in T5 should mark as warning (not error) and document upgrade path to MUST in Phase 2.
4. **distribute.py argparse vs hardcoded mapping** (T1 implementer choice) — for MVP, hardcoded canonical→targets mapping is simplest (4 targets, all in `obsidian/skills/wiki-*/references/language-policy.md`). argparse adds flexibility but YAGNI for 4 fixed copies. Implementer authority.
