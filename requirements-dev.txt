# Shared development and test dependencies.
# One package per line, its reason on the line above it.

# the test runner
pytest
# `-n auto` in the package-tests line: one worker per detected available CPU (200 s -> ~35 s on a 16-CPU laptop)
pytest-xdist
# contract manifest / mechanisms.yaml parser; test modules fail at collection without it
pyyaml
# adjudication_render and its tests
markdown-it-py
# width-aware CJK diagram checks used by the package suite
wcwidth
