---
name: strategy-dag
description: |
  Single-user decision-reasoning assistant — turns a discussion into a transparent chain of thought: one markdown file per node, assumption files with stale propagation, silent mechanical gates, and a regenerated DAG view. Use for 'help me decide X' or 'decision reasoning'. Part 1 draft — core protocol not yet wired.
source_language: en
tags: [decision-making, chain-of-thought, dag, reasoning, assumptions, part-1-draft]
---

Part 1 draft — protocol lands in T11. This stub only reserves the skill's
name, description, and folder shape; the intake flow, the three interrupt
points (GOAL / assumptions / DECISION), the node-writing rules, and the
`dag.py` CLI wiring (`check` / `break` / `claims` / `render`) are not yet
implemented here — do not rely on this skill for a real decision until
Task 11 replaces this body.
