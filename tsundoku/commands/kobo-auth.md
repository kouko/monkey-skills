---
description: "Set up Kobo login for tsundoku — install kobodl binary, run device-flow activation (kobo.com/activate + 6-digit code), or import existing credentials. macOS only."
---

Use the tsundoku:kobo-auth skill to handle this request.
