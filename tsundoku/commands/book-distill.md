---
description: "Distill a book (already converted to Markdown by book-extract) into a coherent set of executable agent skills via the RIA-TV++ pipeline (Adler analytical read → 5 parallel extractors → triple verification → RIA++ render → Zettelkasten linking → adversarial pressure test)."
---

Use the tsundoku:book-distill skill to handle this request.
