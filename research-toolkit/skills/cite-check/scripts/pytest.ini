[pytest]
pythonpath = .
# Redirect pytest cache outside the skill directory so .pytest_cache/ doesn't
# violate the flat-subfolder skill structure rule (the hook bans depth>=2 dirs
# under a skill root; matches the deep-research convention).
cache_dir = /tmp/pytest_cache_cite_check
